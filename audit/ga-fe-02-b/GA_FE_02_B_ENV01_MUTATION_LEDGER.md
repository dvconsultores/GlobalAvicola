# GA-FE-02-B · ENV-01 MUTATION LEDGER

**Regla**: toda mutación de entorno (ENV-01) autorizada por GA-FE-02-B se registra aquí con
timestamp, actor/herramienta, target, BEFORE, operación, AFTER, reversibilidad, rollback, AC y
evidencia. **Sin credenciales, sin passwords, sin tokens.**

| # | Fecha | Actor/Tool | Target | BEFORE | Operación | AFTER | ¿Reversible? | Rollback | AC | Evidencia |
|---|---|---|---|---|---|---|---|---|---|---|
| 1 | 2026-09-11 | Agente · API oficial `POST /roles` (super admin, sin contexto) | ENV-01 · rol «Administrador de Accesos» | Ausente (13 roles activos) | Creación con los 4 permisos canónicos `business_units:read|update|create|delete` (scope `all`) | **id=35** · `company_id NULL` (plantilla de sistema) · exactamente 4 permisos; `GET /roles` total=14; único rol con módulo `business_units` | Sí — `PUT /roles/35` | Mantener como fixture canónico (rol de producto `R-113`/`OD-15 §6`) | AC-F2 | `ROLE_HTTP=201` + verificación `GET /roles`; respuesta en `/tmp/ga_role_create.json` (600) |
| 2 | 2026-09-11 | Agente · API oficial `PUT /users/{id}` (super admin situado en empresa 1) | ENV-01 · usuarios **57–70** (14: 7 móvil + 7 web) | `email=…@testing.local` ⇒ `GET /users/{id}` = **500** ×14; `GET /users` ≥10 filas = 500 | Reparación mínima del dominio del email (parte local = username; nuevo dominio `globalavicola.com`) | `PUT` = **200** ×14 con `username` verificado contra el seed (sin desviaciones) · `GET` = **200** ×14 · `limit=100` = 200 (23 usuarios) · default = 200 | Sí — restaurar `@testing.local` (valor original conocido) | Corrección de fixture malformada (invariante documentado en `baseline_seeds`) | AC-F3 | Antes: `/tmp/ga_f3_before.txt`; reparación: `/tmp/ga_f3_repair.txt`; respuestas por fila `/tmp/ga_f3_rep_*.json` (600). Nota: el primer barrido usó token sin contexto → 403/404 y el guard cortó SIN ninguna mutación; se repitió con `switch-company` a empresa 1 |
| 3 | 2026-09-11 | Agente · commit `b83d908` (repo; sin tocar ENV) | `backend/seeds/integration_seeds.py` + test de regresión | Plantilla de emails `@testing.local` | Dominio válido `@globalavicola.com` + guard de dominios reservados | Semilla endurecida (grep de reservados: 0 coincidencias) | Sí — revert del commit | Evitar recreación de la fixture (R-44) | AC-F3 | El runner backend exige `GA_TEST_ENV` + `GA_TEST_DATABASE_URL` aislados (no disponibles localmente); el test corre en CI de PRs |
| 4 | 2026-09-11 | Agente · commit `716d175` (F4) + deploy por pipeline | Frontend (`Header`/`auth.store`/locales/test) | Selector no renderizado sin nombre; nombre sin resolver con persistida `null` | Fix mínimo (condición de render + resolución de nombre) | RED→GREEN; TSC 0; Vitest 205/205; build OK | Sí — revert (deploy restaura) | — (sin mutación de ENV) | AC-F4 | Verificación runtime PASS (selector «Seleccionar empresa»; dropdown 2 empresas; switch a Avícola Global C.A. 200; nombre en header; /admin/unit-access «Empresa: Avícola Global C.A.»; hard-refresh `/me` 200 sin forbidden; móvil 390×844) — bundle `index-B2-tZnkI.js` |
| 5 | 2026-09-11 | Agente · verificación API (SIN mutación) | ENV-01 · catálogo `BusinessUnit` (post-reporte del propietario) | `GET /business-units` c1 → `[]`; candidatos 4×404 | Verificación de postcondición del gate F1 (tranche GA-FE-02-A; no se ejecutó ninguna mutación) | **SIN CAMBIO** — `count=0` en c1 y c3; `is_active default=True` descarta «inactivas» ⇒ la ejecución reportada no alcanzó la base de ENV-01 | — (sin mutación) | — | AC-F1 | **Retry + self-check (owner/ops, en el host que sirve `avicola.globaldv.net`)**: `docker exec -i globalavicola-backend python - <<'PY' … sembrar_unidades_de_negocio + count/codes … PY` → esperado `creadas: 4 · filas_catalogo: 4 · codigos: [breeder, broiler, grandparent, hatchery]`. Causas probables: otra base (ver `docker logs --tail 20 globalavicola-backend` → «target: host/db»), otro host, o error no leído. Tras el éxito: `GET /business-units` → 4 · **re-verificado 3.ª vez (2026-09-11): sigue `[]` (`CATALOGO_VACIO`); login/switch 200 — el reporte del propietario continúa sin ser observable en ENV-01. Diagnóstico `DB_TARGET` entregado en el informe.** |
| 6 | 2026-09-11 | Agente · decisión gobernada `GA-FE-02-C §2` (SIN mutación todavía) | F1 · catálogo `BusinessUnit` | `GET /business-units` = `[]` (3.ª verificación; ruta manual agotada: `BLOCKED_SERVER_ACCESS_F1`) | **Remediación autónoma por migración de datos Alembic `y5z6a7b8c9d0`** (`down_revision = x4y5z6a7b8c9`): siembra determinista del catálogo en el **arranque normal** del backend (`docker-entrypoint.sh` → `alembic upgrade head`, `GA-REM-024`); seed importa la fuente única (patrón RBAC `l2m3n4o5p6q7`). Cero docker exec / SSH / SQL manual / cambios de despliegue | **PENDIENTE DE DESPLIEGUE** — postcondición exigida: `GET /business-units` → `count=4` `[breeder, broiler, grandparent, hatchery]`, `is_enabled=false`; sin CBU/UBU/roles/permisos nuevos (AC-F1-06…12) | Sí — revert del commit de migración **antes** de su aplicación; tras aplicarse: filas baseline aditivas, **downgrade NO-OP intencional** documentado (spec §4.2) | Revert C2 (pre-aplicación); post-aplicación: no-op justificado | AC-F1-01…17 | COMMIT C1 (spec §4.2) + C2 (migración + test dirigido); verificación post-deploy §4.2.1 |

| 7 | 2026-09-11 | Pipeline normal (push `b4d8c3a` → imagen backend → Watchtower → entrypoint `alembic upgrade head`, `GA-REM-024`) | ENV-01 · catálogo `BusinessUnit` | `GET /business-units` c1/c3 = `[]` (3.ª verificación) | Aplicación no manual de la migración `y5z6a7b8c9d0` (siembra determinista del catálogo; `creadas 4`) | **4 filas canónicas**: c1 = `breeder·broiler·grandparent·hatchery` (`is_enabled=false`); c3 = idéntico (catálogo global, sin duplicación); candidatos 404×4 (contrato); rol 35 intacto; `GET /users`=200 (23; 57–70 presentes); CBU/UBU sin cambio | Sí — revert del commit (post-aplicación: filas baseline; downgrade NO-OP justificado) | — | AC-F1-01…17 | Commit C1 `28e95bb` + C2 `b4d8c3a`; verificación runtime post-deploy (2026-09-11): 4/4 códigos exactos, `name_key` canónicos, sin quinta unidad; test dirigido RED→GREEN 6/6; §4.2.2 |
| 8 | 2026-09-11 | Agente · API oficial (GA-FE-02-C §28–37) | ENV-01 · fixtures de certificación (empresa 1 y 3) | Sin actores de prueba | Alta de **5 usuarios sintéticos** (`ga-fe02-a`=71 · `b`=72 · `c`=73 · `d`=74 · `x`=75, este en empresa 3) + **3 roles temporales** (36/37/38, solo permisos canónicos existentes; B usa el rol canónico 35) + credenciales aleatorias efímeras (solo `/tmp`, nunca impresas/commiteadas) | 5 logins independientes 200 (`/me` 200 cada uno); permisos exactos por actor; B con rol 35 sin asignaciones humanas | Sí — baja de usuario (`DELETE /users/{id}`) y desactivación de roles (`PUT /roles/{id}`) | — | §30–§37 · AC actores | Matriz de cuentas (adenda C) + red/persistencia de la corrida |
| 9 | 2026-09-11 | Agente · UI/API oficial (corrida E2E GA-FE-02-C) | ENV-01 · empresa de prueba 1 · BU `broiler` · concesiones C/D | CBU 4×OFF; sin concesiones vivas | E2E-01…10 + matriz 3D: enable/disable CBU ×3/×3 · concesión C ×2 y revoke ×2 · concesión D ×1 y revoke ×1 (MX-3) · intentos denegados: auto-concesión **403**, cross-company **404**, escritura global **403**, D **403×4** | **Restaurado**: CBU 4×OFF; sin concesiones vivas (C/D/B/X); historia de concesiones conservada (`revoked_at`); auditoría +14 `config_change`/company_business_unit +15 `permission_change`/user_business_unit | Sí — cada mutación restaurada por flujo oficial | — | §40–66 · matriz 3D 4/4 | `GA_FE_02_A_E2E_MATRIX.md` (resultados reales) · red · capturas · evidencia de certificación |
| 10 | 2026-09-11 | Agente · API oficial (cierre §76/§77) | ENV-01 · fixtures de certificación | Usuarios 71–75 activos; roles 36/37/38 activos | Baja de los 5 usuarios sintéticos (204×5; login posterior **403**) + desactivación de roles 36/37/38 (`is_active=false`) | 0 usuarios sintéticos activos (histórico conservado); rol canónico **35 intacto y activo** (4 permisos exactos); roles canónicos 14 activos; credenciales efímeras destruidas (`/tmp`) | n/a (fixtures test-only; documentado) | — | §76–§77 | Respuestas 204 · `GET /roles` (activos=14 canónicos) · `GET /users` (`is_active=false` en los sintéticos) · login 403 |
| 11 | 2026-09-11 | Agente · flujos oficiales (GA-FE-02-D: verificación + MX de la matriz afectada) | ENV-01 · empresa 1 · BU `broiler` · fixture normal `ga-fe02-c2` | CBU 4×OFF; sin concesiones vivas (estado post-GA-FE-02-C) | Batería OD-16: toggles `broiler` ON/OFF ×3 (control ON, verificación, MX); fixture normal re-provisionado: rol 37 reactivado temporalmente + `ga-fe02-c2` creado → concedido → revocado → **dado de baja**; rol 37 desactivado | **Restaurado**: CBU 4×OFF; sin concesiones vivas; C2 inactivo; rol 37 inactivo; rol 35 intacto; batería **21/21** (OFF→0/404 en todo lo productivo; ON→solo habilitadas) + **MX 12/12** (ON/NO/DENY · ON/YES/ALLOW) | Sí — todo por flujo oficial | — | AC-D1-01..08 | `audit/ga-fe-02-d/GA_FE_02_D_OD16_GLOBAL_READ_RECONCILIATION.md` §13 |

## Política

- **D-1 / OD-16 (GA-FE-02-D)**: verificación por flujos oficiales — toggles de `broiler` y fixture normal re-provisionado, todo restaurado. Ver entrada #11.

- **F1** (catálogo): **desde `GA-FE-02-C §2`, dato determinista de despliegue** — migración
  `y5z6a7b8c9d0` aplicada por el arranque normal del backend (`alembic upgrade head`,
  `GA-REM-024`). Al verificarse el deploy, registrar: revisión aplicada, postcondición
  `GET /business-units` (=4), efectos colaterales (0 CBU/UBU/roles/permisos nuevos) e
  idempotencia (test dirigido + arranques del pipeline). Sin pasos manuales de servidor.
- **F2** (rol): `POST /roles` (API oficial, super admin sin contexto → plantilla `company_id NULL`).
  Reversible (`PUT /roles/{id}`); estado preferido del fixture: rol activo canónico.
- **F3** (emails): `PUT /users/{id}` por fila (57–70). Reversible a `{username}@testing.local`
  (valor original conocido). Solo dominio del email; parte local conservada; sin tocar credenciales.
- **F4**: sin mutación de ENV (código; deploy por pipeline).
- **Actores A–E** (§8 spec): alta por `POST /users` (API oficial) con passwords generadas fuera del
  repo; asignación de rol canónico; concesiones por `POST /users/{id}/business-units`. Reversible:
  desactivar usuario (`DELETE /users/{id}`), revocar concesiones (`DELETE .../business-units/{code}`).
- **Concesiones/habilitaciones E2E**: por flujos oficiales; restauración documentada al cierre;
  las filas `CompanyBusinessUnit` deshabilitadas permanecen («apagada explícitamente ≠ nunca
  configurada») y se documentan como artefacto residual legible.
