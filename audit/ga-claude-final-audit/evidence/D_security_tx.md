# D · Auditoría de seguridad / multiempresa / BU / RBAC / sesión / transacciones / idempotencia / concurrencia — backend

**Repositorio** `/home/maria/Proyectos/GlobalAvicola` · **Alcance** `backend/app` (solo lectura) · **Fecha** 2026-09-13 · **HEAD** `c0b4afc` (GA-F01 C3)
**Método** lectura directa de los caminos de código (no de la documentación), grep transversal, cruce con `backend/tests/*.py` y `audit/remediation/REMEDIATION_BACKLOG.md`. No se ejecutó ningún test ni build.

Cadena canónica verificada: `TENANT` → `COMPANY BU ENABLED` (`company_business_units.is_enabled`) → `USER BU GRANT` (`user_business_units.revoked_at IS NULL`) → `RBAC` (`require_permission`, comodín `("*", all)`) → `PERTENENCIA / REGLA DE NEGOCIO`.

Leyenda de veredicto: **PASS** · **PARTIAL** · **FAIL** · **UNKNOWN** (no verificable en estático). Rutas de fichero abreviadas a `app/…` = `backend/app/…`; tests a `tests/…` = `backend/tests/…`.

---

## 0. Punto de entrada de la cadena (común a todos los controles)

| Control | Veredicto | Evidencia (file:line) | Cobertura de tests | Notas / hueco |
|---|---|---|---|---|
| Autenticación por petición: JWT decodificado y **usuario recargado de BD** con `is_active` | PASS | `app/auth/security.py:72-104` (`get_current_user`: `select(User)`, `if not user or not user.is_active → 401`) | `tests/test_rbac.py::test_ac06_*`, `tests/test_security_regression.py` | El usuario desactivado pierde acceso en la **siguiente petición** (no depende del token). |
| Permisos leídos de BD por petición (no del JWT) | PASS | `security.py:111-121` (`user.role.permissions`, `lazy="selectin"` en `auth/models.py:33,47`); `tiene_permiso` `security.py:199-209` | `tests/test_role_administration.py::test_t_092_05_el_permiso_concedido_surte_efecto`, `tests/test_rbac.py` | El JWT solo lleva `sub, username, company_id, role_id, view_type` (`auth/service.py:24-41`); `role_id` del token **no** se usa para autorizar. |
| Empresa efectiva (`OD-11`) resuelta por petición; un usuario normal no puede reclamar otra empresa | PASS | `app/tenancy.py:258-307` (`resolver_empresa_efectiva`: `if not puede_cambiar: return persistida`; empresa destino debe existir y estar activa) | `tests/test_multicompany_isolation.py::test_un_usuario_normal_no_desplaza_su_contexto`, `::test_el_super_admin_no_puede_situarse_en_una_empresa_inexistente`, `tests/test_session_payload.py::test_h13_el_usuario_corriente_no_falsifica_la_empresa` | — |
| Cobertura de autorización en arranque (`AC08`): toda ruta declara permiso o exención | PASS | `app/authorization_coverage.py:94-115`; invocado en `app/main.py:174-176`; `RUTAS_PUBLICAS` = 7 (`:21-29`), `RUTAS_DE_TITULAR` = 8 (`:42-59`) | `tests/test_rbac.py::test_ac08_*`, `::test_ac08b_*` | — |
| Frontera transaccional en arranque (`GA-REM-026 AC11`) | PASS | `app/transaction.py:76-104`; `main.py:181-183` | `tests/test_transaction_boundary.py::test_t_026_10/11` | — |
| Clasificación de alcance BU por ruta (`AC-C15`) | PASS (declarativo) | `app/business_units/route_scope.py:220-252`; `main.py:189-191` | `test_ac_c15_toda_ruta_esta_clasificada` (citado en OD-16 §9) | **Clasificar no protege**: `unidad_requerida()` (`route_scope.py:215-217`) **no es invocado por ninguna ruta ni servicio** (grep vacío). La única `UNIDAD_UNICA` productiva, `/reports/kpis/hatchery`, no está gateada por esa declaración (ver B.7). |

---

## A. Aislamiento de inquilino por módulo

| Control | Veredicto | Evidencia (file:line) | Cobertura de tests | Notas / hueco |
|---|---|---|---|---|
| A.1 `auth` · usuarios list/get/update/deactivate acotados a empresa efectiva; fail-closed sin empresa | PASS | `app/auth/service.py:79-114` (`_acotar`, `_usuario_alcanzable`), `:291-335`, `:384-421`, `:491-505` | `tests/test_user_tenant_isolation.py`, `tests/test_od14_productive_surfaces.py`, `tests/test_role_tenancy.py::test_r05_*` | `update_user` valida `role_id` antes de mutar (`:411-415`). `UserUpdate` `extra=forbid` y sin `company_id` (`auth/schemas.py:65-85`). |
| A.2 `auth` · alta de usuario: empresa resuelta del contexto, no del cuerpo | PASS | `auth/service.py:349-361` (`data.company_id` ignorado salvo `actor is None`, que ninguna ruta usa) | `tests/test_user_tenant_isolation.py` (R-118) | — |
| A.3 `auth` · roles: listado/edición por inquilino, plantillas de sistema visibles y no editables | PASS | `auth/service.py:116-171`, `:562-658` | `tests/test_role_tenancy.py::test_r01…r07` | — |
| A.4 `auth` · **un actor de empresa no puede fabricar ni asignar autoridad global** (`OD-13.c`, `AC-R06`) | **FAIL** | `create_role` (`auth/service.py:585-619`) y `update_role` (`:621-658`) **no validan el contenido** de `permissions`: un actor con `users:create` crea un rol de inquilino (`company_id=empresa`) con `{"module":"*","scope_type":"all"}`. `_rol_asignable` (`:116-142`) solo consulta `_es_autoridad_global` para roles con `company_id IS NULL` (`:139-141`); para un rol de inquilino devuelve `rol.company_id == empresa` (`:142`) → asignable. `get_current_user` (`security.py:119-120`) marca `is_super_admin=True` por el permiso, **sin mirar `Role.company_id`** → el usuario obtiene autoridad global y `switch-company` a cualquier empresa (`auth/service.py:507-540`). | `tests/test_role_tenancy.py::test_r06_*` solo prueba asignar la **plantilla** global (`rol_global`), no un rol de inquilino con comodín. **NO TEST** del camino real. | Ver GAP-01. |
| A.5 `masters` · `_apply_company_filter`: `companies` acotada por identidad, control global solo para `_CONTROL_GLOBAL`, fail-closed sin empresa | PASS | `app/masters/service.py:54-97` | `tests/test_master_tenant_isolation.py` (R-115, R-116, OD-14), `tests/test_company_catalog.py` | Catálogos con `company_id NULL` (compartidos) **no** aparecen en el listado del inquilino (`:95-96` filtra `== company_id`) aunque `verificar_catalogo_de_empresa` (`tenancy.py:114`) los acepta como referencia: inconsistencia lectura/escritura, no fuga. |
| A.6 `masters` · cadena granja→galpón, planta→incubadora (`_PADRES_TENANT`) | PASS | `masters/service.py:187-206`; `masters/router.py:132-174` (`get_houses_by_farm`, `get_incubators_by_hatchery` con `company_id is None → 404`) | `tests/security/test_multitenant_isolation.py::test_no_se_puede_crear_un_galpon_en_una_granja_ajena`, `tests/test_od14_productive_surfaces.py::test_s07/s08_*`, `tests/test_master_management.py::test_t_090_04` | — |
| A.7 `masters` · **`company_id` fijable desde el cliente en Create/Update** | **FAIL** (registrado) | `masters/schemas.py:64-65` (`FarmBase.company_id: int`), `:124-125` (`HatcheryBase`), `:188,249,268,286,306,324,341,358,377,394,412,632` (Create con `company_id`), `:436,446,465,474,485,493,503,651` (Update con `company_id: Optional[Any]`). `MasterService.create` solo lo impone **cuando llega nulo** (`masters/service.py:232-234`); `update` aplica `setattr` sobre todo lo enviado (`:248-254`) → un maestro propio puede moverse a otra empresa o a `NULL` (= compartido para `verificar_catalogo_de_empresa`). | `tests/test_multicompany_isolation.py` fixture usa `company_id` propio; **ningún test** envía `company_id` ajeno en `POST/PUT /masters/*` (grep). **NO TEST** | **EXISTING: `R-50`** (`audit/remediation/UPDATE_SCHEMA_SECURITY_MATRIX.md:59,105`, «mitigado por RBAC»: `masters:create/update` exclusivos de Super Admin en la semilla `alembic/versions/l2m3n4o5p6q7:45-107`). `R-50` **no figura** en `REMEDIATION_BACKLOG.md`. La mitigación decae en cuanto un rol de inquilino reciba `masters:create/update` (GA-REM-034 lo permite). Ver GAP-05. |
| A.8 `masters` · curvas de peso: tenencia por la línea, sin empresa → 404 | PASS | `app/masters/curves.py:66-87` | `tests/test_od14_productive_surfaces.py::test_s06_*`, `tests/test_genetic_curves.py` | — |
| A.9 `lots` · list/get/update/close/phases/opening-balance por empresa (+unidad) | PASS | `app/lots/service.py:250-273` (vía `MasterService` con `unidades`), `:411-436`, `:438-443`, `:661-697` | `tests/test_lot_row_scope.py`, `tests/test_multicompany_isolation.py::test_idor_*` | — |
| A.10 `lots` · alta: pertenencia de `farm_id`, `area_id` | PARTIAL | `lots/service.py:320-329` (granja: `403` si ajena), `:341-347` (área: `verificar_catalogo_de_empresa`) | `tests/test_lot_area_ownership.py::test_ga06a_01/02` | Ver A.11. |
| A.11 `lots` · **alta/edición no verifican `house_id`, `genetic_line_id` ni `weight_curve_id`** | **FAIL** | `lots/service.py:362-392` asigna `house_id=data.house_id`, `genetic_line_id=data.genetic_line_id` sin `verificar_pertenencia`; `_curva_del_lote` (`:275-307`) solo comprueba `curva.genetic_line_id == genetic_line_id`, nunca la empresa de la línea; `update_lot` → `MasterService.update` cuyo `_PADRES_TENANT` es `{farm_id, hatchery_id}` (`masters/service.py:187`), `LotUpdate` admite `house_id`, `genetic_line_id` (`lots/schemas.py:65-67`). Efecto: lote de A ubicado en galpón de B (clase estructural `R-42`/`R-59`/`R-180`) y evaluación de pesos contra la **curva de B** con sus `expected_min/max` expuestos en `GET /operations/{id}/weight-evaluation` (`operations/service.py:744-824`). | **NO TEST** (grep en `tests/` sin caso de `house_id`/línea ajena en `POST/PUT /lots`) | No registrado en backlog (grep `house_id.*lot`, `línea genética ajena`: vacío). Ver GAP-06. |
| A.12 `lots` · egg-batches / chick-batches (contrato de traspaso) | PASS | `app/lots/router.py:239-331` (`verificar_vinculo_generacional` + origen al alcance vía `get_lot` + `validar_flujo`) ; `app/tenancy.py:204-253` | `tests/test_handoff_contract.py`, `tests/test_traceability_ownership.py` | El destino se lee sin unidad **a propósito** (`_lote_destino` `:303-314`); su empresa ya la comprobó `verificar_vinculo_generacional`. |
| A.13 `operations` · create/list/get/update/cancel/submit acotados a empresa (fail-closed sin empresa) | PASS | `app/operations/service.py:93-102` (`_acotar_a_empresa` → `false()` sin empresa), `:1052`, `:1109-1124`, `:1130-1174`, `:1307-1351`; `validate_lot_active(company_id)` `validators.py:501-529` | `tests/test_od14_productive_surfaces.py::test_s02_*`, `tests/test_multi_company.py`, `tests/security/test_multitenant_isolation.py` | — |
| A.14 `operations` · referencias estructurales del submovimiento (`R-180`) y catálogos (`R-179`) | PASS | `operations/service.py:841-877`, `:1209-1216`; `tenancy.py:73-186` | `tests/test_submovement_structural_tenancy.py`, `tests/test_master_reference_tenancy.py` | — |
| A.15 `operations` · edición/corrección: destino verificado como un alta | PASS | `operations/service.py:1261-1305` (`verificar_destino_de_edicion`), `app/corrections/service.py:70-81` | `tests/test_operations_bu_enforcement.py::test_w09_*`, `tests/test_edit_validation_parity.py`, `tests/test_master_reference_tenancy.py::test_r179_03_04` | — |
| A.16 `operations` · evidencias upload/download/delete: empresa | PASS | `operations/router.py:368-373` (upload vía `get_event`), `service.py:1387-1435` (`evidence.company_id != self.company_id → 403`, luego `get_event`) | `tests/test_od14_productive_surfaces.py::test_s03_*/s04_*`, `tests/test_lots_bu_enforcement.py::test_e05_*` | Contrato `403` (no `404`) certificado en `R-139`; distinto de la política general «ajeno = inexistente». |
| A.17 `operations` · `sap_document_ref` (OC) buscada dentro de la empresa | PASS | `validators.py:757-764`, `:778-779` | `tests/test_purchase_order_receipt.py` | — |
| A.18 `review/approvals/corrections/reversals` · pertenencia del evento | PASS | `app/review/service.py:387-402`, `:614-634`; `corrections/service.py:26-30`, `:116-127`, `:136-141`; `app/reversals/service.py:88-96`, `:165-178` | `tests/security/test_multitenant_isolation.py::test_no_se_puede_{corregir,revisar,aprobar}_un_evento_ajeno`, `tests/test_internal_reversal.py::test_s01_s02_*` | `create_review_batch` (`review/service.py:206-243`) usa `update(...).where(id==event_id)` sin empresa, pero solo tras `_get_event` por cada id (`:210`). |
| A.19 `reports/dashboard` · filtros de lote por empresa | PASS | `app/reports/service.py:37-70`, `:314-318`, `:379-386`; `app/dashboard/service.py:17-36`, `:49-73`, `:96-140` | `tests/test_kpi_scope.py::test_el_kpi_de_otra_empresa_sigue_siendo_inalcanzable`, `tests/test_kpi_hatchery.py::test_t_022_06` | — |
| A.20 `audit` · `company_id == self.company_id` en list/get/timeline; `company_id` **no nulable** | PASS (lectura) / PARTIAL (escritura) | `app/audit/service.py:48-49`, `:104-108`, `:117-122`; `audit/models.py:78`; `audit/helpers.py:250-252` (sin empresa → `None`, **no se escribe**) | `tests/test_audit_query.py`, `tests/test_audit_coverage.py::test_t_081_02` | **EXISTING: `R-83`** (P2 abierto). Además `audit/listeners.py:140` escribe `company_id or 0` (FK inexistente → `IntegrityError` si el listener dispara sin empresa; hoy inalcanzable por `OD-14.d`). |
| A.21 `notifications` · destinatario **y** empresa | PASS | `app/notifications/service.py:124-134` (`_mias`), `:159-164`; `router.py:55-80` (`404` a terceros); `recipients.py:54-98`, `:132-145` (candidatos filtrados por empresa) | `tests/test_notifications.py`, `tests/test_notification_recipients.py::test_t_038_25`, `::test_t_039_11` | `_mias` omite el filtro de empresa si `company_id` es falsy (`:132`); el filtro por destinatario basta. |
| A.22 `business_units` admin · empresa resuelta del contexto, dirección por código | PASS | `app/business_units/router.py:58-73`; `admin.py:73-110` | `tests/test_business_unit_admin.py::test_el_listado_no_deja_ver_la_configuracion_de_otra_empresa`, `::test_habilitar_toca_la_fila_de_la_empresa_propia_y_no_la_del_vecino`, `::test_no_se_concede_a_un_usuario_de_otra_empresa` | — |
| A.23 `sap` · references/consolidated/payloads/jobs/errors por empresa **para el actor de empresa** | PASS (actor de empresa) / **FAIL (autoridad global sin contexto)** | `app/integrations/sap/service.py:76-81`: `_company_filter` devuelve `true()` cuando `company_id is None` («No filter for super admins — sees all companies») → `list_references` `:128-135`, `consolidate_approved` `:158-161`, `export_to_sap` `:239-242`, **`retry_failed` `:404-408` (escritura: reenvía cargas de todas las empresas sin `_require_company_id`)**, `list_sync_jobs` `:532-537`, `list_consolidated` `:547-551`, `list_errors` `:564-567`, `list_payloads` `:582-583`. `consolidate`/`export` solo fallan cerrados **después** de leer filas ajenas (`_require_company_id` `:208`, `:258`). | `tests/test_sap_transversal.py::test_ac_sap02_*` (actor de empresa). **NO TEST** de autoridad global sin contexto en SAP (grep `global|super|sin contexto` → nada relevante). | Contrario a `OD-14.d` (fail-closed sin contexto) y al resto del producto (`_acotar_a_empresa`, `_apply_company_filter`). `FEATURE_SAP_ENABLED` por defecto **`true`** en `docker-compose.yml:26`. No registrado (grep `_company_filter`, `sap.*sin contexto`: vacío). Ver GAP-04. |

### A.24 Inventario exhaustivo de atajos `is_super_admin` en `backend/app` (grep) y su carácter contextual (`OD-14`)

| # | Sitio | Efecto | ¿Contextual (exige empresa efectiva)? |
|---|---|---|---|
| 1 | `app/auth/security.py:107-120` | cálculo de la capacidad (`("*", all)`) | n/a (origen) |
| 2 | `app/auth/security.py:145` (`puede_cambiar=is_super_admin` → `tenancy.py:294-307`) | honra `company_id` del token solo a la autoridad global; valida empresa activa | sí |
| 3 | `app/auth/security.py:170-178` `get_company_filter` → `None` (sin filtro) | **legado, sin ningún consumidor** (grep vacío) | n/a — código muerto que documenta la doctrina antigua; recomendable retirarlo |
| 4 | `app/auth/security.py:206` `tiene_permiso` | RBAC: pasa siempre | por diseño (RBAC ≠ inquilino) |
| 5 | `app/auth/service.py:77` `_contexto` | fail-closed sin empresa (`_acotar` `:91-96`) | sí |
| 6 | `app/auth/service.py:133,155` `_rol_asignable`/`_rol_administrable` | autoridad global asigna/edita cualquier rol | control global (`OD-14.c`): aceptable; ver A.4 para el hueco inverso |
| 7 | **`app/auth/service.py:449` `change_password`** `es_administrador = is_super_admin`; usuario buscado **sin empresa** (`:443-446`) | la autoridad global **sin contexto** restablece la contraseña de cualquier usuario de cualquier empresa; `users:update` **no** basta | **NO** (superficie `CONTROL` de inquilino, `route_scope.py:81`, sin contexto) |
| 8 | `app/auth/service.py:510` `switch_company` | solo la autoridad global cambia de empresa | CORE, correcto (`OD-14.b`) |
| 9 | `app/auth/router.py:72` | proyección en `/me` | n/a |
| 10 | `app/business_units/service.py:143` `unidades_de_alcance_productivo` | global → `unidades_habilitadas(company_id)`; sin empresa → `[]` | sí |
| 11 | `app/business_units/service.py:278-284` `exigir_unidad_operativa` | global: `sin_empresa` si no situada; `no_habilitada` si la unidad está apagada | sí (`OD-16.e/f`) |
| 12 | `app/lots/service.py:206` | asignación **sin uso** posterior | n/a (código muerto) |
| 13 | `app/operations/service.py:1359,1368` `get_evidences`/`create_evidence` | comparación redundante tras `get_event` (que ya acota por empresa) | inocuo (certificado en `A01_SUPER_ADMIN_SHORTCUT_VERIFICATION.md` #8-9) |
| 14 | `app/masters/service.py:39,85` | sin filtro **solo** para `companies` (`_CONTROL_GLOBAL`) | sí (`OD-14.c`) |
| 15 | `app/integrations/sap/service.py:76-81` (`company_id is None` → `true()`) — sin la palabra `is_super_admin` pero mismo atajo | **fail-open** de lectura en 6 superficies y de escritura en `retry_failed` | **NO** (ver A.23) |

---

## B. Aplicación de unidad de negocio (BU)

| Control | Veredicto | Evidencia (file:line) | Cobertura de tests | Notas / hueco |
|---|---|---|---|---|
| B.1 Resolutor central: efectivas = misma empresa ∧ habilitada ∧ concedida viva ∧ activa en producto; **leído de BD en cada llamada** | PASS | `app/business_units/service.py:97-123` (`unidades_efectivas_por_id`), `:126-147` (`unidades_de_alcance_productivo`) | `tests/test_business_units.py`, `tests/test_business_unit_guard.py`, `tests/test_session_payload.py::test_una_revocacion_se_ve_en_la_sesion_siguiente` | Sin concesiones → `[]` → `predicado` = `false()` (`scope.py:48-49`). |
| B.2 `operations` create/update/submit/cancel/evidencias/alertas: guarda de escritura `exigir_unidad_operativa` | PASS | `operations/service.py:159-214`, `:242-246` (create), `:1290-1292` (edición/corrección), `:1309` (submit), `:1327` (cancel), `:1370` (evidencia), `:1408` (delete evidencia), `:1019` (alerta); router `:373` (antes de tocar disco) | `tests/test_operations_bu_enforcement.py` (W02…W14), `tests/test_state_continuity.py::test_s06/s07_*` | — |
| B.3 `operations` lectura (list/get/alerts/evidencias): predicado de evento (lote alcanzable ∨ clasificación a habilitación alcanzable ∨ tipo inequívoco) | PASS | `operations/service.py:1058-1061`, `:1116-1119`, `:997-1009`; `business_units/classification.py:65-113` | `tests/test_od14_productive_surfaces.py::test_s02_control_la_unidad_apagada_para_la_empresa_no_se_ve`, `tests/test_pending_classification.py`, `tests/test_lots_bu_enforcement.py::test_e02/e03/e08_*` | Descarga de evidencia pasa por `get_event` (`:1434`) — `R-162` cerrado. |
| B.4 `lots` create/update/close/activate-manual/phases: guarda de escritura + lectura por unidad | PASS | `lots/service.py:223-243`, `:359-360`, `:418`, `:443`, `:580`, `:692`; lectura `:255-256`, `:271-272` | `tests/test_lots_bu_enforcement.py` (L03…L15), `tests/test_lot_row_scope.py` | — |
| B.5 `review/approvals` start/return/complete/approve/reject: colas acotadas y **habilitación exigida** (`R-165`) | PASS | `review/service.py:128-147`, `:164-169`, `:387-402` (`_get_event` → `_exigir_habilitacion` `:110-117` → `OperationsService.exigir_unidad_operativa`), `:614-634` | `tests/test_review_bu_enforcement.py::test_165_01…05`, `tests/test_review.py` | `ApprovalStepService._ambito_de_unidad` (`:645-664`) definido y **sin uso** (plano de control, correcto que no filtre). |
| B.6 `corrections` / `reversals`: guarda de escritura | PASS | `corrections/service.py:28-30`; `reversals/service.py:94-95`, `:175` | `tests/test_state_continuity.py::test_s06/s07_*`, `tests/test_internal_reversal.py::test_s03_s04_*` | — |
| B.7 `reports` KPI por lote: `_exigir_lote` (404 fuera de alcance) | PASS | `reports/service.py:37-62`; llamadas en `:141,156,167,192,315,380,416,441,473,514,551,581,637,694` | `tests/test_kpi_scope.py` (7 tests por lote), `::test_apagar_la_unidad_retira_su_kpi` | — |
| B.8 `reports` **agregados sin `lot_id`**: `GET /reports/kpis/hatchery` y `GET /reports/kpis` (`hatchery_yield`) | **FAIL** | `reports/service.py:191-207` (`get_kpi_hatchery(lot_id=None)`: `where(company_id)` **sin `_filtro_de_lotes`**), `:262-308` (`_huevos_cargados`, `_huevos_por_tipo`, `_descartados`: ídem), `:415-420` (`get_all_kpis` llama `get_kpi_hatchery(None)` para todo actor con `reports:read`). `_filtro_de_lotes` (`:64-70`) existe y **no se usa en ningún agregado**. Un usuario solo-`breeder` obtiene nacidos/cargados/fértiles de toda la incubadora de la empresa. La ruta está declarada `UNIDAD_UNICA hatchery` (`route_scope.py:174-175`) pero la declaración no se aplica (ver §0). | `tests/test_kpi_hatchery.py::test_t_022_06` (solo otra empresa); `tests/test_kpi_scope.py` no cubre el agregado sin lote. **NO TEST** | Ver GAP-07. `get_sap_comparison(None)` (`:379-409`) devuelve `lot_id`, `sap_ref`, estado de **todos** los eventos de la empresa (superficie `CONTRATO`, excepción declarada `BU-D04`; aceptable por diseño pero conviene citarlo). |
| B.9 `dashboard` admin/mobile: contadores, tendencia y `lots_by_type` acotados por lotes alcanzables | PASS | `dashboard/service.py:17-36`, `:44-73`, `:93-140`, `:154-204` | `tests/test_kpi_scope.py::test_el_panel_*`, `::test_los_contadores_del_panel_no_cuentan_eventos_ajenos` | — |
| B.10 `dashboard` **`active_alerts` sin predicado de unidad** | **FAIL** | `dashboard/service.py:206-242` (`_get_active_alerts`: `where(company_id, is_resolved == False)` **sin** `lot_id.in_(_lotes)`; el comentario `:208-211` afirma lo contrario). Expone `lot_id`, `alert_type`, `message` (con cifras de mortalidad/peso) y `actual_value` de lotes de unidades no concedidas o apagadas. Contrasta con `operations/service.py:997-1009`, que sí acota las mismas alertas. | **NO TEST** (grep `active_alerts` en `tests/`: vacío) | Ver GAP-08. |
| B.11 `notifications`: canal `CORE`, contenido por destinatario resuelto en backend | PASS | `notifications/recipients.py:101-147`; `service.py:124-134` | `tests/test_notification_recipients.py` | El aviso puede llevar `lot_id` de una unidad que el destinatario (p. ej. contralor) no tiene concedida: decisión `OD-08` (funciones de empresa), no hueco. |
| B.12 `audit`: `CORE` con visibilidad de control (`BU-D03`), sin filtro de unidad | PASS (decisión) | `audit/service.py:48` | `tests/test_audit_query.py` | Quien tiene `audit:read` ve la historia de todas las unidades de su empresa; consistente con `OD-09.a`. |
| B.13 Autoridad global y unidad **apagada** = inaccesible en lectura y escritura (`OD-16.e`) | PASS | `business_units/service.py:143-144` (lee por habilitadas), `:278-284` (escribe solo habilitada); `masters/service.py:99-120` (predicado aplicado también a la global) | `tests/test_od16_global_read_boundary.py`, `tests/test_lots_bu_enforcement.py::test_l07/l08_*`, `tests/test_review_bu_enforcement.py::test_165_01` | Excepciones por diseño: plano de control (`business_units` admin `:420` L15), SAP (`CONTRATO`), auditoría. |
| B.14 Concesión viva sobre unidad apagada **no** da acceso (`OD-16.e`) | PASS | `service.py:117-121` (`is_enabled` en la misma consulta); además `OD-23` la termina (`admin.py:178-193`) | `tests/test_operations_bu_enforcement.py::test_w02_*`, `tests/test_lots_bu_enforcement.py::test_l03_*`, `tests/test_r188_bu_lifecycle.py` | — |
| B.15 Lectura productiva que filtre datos de otra unidad a un usuario con concesión distinta | PARTIAL | Lotes/eventos/alertas de operaciones/KPI por lote: acotados (B.3, B.4, B.7). Fugas residuales: B.8 (agregado incubadora) y B.10 (alertas del panel). | ver B.8, B.10 | — |
| B.16 `route_scope.py` frente a código real | PARTIAL | `route_scope.py:44-196` declara; `MULTI_UNIDAD` acotado en fase 3-6 (verificado arriba); `UNIDAD_UNICA` (`hatcheries/incubators/hatchers/processing-plants`, `kpis/hatchery`) **no** tiene aplicación en código (`unidad_requerida` sin llamadores) | `test_ac_c15_*` (solo declaración) | Los maestros `UNIDAD_UNICA` se listan por empresa a cualquier `masters:read` (`MasterService` sin `unidades`, `masters/router.py:38-90`): consistente con `OD-09.a` (maestros no son dato productivo) pero divergente de la declaración. |

---

## C. Ciclo de vida `OD-23` y sesión activa

| Control | Veredicto | Evidencia (file:line) | Cobertura de tests | Notas / hueco |
|---|---|---|---|---|
| C.1 `disable` marca `revoked_at` en todas las concesiones vivas de esa habilitación y audita **cada una** con causa | PASS | `business_units/admin.py:178-193` (marca), `:205-214` (`PERMISSION_CHANGE` por concesión, `cause: company_business_unit_disabled`), `:197-203` (`CONFIG_CHANGE`) | `tests/test_r188_bu_lifecycle.py::test_r188_apagar_termina_las_concesiones_vivas`, `::test_r188_auditoria_de_terminacion_por_ciclo`, `::test_r188_apagado_idempotente_normaliza_estado_previo` | Apagado repetido normaliza sin duplicar. |
| C.2 `enable` **no** revive concesiones | PASS | `admin.py:170-176` solo fija `is_enabled`; ningún camino en `app/` pone `revoked_at = NULL` (grep) | `tests/test_r188_bu_lifecycle.py::test_r188_reactivar_no_devuelve_la_concesion`, `tests/test_business_unit_admin.py::test_r188_deshabilitar_termina_y_rehabilitar_no_devuelve` | — |
| C.3 Concesión nueva restaura y conserva historia (fila revocada permanece) | PASS | `admin.py:368-391`; índice único **parcial** entre vivas `business_units/models.py:121-122` | `tests/test_r188_bu_lifecycle.py::test_r188_concesion_nueva_restaura_y_conserva_historia`, `tests/test_business_unit_admin.py::test_una_concesion_revocada_se_puede_volver_a_otorgar` | — |
| C.4 Conceder con empresa OFF → `409`; unidad inexistente/inactiva → `404` | PASS | `admin.py:356-366`; `router.py:76-89` (`_traducir`) | `tests/test_r188_bu_lifecycle.py::test_r188_conceder_con_unidad_apagada_rechaza`, `tests/test_business_unit_admin.py::test_habilitar_una_unidad_inexistente_es_404` | — |
| C.5 Revocar marca (no borra), efecto inmediato, `404` si no viva | PASS | `admin.py:394-429`; `service.py:353-377` | `tests/test_business_unit_admin.py::test_revocar_retira_el_acceso_de_inmediato_y_conserva_la_historia`, `::test_revocar_lo_ya_revocado_es_404_*` | — |
| C.6 Sesión activa pierde acceso **de inmediato** tras disable/revoke (no hay estado BU en el JWT) | PASS | Claims: `auth/service.py:24-41` (sin unidades); evaluación por petición: `service.py:97-123`; `/me` compone en cada llamada `auth/router.py:61-84` | `tests/test_session_payload.py::test_una_revocacion_se_ve_en_la_sesion_siguiente`, `tests/test_r188_bu_lifecycle.py` | — |
| C.7 Cambio de rol / permisos surte efecto en la siguiente petición | PASS | `security.py:98-121` (recarga usuario+rol+permisos) | `tests/test_role_administration.py::test_t_092_05_*` | `role_id` del JWT no se usa. |
| C.8 Cambio de empresa persistida de un usuario revoca sus concesiones (`OD-09.e`) | PASS (por ausencia de camino) | `service.py:321-350` (`revocar_concesiones`) sin llamador; `UserUpdate` no admite `company_id` (`auth/schemas.py:74-85`) | `tests/test_r188_bu_lifecycle.py::test_r188_transferencia_de_empresa_intacta` | Documentado: la garantía vale lo que valga su futuro llamador. |

---

## D. Autenticación y sesión

| Control | Veredicto | Evidencia (file:line) | Cobertura de tests | Notas / hueco |
|---|---|---|---|---|
| D.1 Login: credenciales verificadas con bcrypt; `LOGIN_FAILED` auditado (con `commit` propio para sobrevivir al 401); cuenta desactivada → `403` | PASS | `auth/service.py:175-223`; `security.py:15-27` | `tests/test_auth.py`, `tests/test_audit_coverage.py::test_t_081_01/02` | Usuario inexistente no se audita (`R-83`). `last_login` **nunca se escribe** (grep: solo modelo/esquema) — pendiente de `GA-REM-003 §6`. |
| D.2 Rate limit en login (`5/minute`) y global (`60/minute`) | PARTIAL | `app/main.py:15-33`, `:70-73`; `auth/router.py:29-32`; `config.py:75-76,115` (`FEATURE_RATE_LIMIT_ENABLED=False` por defecto; `docker-compose.yml:29` lo fuerza a `true`; `.env.example:45` `false`) | **NO TEST** (grep `429`, `rate_limit` en `tests/`: vacío) | Clave = `get_remote_address` (`main.py:17`). El backend corre `uvicorn` sin `--forwarded-allow-ips` (`backend/Dockerfile:62`) detrás de `frontend/nginx.conf:67` (`X-Forwarded-For`): en producción todos los clientes comparten la IP del proxy → un solo cupo de 5 logins/min y 60 req/min para **toda la empresa** (denegación de servicio trivial) o, si el proxy no es de confianza, sin efecto. **Sin bloqueo por cuenta** (lockout). UNKNOWN en runtime. Ver GAP-11. |
| D.3 Refresh: valida `type == "refresh"`, recarga usuario de BD, conserva contexto solo para la autoridad global | PASS | `auth/service.py:241-287` | `tests/test_security_regression.py::test_r43_*`, `tests/test_rbac.py::test_ga_rem_003_*`, `tests/test_multicompany_isolation.py::test_el_contexto_sobrevive_a_la_renovacion` | — |
| D.4 **El access path no comprueba `type`: un refresh token (7 días) sirve como Bearer** | **FAIL** | `security.py:59-65` (`decode_token` no mira `type`), `:87-93` (`get_current_user` solo lee `sub`); `create_refresh_token` `:48-56` emite `sub` + `exp` 7 días (`config.py:49`) | **NO TEST** (grep `Bearer.*refresh`: vacío) | El límite de 30 min del access token (`config.py:48`) es ilusorio: el cliente guarda ambos (el refresh en `localStorage`, `GA-REM-003:74`) y el de 7 días autentica cualquier ruta. Ver GAP-03. |
| D.5 Rotación del refresh / logout / revocación | **FAIL** (registrado) | No existe `POST /logout` (`auth/router.py`, `authorization_coverage.py:21-29`); `refresh_token` reemite sin invalidar el anterior (`:282-286`); sin `jti`/denylist (grep) | **NO TEST** | **EXISTING: `GA-REM-003`** (`REMEDIATION_BACKLOG.md:90` «P1-4 sin logout ni revocación»; spec `:26-30,41,58,97-100`). El informe `GA-REM-002-003-CERTIFICATION-REPORT.md` no cierra `AC04` (logout). |
| D.6 Cambio de contraseña: titular con la actual; política 8+; auditado sin secreto | PASS | `auth/service.py:423-489`; `auth/schemas.py:44-62` | `tests/test_p013_password.py::test_t012_01…08` | Las sesiones previas siguen vivas (`test_t012_08`, decisión `GA-REM-012`); combinado con D.4/D.5, una credencial comprometida sigue válida hasta 7 días. |
| D.7 **Restablecimiento por administrador**: solo `is_super_admin`, **sin contexto de empresa** | **FAIL** | `auth/service.py:443-446` (`select(User).where(id)` global), `:449-455` (`es_administrador = is_super_admin`; `users:update` → `403`); comentario `:434-436` reconoce que debía migrar a `users:update`. Auditoría con `company_id=user.company_id` (`:480`) | `tests/test_p013_password.py::test_t012_05` (operador → 403), `::test_t012_06` (super admin, misma empresa). **NO TEST** de super admin sin contexto contra otra empresa ni de `users:update` de la empresa. | Viola `OD-14.d` (superficie `CONTROL` de inquilino operada sin contexto, `route_scope.py:81`) y deja al administrador de empresa sin poder restablecer contraseñas. Ver GAP-09. |
| D.8 `switch-company`: solo autoridad global, empresa activa, auditado `CONTEXT_SWITCHED` | PASS | `auth/service.py:507-540` | `tests/test_multicompany_isolation.py::test_un_usuario_normal_no_puede_cambiar_de_empresa`, `::test_el_super_admin_no_puede_situarse_en_una_empresa_inexistente` | Sin auditoría cuando la empresa persistida es `None`: `audit_accion` recibe `company_id=company_id` (destino) → sí se escribe. OK. |
| D.9 Expiración 30 min; secreto JWT obligatorio y no por defecto; algoritmo fijado | PASS | `config.py:46-58`; `security.py:34-45,61` | `tests/test_runtime_startup.py` (parcial) | Sin `iss/aud` (aceptable para un solo emisor). |
| D.10 `/me` informa (no decide) los cuatro conjuntos | PASS | `auth/router.py:40-84`; `auth/schemas.py:100-140` | `tests/test_session_payload.py` (h11-h14) | — |

---

## E. Administrador de accesos (`OD-15`)

| Control | Veredicto | Evidencia (file:line) | Cobertura de tests | Notas / hueco |
|---|---|---|---|---|
| E.1 Permisos propios `business_units:read/update/create/delete` (plano de control) | PASS | `business_units/router.py:43-55`, `:98,109,129,151,172,193,216`; módulo en catálogo `auth/service.py:546-553` | `tests/test_access_administration.py`, `tests/test_business_unit_admin.py::test_llamarse_administrador_no_administra_nada` | — |
| E.2 Auto-concesión denegada **en servidor**, antes de cualquier otra puerta, sin excepción de arranque (`OD-15.a/c`) | PASS | `admin.py:348-350` (`user_id == actor.id → SegregacionDeFunciones` → `403` `router.py:85-88`) | `tests/test_r188_bu_lifecycle.py::test_r188_self_grant_denegado`, `tests/test_grant_candidates.py::test_c11_*`, `tests/test_access_administration.py` | Auto-revocación permitida (`OD-15.b`). |
| E.3 Concesión cruzada entre empresas imposible (dos puertas) | PASS | `admin.py:98-110`, `:352-354`; `service.py:201-231` (`conceder_unidad` rechaza empresa distinta) | `tests/test_r188_bu_lifecycle.py::test_r188_cross_company_denegado`, `tests/test_business_unit_admin.py::test_no_se_concede_a_un_usuario_de_otra_empresa`, `::test_el_administrador_no_puede_darse_acceso_a_otra_empresa` | — |
| E.4 Candidatos con `business_units:create`, sin `users:read`; sin parámetros de búsqueda/id; proyección mínima | PASS | `router.py:146-162`; `admin.py:265-314` | `tests/test_grant_candidates.py::test_c2_*`, `::test_c4_c5_*`, `::test_c8_*`, `::test_c3_*` | `R-129` cerrado. |
| E.5 Administrar no es acceder: el rol de accesos no gana alcance productivo; nada lee el rol para resolver unidades | PASS | `admin.py:18-22` (docstring) y ausencia de lectura de `unidades_efectivas` en `admin.py`; `service.py:97-123` no consulta roles | `tests/test_business_unit_admin.py::test_tratamiento_ese_mismo_administrador_no_ve_dato_productivo`, `::test_administrar_no_cambia_el_alcance_operativo_del_administrador`, `tests/test_session_payload.py::test_h14_*` | — |
| E.6 Escalada a autoridad global «por rol» desde una empresa | **FAIL** | ver A.4 (rol de inquilino con `("*", all)` creable y asignable por `users:create/update`) | **NO TEST** | Requiere `users:create` + `users:update` en el inquilino (hoy solo Super Admin en la semilla, pero es la capacidad prevista para el administrador de empresa por `GA-REM-034`/`OD-13`). GAP-01. |

---

## F. Transacciones, atomicidad, idempotencia, concurrencia

| Control | Veredicto | Evidencia (file:line) | Cobertura de tests | Notas / hueco |
|---|---|---|---|---|
| F.1 Frontera transaccional por petición: commit en la ruta **antes** de responder; fallo de commit → 500 + rollback; errores → rollback en `get_db` | PASS | `app/transaction.py:36-60`; `app/database.py:25-44` | `tests/test_transaction_boundary.py::test_t_026_01…09,12` | Excepciones de `commit` en servicio: `auth/service.py:199` (audit de login fallido, deliberado), `operations/service.py:1383,1414` (evidencias, «única excepción documentada», `test_t_026_09`), `notifications/sla.py:252` (tarea de fondo). |
| F.2 Aprobación + creación automática del lote `L-GP-{año}-{nn}` en la **misma** transacción; fallo revierte ambos | PASS | `review/service.py:490-531` (approve) y `:333-351` (nivel único); `lots/service.py:128-196`: `pg_advisory_xact_lock(hashtext('lote-gp:{empresa}:{año}'))` (`:100-108,152`), `begin_nested()` + reintento ante `IntegrityError` de `lot_code` global (`:154-181`), `event.lot_id` fijado `:185` | `tests/test_r153_import_lot_auto.py::test_r153_ac31_ac32_fallo_de_lote_revierte_la_aprobacion`, `::test_r153_ac23_ac26_doble_aprobacion_no_duplica`, `::test_r153_ac09_ac10_secuencia_consecutiva` | Lock solo en PostgreSQL (`:106`). |
| F.3 Recepción/población: decrementos bajo `SELECT … FOR UPDATE` de la fila del lote (`R-130`) | PASS | `validators.py:197-231` (`bloquear_saldo_del_lote`, `validate_bird_decrement`) | `tests/test_population_invariant.py`, `tests/test_mortality.py` | Aprobación **no** bloquea el saldo (`R-166 ≠ R-161`, decisión). |
| F.4 Saldos de huevo/incubación (`BR-02/03/04`) bajo el mismo bloqueo — estado de `R-161` | PASS · **`R-161` CERRADO** | `validators.py:239-288` (`validate_egg_dispatch`, `validate_incubation_load`, `validate_chick_dispatch` llaman `bloquear_saldo_del_lote` antes de leer) | `tests/test_egg_incubation_concurrency.py::test_r161_05/06_*` | Backlog: evidencia `R-161-EGG-INCUBATION-CONCURRENCY-EVIDENCE.md` (`REMEDIATION_BACKLOG.md:1267`); la fila `:1001` y el recuento `:1013,1045` quedaron con texto «OPEN» anterior al cierre. |
| F.5 Decisión de revisión concurrente: `FOR UPDATE` + relectura del estado (`R-166`) | PASS | `review/service.py:89-107` (`_bloquear_evento`), aplicado en `:401` y `:627` **después** de la cadena inquilino/unidad | `tests/test_review_decision_concurrency.py::test_r166_01…13` | Cerrado (`REMEDIATION_BACKLOG.md:1481-1492`). |
| F.6 Reverso interno (`OD-19`): original bloqueado al solicitar y al aplicar; exclusión de contrapartida activa; exclusión de efectos en saldos | PASS | `reversals/service.py:74-79` (`_bloquear_original` `populate_existing`), `:96`, `:113-123` (una sola contrapartida activa/efectiva, `CONTRAPARTIDA_ACTIVA` `:48-50`), `:181-219` (`efectuar_reverso_si_procede`: relee `APPROVED`, bloquea saldo, valida `≥0`, marca ambos `REVERSED`); `validators.py:21-46` (`_suma_neta` descuenta contrapartidas efectivas); edición/corrección de contrapartida bloqueada `operations/service.py:1141-1144`, `corrections/service.py:33-36` | `tests/test_internal_reversal.py` (RV/EF/S/AU), `tests/test_reversal_role_matrix.py` | `_suma_neta` solo cubre saldo de aves; huevos/incubación bloqueados por diseño (`BLOQUEADOS_POR_R161` `:42-46`). |
| F.7 Traspaso entre cadenas (`OD-10.a/b`): contrato | PASS | `business_units/handoff.py:34-67` (`FLUJOS` egg_batch: grandparent/breeder→hatchery; chick_batch: hatchery→breeder/broiler; lote sin cadena rechazado); `lots/router.py:239-331`; vínculo automático `operations/service.py:433-541` (par despacho/recepción por destino declarado; **exactamente uno por despacho** `:477-496,523-541`); cantidades derivadas de los movimientos persistidos `:388-400`; linaje efectivo derivado del estado `lots/router.py:203-236` | `tests/test_handoff_contract.py`, `tests/test_reception_lineage.py`, `tests/test_lineage_cancel_move.py`, `tests/test_birth_classification.py` | Pendiente de clasificar: `classification.py:116-148` (creador o `masters:update`), clasificación exige habilitación de la **misma** empresa y activa (`:170-176`), reclasificación bloqueada con efectos (`:222-302`). Sin unicidad en BD de `(dispatch_event_id)` en `egg_batches`/`chick_batches` (`lots/models.py`): la unicidad «exactamente uno» es de aplicación y sin bloqueo (P3). |
| F.8 Unicidad de concesiones/habilitaciones | PASS | `business_units/models.py:67` (`uq_company_business_unit`), `:121-122` (índice único parcial `revoked_at IS NULL`) | `tests/test_business_unit_admin.py::test_conceder_dos_veces_no_duplica`, `tests/test_migration_bu_catalog.py` | — |
| F.9 SAP: `idempotency_key` SHA-256 única; estados `PREPARED/SENDING/CONFIRMED/FAILED/RETRYING`; backoff 1/5/15 min; adaptador manual **nunca** marca `SENT_TO_SAP` | PASS con defecto | `sap/adapter.py:196-202`; `sap/models.py:143-144`, `:47-52`; `sap/service.py:275-293`, `:344-373`, `:402-462` | `tests/test_sap.py`, `tests/test_sap_transversal.py` | Defecto: `service.py:289-291` consume el `Result` dos veces (`existing.scalar_one_or_none()` en el `if` y de nuevo para `.id`) → el camino idempotente de re-enlace falla (`AttributeError`/`ResourceClosedError` → 500). `consolidate_approved` (`:158-227`) **sin bloqueo** de los eventos: dos consolidaciones concurrentes pueden crear dos `ConsolidatedMovement` con los mismos `event_ids` (la unicidad de `idempotency_key` solo detiene la segunda **exportación**, con 500). P3. |
| F.10 `operations.idempotency_key` (`R-146`): clave de cliente, opcional, única global, consultada por empresa | PASS (implementado) | `operations/schemas.py:167`, `:183` (inmutable tras alta); `operations/models.py:101` (`unique=True`); `operations/service.py:224-234` (busca `key ∧ company_id` y devuelve el existente) | `tests/test_multi_company.py::test_idempotency_key_prevents_duplicate` | **EXISTING: `R-146`** (P2, tabla `REMEDIATION_BACKLOG.md:938` aún como hallazgo; el código ya lo implementa — verificar cierre). Residuo: unicidad **global** + búsqueda **por empresa** → una clave idéntica desde otra empresa produce `IntegrityError` (500) y un oráculo de existencia (P3). Sin clave, un doble `POST` duplica (aceptado por diseño). |
| F.11 Doble `submit`/`cancel` del mismo evento | PASS | `cancel_event` bloquea el lote y relee estado (`operations/service.py:1332-1339`); `submit_to_review` (`:1307-1323`) sin bloqueo pero transición idempotente (`REGISTERED/RETURNED/REJECTED → PENDING_REVIEW`) | `tests/test_edit_cancel_balance.py`, `tests/test_state_continuity.py::test_u05_*` | Un doble submit concurrente deja dos filas de auditoría `UPDATED`; sin efecto en saldo. |
| F.12 Restricciones únicas declaradas en `backend/app/*/models.py` (inventario completo) | — | `masters/models.py:61` `companies.name`; `:219` `(genetic_line_id, version_label)`; `:242` `(curve_id, age_days)`; `:294` `lots.lot_code` (**global**); `lots/models.py:45` `opening_balances.lot_id`; `auth/models.py:18-19` `users.email`, `users.username` (**globales**); `business_units/models.py:41` `business_units.code`, `:67`, `:121-122`; `operations/models.py:101` `operational_events.idempotency_key` (**global**); `sap/models.py:143-144` `sap_payloads.idempotency_key`, `external_transaction_id` | — | Unicidades globales (`lot_code`, `username`, `email`, `idempotency_key`) devuelven `409` o `500` que confirman la existencia del valor en otra empresa (`lots/service.py:311-318`, `auth/service.py:345-347`): oráculo entre inquilinos de bajo impacto (P3). Sin unicidad para `farms.code`, `houses(farm_id,name)`, `sap_references(company_id, ref_type, sap_code)`. |
| F.13 Riesgos de estado partido conocidos | PARTIAL | `operations/service.py:1409-1414` (`delete_evidence`: `os.remove` **antes** de `db.delete` + `commit`: si el commit falla, fichero perdido y fila viva); `router.py:374-380` (fichero escrito antes de `create_evidence`; si la inserción falla queda huérfano — mitigado porque la guarda de unidad corre antes `:373`); `sap/adapter.py:110-113` escribe el artefacto antes de persistir el `SapPayload` como `PREPARED` | `tests/test_transaction_boundary.py::test_t_026_09` (solo doble commit) | P3. |

---

## G. Evidencias (adjuntos)

| Control | Veredicto | Evidencia (file:line) | Cobertura de tests | Notas / hueco |
|---|---|---|---|---|
| G.1 Path traversal en el nombre | PASS | `operations/router.py:374-378` (`os.path.basename`, prefijo `uuid4().hex`, directorio `MEDIA_DIR/evidences/{company_id}/{event_id}`) | **NO TEST** (grep `traversal`) | — |
| G.2 Tipo y tamaño | PARTIAL | `router.py:21-22` (`_ALLOWED_MIME`, `_MAX_SIZE` 10 MB), `:359-366` | **NO TEST** (grep `mime`) | El tipo se toma del `content_type` declarado por el cliente, sin inspección de firma; la descarga sirve el `mime_type` guardado (`:405-409`). `X-Content-Type-Options: nosniff` (`main.py:114`) mitiga la ejecución en navegador. P3. |
| G.3 Descarga: empresa + evento al alcance de unidad (`R-162`) | PASS | `service.py:1416-1435` | `tests/test_lots_bu_enforcement.py::test_e01…e08`, `tests/test_od14_productive_surfaces.py::test_s04_*` | — |
| G.4 Borrado: empresa + unidad + guarda de escritura | PASS | `service.py:1387-1414` | `tests/test_operations_bu_enforcement.py::test_w06b_*`, `tests/test_od14_productive_surfaces.py::test_s03_*` | — |
| G.5 Persistencia del almacén (`R-52`) | PASS (config) | `docker-compose.yml:31-32,42-43,112-114` (`MEDIA_DIR=/app/media`, volumen `avicola-media`); `router.py:20` | — | **EXISTING: `R-52`** (RES-05, ops, `REMEDIATION_BACKLOG.md:2115,2134`). |
| G.6 Auditoría de subida/borrado de evidencias | **FAIL** (menor) | Ni `create_evidence` ni `delete_evidence` llaman `audit_accion`; los listeners solo cubren `OperationalEvent`, `CorrectionLog`, `ApprovalAction` (`audit/listeners.py:71-113`) | **NO TEST** | Borrado físico de un adjunto sin rastro en `P-09`. No registrado (grep). P3, GAP-12. |

---

## H. Inmutabilidad de la auditoría (`R-148`)

| Control | Veredicto | Evidencia (file:line) | Cobertura de tests | Notas / hueco |
|---|---|---|---|---|
| H.1 Sin `UPDATE`/`DELETE` sobre `audit_logs` en la aplicación | PASS | grep `delete(AuditLog`, `update(AuditLog` en `app/`: vacío; router de solo lectura `audit/router.py:16-63` | `tests/test_audit_query.py` | Los tests borran `audit_logs` para limpieza (`tests/test_session_payload.py:153`, etc.), no el producto. |
| H.2 Protección a nivel de BD (trigger/regla/revocación de `UPDATE/DELETE`) | **FAIL** (registrado) | `alembic/versions/ee30bd1aa374_add_audit_log.py` crea la tabla sin trigger; grep `TRIGGER|RULE|REVOKE` en `alembic/versions`: vacío | **NO TEST** | **EXISTING: `R-148`** (P2, `REMEDIATION_BACKLOG.md:940,2115`). |
| H.3 Listeners `after_flush` + helpers explícitos | UNKNOWN | `audit/listeners.py:71-113` (dispara sobre `Session`, incluida la sesión síncrona subyacente de `AsyncSession`) y `audit/helpers.py:73-92` (`audit_event_created`) se invocan ambos para el alta de evento (`operations/service.py:308`) → posible doble fila `CREATED` | `tests/test_audit_coverage.py`, `tests/test_full_workflow_audit.py` | Fuera del alcance de seguridad; señalado para la auditoría de trazabilidad. |

---

## I. Endurecimiento de entrada / configuración

| Control | Veredicto | Evidencia (file:line) | Cobertura de tests | Notas / hueco |
|---|---|---|---|---|
| I.1 SQL crudo | PASS | Única `text()`: `lots/service.py:107` con parámetro ligado (`:clave`); ningún `execute(f"…")` (grep) | — | — |
| I.2 Búsquedas `ilike` parametrizadas | PASS | `auth/service.py:304-309`; `masters/service.py:142-149` | — | Comodines `%`/`_` no escapados (solo afecta al resultado). |
| I.3 Topes de paginación | PASS | `users` ≤100 (`auth/router.py:102`), maestros ≤100 (`masters/router.py:33`), lotes ≤100, operaciones ≤100, alertas ≤200, auditoría ≤200 (`audit/router.py:29`), revisión ≤200, correcciones ≤200, SAP ≤200, notificaciones ≤100, pendientes ≤100 (`operations/router.py:122`) | — | `pending_classification` `skip: int = 0` sin `ge=0` (`operations/router.py:121`) → `offset(-1)` → 500. P3. |
| I.4 Política 404 vs 403 (anti-enumeración) | PASS (documentada) | Ajeno = inexistente: `tenancy.py:39-70`, `admin.py:49-55`; excepciones deliberadas: evidencias `403` (`R-139`), unidad de lote `403` con código (`lots/service.py:230-232`), `SegregacionDeFunciones` `403` | `tests/test_submovement_structural_tenancy.py::test_r180_13_*`, `tests/test_grant_candidates.py::test_c3_*` | — |
| I.5 Asignación masiva vía `setattr` | PARTIAL | `auth/service.py:416-417` (`UserUpdate` `extra=forbid`, sin `company_id`, `role_id` validado); `:635-636` (`RoleUpdate`); `review/service.py:691-692` (`ApprovalStepUpdate`); `corrections/service.py:82` (lista blanca `campos_corregibles` `:171-181`); `operations/service.py:1166-1167` (`OperationalEventUpdate` `extra=forbid`, sin `status/event_type/idempotency_key`); **`masters/service.py:253-254`** (Update con `company_id` en 8 esquemas, ver A.7) | `tests/test_security_regression.py::test_r32_*`, `::test_r51_*`, `tests/test_p014_persistence.py` | `PermissionCreate.module/scope_type` sin validación (`auth/schemas.py:201-205`) → ver A.4; `action` inválida → `ValueError` 500 (`auth/service.py:602`). |
| I.6 CORS | PASS | `main.py:100-107` (`allow_origins` explícito desde `BACKEND_CORS_ORIGINS`, `allow_credentials=True`) | **NO TEST** | Métodos/cabeceras `*` con orígenes explícitos: aceptable. |
| I.7 Cabeceras de seguridad | PASS | `main.py:110-124` (`nosniff`, `X-Frame-Options: DENY`, `Referrer-Policy`, `Permissions-Policy`, `Cache-Control: no-store`, HSTS solo en `production`) | **NO TEST** | Sin CSP (el backend es API; la CSP corresponde a `frontend/nginx.conf`). |
| I.8 Rate limiting: valor por defecto en producción | PARTIAL | `docker-compose.yml:29` `FEATURE_RATE_LIMIT_ENABLED:-true`; `.env.example:45` `false`; `config.py:115` `False` | **NO TEST** | Ver D.2 (clave por IP tras proxy). |
| I.9 `DEBUG`/documentación pública | PASS/nota | `docker-compose.yml:19,87` `DEBUG: "false"`; `/docs`, `/redoc`, `/openapi.json` públicos (`authorization_coverage.py:25-28`, `main.py:64-66`) | `tests/test_rbac.py::test_ac08b_*` | Exponer el esquema OpenAPI en producción es una decisión declarada; P3 informativo. |
| I.10 Secretos por defecto | PASS | `config.py:46-67` (arranque falla con `JWT_SECRET_KEY` vacío/`change_me` o `POSTGRES_PASSWORD` inseguro) | `tests/test_environment_guard.py`, `tests/test_runtime_startup.py` | — |

---

## SECURITY GAP CANDIDATES

Severidad propuesta: **P0** explotable ahora con privilegios ordinarios y cruce de inquilino · **P1** cruce de inquilino/escalada que requiere una capacidad administrativa prevista · **P2** fuga de unidad o control de sesión débil · **P3** endurecimiento/robustez.

| ID | Sev. | Hallazgo | Evidencia | Registro |
|---|---|---|---|---|
| **GAP-01** | **P1** (P0 si un rol de inquilino ya tiene `users:create`+`users:update`) | **Fabricación de autoridad global desde una empresa**: `create_role`/`update_role` aceptan `{"module":"*","scope_type":"all"}` en un rol de inquilino; `_rol_asignable` lo considera asignable por ser de la propia empresa; `get_current_user` concede `is_super_admin` por el permiso sin mirar `Role.company_id` → `switch-company` a cualquier empresa. Contradice `OD-13.c` («se prohíbe fabricar autoridad global desde una superficie de empresa») y `AC-R06`. | `auth/service.py:585-619`, `:621-658`, `:116-142` (rama `:142`), `security.py:119-120` | **NUEVO** (no en backlog; `R-117` cubrió solo la asignación del rol global existente). Sin test del camino. Corrección sugerida: rechazar en `create_role`/`update_role` cualquier permiso `("*", scope "all")` cuando el actor no es global, y en `_rol_asignable` aplicar `_es_autoridad_global` **también** a roles de inquilino; opcionalmente exigir `Role.company_id IS NULL` para computar `is_super_admin`. |
| **GAP-02** | P2 | **Autoridad global sin contexto** lee dato SAP de **todas** las empresas y **reintenta cargas** de todas (`retry_failed`), contra `OD-14.d` y la doctrina del resto del producto. `FEATURE_SAP_ENABLED` es `true` por defecto en `docker-compose.yml:26`. | `sap/service.py:76-81`, `:128-135`, `:404-413`, `:532-537`, `:547-551`, `:564-567`, `:582-583` | **NUEVO** (relacionado con `R-112`/`OD-12` pero no cubierto por ellos). Sin test. Corrección: `_company_filter` → `false()` cuando `company_id is None` (mismo patrón que `_acotar_a_empresa`). |
| **GAP-03** | P2 | **Refresh token aceptado como access token**: `get_current_user` no verifica `type == "access"`; el refresh (7 días, en `localStorage`) autentica cualquier ruta, vaciando la expiración de 30 min. Se agrava con la ausencia de rotación/logout (`GA-REM-003`). | `security.py:59-65`, `:87-93`; `auth/service.py:48-56` | **NUEVO** (GA-REM-003 registra la falta de logout/revocación, no este cruce de tipos). Sin test. Corrección: rechazar `payload.get("type") != "access"` en `get_current_user`. |
| **GAP-04** | P2 | Restablecimiento de contraseña por administrador: solo `is_super_admin`, **sin contexto de empresa** (viola `OD-14.d`) y `users:update` de la empresa no puede restablecer (el comentario del propio código reconoce la deuda). | `auth/service.py:434-436`, `:443-455` | **NUEVO**. Test parcial (`test_t012_05/06`). Corrección: localizar el objetivo con `_usuario_alcanzable(user_id, actor)` y autorizar por `tiene_permiso(actor, "users", "update")`. |
| **GAP-05** | P2 → **P1 condicional** | `company_id` fijable por el cliente en 19 esquemas de maestros (`Farm`/`Hatchery` incluso **obligatorio**) y en 8 esquemas de actualización: quien tenga `masters:create/update` crea maestros en otra empresa o **mueve** los propios a otra empresa o a `NULL` (compartido). La mitigación registrada («exclusivo del Super Admin») depende de la semilla y decae al delegar `masters:*` a un rol de inquilino. | `masters/schemas.py:64-65,124-125,436-503,632-651`; `masters/service.py:232-234,248-254` | **EXISTING: `R-50`** (solo en `UPDATE_SCHEMA_SECURITY_MATRIX.md:59,105`; **ausente** de `REMEDIATION_BACKLOG.md`). Sin test. Corrección: eliminar `company_id` de Create/Update (resolver siempre del contexto, `OD-14.c`), como ya hace `create_user`. |
| **GAP-06** | P2 | Lote con referencias estructurales/catálogo **sin verificar**: `house_id` (galpón de otra empresa, clase `R-42/R-59/R-180`), `genetic_line_id` y `weight_curve_id` (curva de otra empresa aplicada y **expuesta** en `weight-evaluation`). | `lots/service.py:362-392`, `:275-307`, `:411-436`; `masters/service.py:187`; `lots/schemas.py:14-20,65-67` | **NUEVO** (grep en backlog: vacío). Sin test. Corrección: `verificar_pertenencia(House)` (vía granja) y `verificar_catalogo_de_empresa(GeneticLine)` en alta y edición; exigir que `weight_curve_id` cuelgue de una línea de la empresa. |
| **GAP-07** | P2 | Agregado de incubadora sin `lot_id` (`/reports/kpis/hatchery`, `/reports/kpis`) **no acotado por unidad**: usuario sin `hatchery` obtiene nacidos/cargados/fértiles de la empresa. `_filtro_de_lotes` existe y no se usa; la declaración `UNIDAD_UNICA` de `route_scope` no se aplica en ninguna ruta. | `reports/service.py:191-207`, `:262-308`, `:415-420`, `:64-70`; `route_scope.py:174-175,215-217` | **NUEVO**. Sin test (solo «otra empresa»). Corrección: `where(OperationalEvent.lot_id.in_(await self._filtro_de_lotes()))` en los cuatro sumatorios cuando `lot_id is None`. |
| **GAP-08** | P2 | `dashboard/admin.active_alerts` sin predicado de unidad: expone `lot_id`, mensaje y valor de alertas de unidades no concedidas/apagadas (el comentario del método afirma lo contrario). | `dashboard/service.py:206-242` (comparar con `operations/service.py:997-1009`) | **NUEVO**. Sin test (grep `active_alerts`: vacío). Corrección: `OperationalAlert.lot_id.in_(_lotes)`. |
| **GAP-09** | P2 | Sin rotación del refresh token, sin `POST /logout`, sin denylist; cambio de contraseña no invalida sesiones. | `auth/service.py:241-287`; `auth/router.py`; `tests/test_p013_password.py::test_t012_08` | **EXISTING: `GA-REM-003`** («P1-4 sin logout ni revocación», `REMEDIATION_BACKLOG.md:90`; spec `:26-30,41,58`); no cerrado en `GA-REM-002-003-CERTIFICATION-REPORT.md`. |
| **GAP-10** | P2 | `audit_logs.company_id NOT NULL`: acciones sin empresa (autoridad global sin contexto, login fallido de usuario inexistente) no se auditan; listener escribe `company_id or 0` (FK inexistente). | `audit/helpers.py:244-252`; `audit/listeners.py:140`; `audit/models.py:78` | **EXISTING: `R-83`** (P2 abierto). |
| **GAP-11** | P3 (P2 operativo) | Rate limiting por IP de origen tras proxy sin `--forwarded-allow-ips`: un solo cupo `5/min` de login y `60/min` global para todos los usuarios (auto-DoS) o límite inefectivo; sin bloqueo por cuenta. | `main.py:17`, `backend/Dockerfile:62`, `frontend/nginx.conf:67`, `docker-compose.yml:29` | **NUEVO** (`S-05` no registra la clave por proxy). UNKNOWN en runtime. |
| **GAP-12** | P3 | Subida y **borrado físico** de evidencias sin registro de auditoría; borrado del fichero antes del commit (estado partido); tipo MIME solo declarado por el cliente. | `operations/service.py:1363-1414`; `router.py:359-380` | **NUEVO**. Relacionado con `R-81` (cobertura de auditoría) y `R-52` (almacén). |
| **GAP-13** | P3 | Inmutabilidad de `audit_logs` solo en aplicación (sin trigger/`REVOKE`). | `alembic/versions/ee30bd1aa374_add_audit_log.py` | **EXISTING: `R-148`** (P2 abierto, RES-08). |
| **GAP-14** | P3 | Idempotencia SAP: doble consumo del `Result` en el re-enlace idempotente (500); `consolidate_approved` sin bloqueo → dos consolidaciones concurrentes duplican `ConsolidatedMovement` (la segunda exportación cae por unicidad con 500). | `sap/service.py:283-293`, `:158-227` | **NUEVO** (relacionado con `R-112`/`GA-REM-010`). |
| **GAP-15** | P3 | Unicidades **globales** consultadas **por empresa**: `operational_events.idempotency_key` (colisión entre empresas → `IntegrityError` 500 + oráculo), `lots.lot_code` (409 revela código ajeno), `users.username/email` (409). | `operations/models.py:101` vs `service.py:224-231`; `lots/service.py:311-318`; `auth/service.py:345-347` | **NUEVO** (residuo de `R-146`, que ya está implementada en código pero figura abierta en `REMEDIATION_BACKLOG.md:938`). |
| **GAP-16** | P3 | `PermissionCreate.module/action/scope_type` sin validación contra el catálogo: acción inválida → 500; módulo arbitrario aceptado. `pending_classification.skip` sin `ge=0`. `get_company_filter` (legado fail-open) sin consumidores pero exportado en `dependencies.py`. | `auth/schemas.py:201-205`, `auth/service.py:602`; `operations/router.py:121`; `security.py:170-178`, `dependencies.py:2` | **NUEVO** (endurecimiento). |
| GAP-17 | P3 | `route_scope.py` declara `UNIDAD_UNICA` para maestros de incubadora/planta y `kpis/hatchery`, pero ningún código aplica `unidad_requerida`; la clasificación y la implementación divergen (documentado como «clasificar no protege»). | `route_scope.py:50-53,174-175,215-217` | **NUEVO** (documental; la parte productiva es GAP-07). |

Hallazgos referenciados en el encargo y **no** duplicados aquí por estar cerrados en código: `R-161` (cerrado, F.4), `R-166` (cerrado, F.5), `R-162`/`R-165` (cerrados, B.3/B.5), `R-143` (cerrado, `review/service.py:48-67`), `R-139` (cerrado, A.24). Abiertos y ya registrados: `R-140` (cancel sin motivo/rol, parcial: `operations/service.py:1335-1339` sí bloquea `SAP_CONFIRMED/SAP_ERROR`), `R-142`, `R-77`, `R-80`, `R-98`/`R-119` (frontend), `R-112`, `R-164` (`lots.company_id` nulable sin `NOT NULL`, `masters/models.py:283`).

---

## Veredicto por compuerta

| Compuerta | Veredicto | Motivo principal |
|---|---|---|
| Inquilino (tenant) | **PARTIAL** | Aislamiento sólido y fail-closed en auth/masters/lots/operations/review/audit/notifications; huecos: SAP sin contexto (GAP-02), `company_id` de maestros (GAP-05), estructurales del lote (GAP-06), contraseña sin contexto (GAP-04). |
| Habilitación de empresa (company BU) | **PASS** | Absoluta en lectura y escritura para todo actor, incluida la autoridad global (`OD-16.e/f`); `OD-23` implementado con auditoría por concesión. |
| Concesión de usuario (user BU) | **PASS** | Evaluada de BD en cada petición; revocación y apagado surten efecto de inmediato; vacío ⇒ nada. |
| RBAC | **PARTIAL** | Cobertura total de rutas y permisos por BD; **FAIL** en la fabricación de autoridad global desde un rol de inquilino (GAP-01). |
| OD-16 (apagado prevalece) | **PASS** | B.13/B.14. |
| OD-23 (ciclo apagar/encender) | **PASS** | C.1–C.6. |
| Administrador de accesos (OD-15) | **PASS** | E.1–E.5 (self-grant, cruce de empresa, candidatos, «administrar ≠ acceder»). |
| Actor global (OD-14) | **PARTIAL** | Contextual en todo el dato productivo y maestros; **no** en SAP (GAP-02) ni en el restablecimiento de contraseña (GAP-04). |
| Cruce de empresa | **PARTIAL** | Bloqueado en las superficies certificadas; residuos GAP-05/GAP-06 y oráculos de unicidad global (GAP-15). |
| Atomicidad | **PASS** | Frontera transaccional por ruta con verificación en arranque; aprobación+lote, reverso y notificaciones en la misma transacción; excepciones documentadas (evidencias, login fallido). |
| Idempotencia | **PARTIAL** | Clave de cliente y SAP implementadas; defecto en el re-enlace SAP y colisión global/por empresa (GAP-14/15). |
| Concurrencia | **PASS** | `FOR UPDATE` en saldos (aves, huevos, incubación), decisión de revisión y reverso; lock asesor para el consecutivo del lote; consolidación SAP sin bloqueo (P3). |
| Sesión | **PARTIAL** | Recarga de usuario/permisos/unidades por petición (revocación inmediata) pero refresh usable como access (GAP-03) y sin logout/rotación (GAP-09). |
| Fuga por unidad en lecturas agregadas | **PARTIAL** | GAP-07 (incubadora sin lote) y GAP-08 (alertas del panel). |
