# Auditoría C — Contrato de respuesta · Manejo de errores · Estado tras mutación

Repositorio: `/home/maria/Proyectos/GlobalAvicola` (solo lectura; sin tests ni builds). Fecha: 2026-09-13.
Rutas citadas: `backend/app/...` y `frontend/src/...` (línea = `archivo:línea`).

Leyenda de severidad propuesta: **Alta** (flujo de negocio roto o pantalla en blanco) · **Media** (dato mostrado incorrecto/omitido o feedback ausente con vía de escape) · **Baja** (cosmético, deuda de contrato, riesgo latente).

---

## AUDIT 1 — PARIDAD DEL CONTRATO DE RESPUESTA

### 1.1 Rutas consumidas por el frontend que NO declaran `response_model` (devuelven ORM crudo o `dict`)

| Ruta | Evidencia | Forma real devuelta | Consumidor |
|---|---|---|---|
| `GET /review/pending` | `review/router.py:22-40` | `{events: [ORM OperationalEvent con relaciones selectin], total}` | ReviewCenter, useReview |
| `GET /review/batches` | `review/router.py:57-66` | `{batches: [ORM ReviewBatch (+actions selectin)], total}` | ReviewDetail |
| `POST /review/start/{id}`, `/review/return`, `/review/complete` | `review/router.py:73-100` | ORM `OperationalEvent` | ReviewCenter, ReviewDetail |
| `GET /approvals/pending` | `review/router.py:107-120` | `{events: [ORM], total}` (solo `status=corrected`) | ApprovalPanel |
| `POST /approvals/approve|reject|batch-*` | `review/router.py:123-160` | ORM `OperationalEvent` / lista | ApprovalPanel, ReviewDetail |
| `GET /approval-steps` | `review/router.py:167-173` | lista ORM | (sin consumidor) |
| `GET /audit`, `GET /audit/timeline/...` | `audit/router.py:16-41, 54-63` | `{logs: [ORM AuditLog], total}` / `{timeline,...}` | AuditPage (timeline sin consumidor) |
| `GET /reports/*` (todos) | `reports/router.py:15-156` | `dict` ad hoc por KPI | LotDetailPage, ReportsPage, LotReportPage, SapComparisonPage |
| `GET /dashboard/mobile|admin` | `dashboard/router.py:13-28` | `dict` | DashboardPage |
| `GET /sap/references`, `POST /sap/references/import`, `POST /sap/consolidate`, `POST /sap/retry`, `GET /sap/sync/jobs`, `GET /sap/payloads`, `GET /sap/errors`, `GET /sap/connection-check` | `integrations/sap/router.py:20-184` | ORM crudo / `dict` | SapManagerPage, OperationFormPage (references) |
| `GET /operations/event-types` | `operations/router.py:33-35` | `[{type,label}]` (el servicio TS lo tipa `string[]`, `operations.service.ts:49-50`) | (sin consumidor) |
| `GET /roles/permissions-catalog` | `auth/router.py:164-174` | `PermissionCatalog` sin declarar | RolesPage |

Nota: los ORM crudos de `/review/*` y `/approvals/*` viajan con `bird_movements`, `evidences`, `alerts`, etc. (todas las relaciones son `lazy="selectin"`, `operations/models.py:163-170`), es decir, la lista de revisión expone más que el detalle tipado. `/sap/references` depende de ese comportamiento: `SapReferenceRead` (`sap/schemas.py:31-42`) **no** declara `quantity`, pero `OperationFormPage.tsx:636` lee `sapOrder.quantity` (funciona solo porque la ruta no tiene `response_model`).

### 1.2 Paridad por pantalla (ruta → esquema backend → tipo TS → campos renderizados → hallazgo)

| Pantalla / componente | Ruta(s) | Esquema backend | Tipo TS usado | Campos renderizados | Hallazgos |
|---|---|---|---|---|---|
| `stores/auth.store.ts` (`/me`) | `GET /me` | `SessionRead` (`auth/schemas.py:101-141`): `UserRead` + `is_super_admin`, `effective_company_id`, `permissions[]`, `company_business_units[]`, `granted_business_units[]`, `effective_business_units[]`, `company_name`, `area_id` | `User` (`auth.store.ts:23-41`) | permisos/unidades/empresa efectiva (guards, Header, UnitAccess) | Paridad correcta. `authService.UserResponse` (`auth.service.ts:14-26`) es un tipo obsoleto sin `is_super_admin/permissions` (no usado por el store). |
| LotListPage | `GET /lots?limit=100` | `list[LotRead]` (`lots/schemas.py:77-86`): `start_date: datetime|null`, `house_id|null`, `planned_close_date`, `activation_type`, `weight_curve_id`, `area_id` — **sin** `sap_reference`, `current_phase`, `age_days` | `any` | `lot_code`, `status`, `bird_type`, `start_date` | `start_date` se pinta como ISO completo (`LotListPage.tsx:88,117`: `lot.start_date || '—'`) → «2026-09-01T00:00:00Z». Filtro `bird_type` en cliente sobre 100 filas (`:29-30`): >100 lotes = lotes invisibles. `BIRD_TYPE_KEYS` (`:9`) omite `hatchery`. `domain.types.Lot` (`domain.types.ts:92-106`) declara `start_date: string` no nulo y `current_phase/age_days` inexistentes; `lots.service.LotResponse.sap_reference` (`lots.service.ts:16`) no existe en backend. |
| LotDetailPage | `GET /lots/{id}`, `/lots/{id}/phases`, `/reports/kpis?lot_id`, `/operations?lot_id&limit=50`, `/reports/kpi/ipe/{id}`, `/reports/kpi/weight-uniformity/{id}`, `/operations/alerts` | `LotDetailRead` (`lots/schemas.py:89-91`) incluye `phases` y `opening_balance`; `LotPhaseRead` (`:112-116`) tiene `phase_id` pero **no** `phase` anidado (el ORM sí lo carga, `lots/models.py:31`, pero el `response_model` lo descarta) | `any` | código, estado, tipo, `start_date`, `planned_close_date`, `farm_id`/`house_id` (ids), KPIs, eventos, alertas | (a) `activePhase?.phase?.name` (`LotDetailPage.tsx:91`) siempre `undefined` → `resolveStageKey` (`processCatalog.ts:312-322`) devuelve siempre *rearing* para reproductoras/abuelas: botón «Iniciar Producción» visible aunque ya esté en producción y catálogo de operaciones de la fase equivocada. (b) `opening_balance` de `LotDetailRead` **se ignora**: la pantalla no muestra población inicial ni saldo (no hay «población tras recepción»). (c) Tabla semanal (`:249-268`) lee `e.bird_movements/feed_movements` de `/operations` lista, que devuelve `OperationalEventRead` (`operations/schemas.py:256-266`) **sin sublistas** → la «Vista semanal» nunca aparece. (d) Granja/galpón como ids numéricos (`:346-347`); `area_id` no se muestra. (e) `start_date` ISO crudo (`:342`) mientras `planned_close_date` se recorta a 10 (`:344`). (f) Llama a `/lots/{id}/phases` aunque `/lots/{id}` ya trae `phases`. (g) Alertas: `threshold_value/actual_value` no se muestran (`:436-449`). |
| LotFormPage | `POST /lots`, `GET /masters/*`, `GET /masters/genetic-lines/{id}/weight-curves` | `LotCreate` (`lots/schemas.py:34-47`) **sin** `sap_reference` | zod local | — | `sap_reference` viaja (`LotFormPage.tsx:131`) y Pydantic lo descarta en silencio (patrón P0-14/R-47): el 201 no persiste la referencia SAP capturada. `start_date` viaja como `YYYY-MM-DD` a un `DateTime(timezone=True)` (`masters/models.py:302`) y vuelve como datetime → ver render ISO. |
| OperationListPage | `GET /operations?limit=100&lot_id&event_type` | `list[OperationalEventRead]` | `any` | `event_type`, `status` (crudo), `lot_id`, `event_date` | `ev.status` sin traducir (`OperationListPage.tsx:85`); `STATUS_COLORS` (`:10-14`) solo 5 de 14 estados; «Lote #» cuando `lot_id` es `null` (`:87`, importación de abuelas R-153). Filtro por etapa en cliente sobre 100 filas (`:36-39`). |
| MyPendingPage / useMyPending | `GET /operations?registered_by_me&status=draft,registered&limit=50` | ídem (backend acepta coma, `operations/router.py:49-52`) | `Operation` local con `lot?: {name}` | tipo, estado, `lot.name`, fecha | `op.lot` no existe en backend → nombre de lote nunca se muestra (`MyPendingPage.tsx:18,101`). `new Date('YYYY-MM-DD').toLocaleDateString()` (`:102`) interpreta UTC → en UTC−4 muestra el día anterior (R-75/R-80). |
| OperationDetailPage | `GET /operations/{id}`, `POST /operations/{id}/submit`, `POST/DELETE /operations/{id}/evidences`, `GET .../download` | `OperationalEventDetailRead` (`operations/schemas.py:298-305`) declara `evidences` y `egg_storage_records`, **pero el router los descarta**: `operations/router.py:277-292` hace `pop("evidences")`/`pop("egg_storage_records")` y solo reasigna bird/egg/feed/hatchery/inspection → `evidences` y `egg_storage_records` llegan **siempre `[]`** | `any` + `Evidence` local | id, estado, tipo, fecha, lote (con «se creará al aprobar»), versión, observaciones, `sap_document_ref`, aves/alimento/huevos, plan de importación, evidencias | (a) `setEvidences(data.evidences || [])` (`OperationDetailPage.tsx:66`) queda vacío en cada carga: los adjuntos subidos **desaparecen al refrescar** (existe `GET /operations/{id}/evidences`, `router.py:334-341`, que la pantalla no usa). (b) No se renderizan `water_liters`, `received_total/dead_on_arrival/rejected_on_arrival`, `chicks_healthy/weak`, `supplier_id`, `vaccine_id`, `medication_id`, `hatchery_params`, `inspection_details`, `egg_storage_records`, `reviewed_by_id/approved_by_id` (`:196-258`). (c) `STATUS_COLORS` (`:11-18`) sin `sap_error`, `reversed`, `cancelled`. (d) `event_type` crudo (`:196`). |
| WeightEvaluation | `GET /operations/{id}/weight-evaluation` | `WeightEvaluationRead` (`operations/schemas.py:356-368`) con `reason` | `Evaluation` local (correcto) | estado por fila, rango, versión de curva, edad | `reason` (por qué no hay referencia) **no se muestra**; se pinta un texto genérico (`WeightEvaluation.tsx:84-91`). Si `evaluations` es `[]` (p. ej. recepción sin peso) el bloque desaparece sin decir nada (`:56`). |
| OperationFormPage | `POST /operations`, catálogos, `/lots?limit=100`, `/sap/references` | `OperationalEventCreate` → responde `OperationalEventRead` | zod | — | La respuesta del POST se **descarta** (`OperationFormPage.tsx:452-455`): no hay id, ni enlace al registro creado, ni CTA «enviar a revisión». `/lots?limit=100` (`:343`) sin paginar. |
| ReviewCenter | `GET /review/pending?status=&operator_id=&...` | El router **solo** acepta `farm_id, lot_id, event_type, date_from, date_to, limit, offset` (`review/router.py:23-33`); el servicio fija `status IN (registered, pending_review)` (`review/service.py:165-174`) | `any` (`reviewService.getPending` tipa `events: ApprovalAction[]`, `review.service.ts:35` — incorrecto) | id, estado, tipo, lote, fecha | **`status` y `operator_id` se ignoran**: las 5 pestañas (`ReviewCenter.tsx:33-40,71-96`) devuelven la misma lista; los eventos `in_review`, `returned`, `approved`, `consolidated` nunca se listan; los botones «Completar/Devolver» (`:324-334,422-432`) exigen `status==='in_review'` y **nunca aparecen**. El filtro de operador carga `/users?limit=100` (`:110`) para un parámetro que el backend no atiende. |
| ReviewDetail | `GET /operations/{id}`, `GET /corrections/event/{id}`, `GET /review/batches?limit=50`, acciones | `CorrectionRead` (`corrections/schemas.py:26-37`); `/review/batches` ORM con `actions` (`review/models.py:57-59`, selectin) | `any` | datos, submovimientos, correcciones, «historial de acciones» | El historial solo ve `ApprovalAction` con `batch_id` (`ReviewDetail.tsx:46-53`); las acciones individuales (start/return/approve/reject, `batch_id=null`) **nunca** aparecen; no existe `GET` de acciones por evento. `event.status` crudo (`:95`). Botones approve/reject para `corrected|in_review` (`:239`) coinciden con `_get_event_for_approval` (`review/service.py:629`). |
| ApprovalPanel | `GET /approvals/pending`, `POST /approvals/*` | lista `status=corrected` únicamente (`review/service.py:463`) | `any` | id, tipo, lote, fecha, estado | KPIs «Aprobados/Rechazados» (`ApprovalPanel.tsx:146,168`) cuentan sobre una lista que solo contiene `corrected` → siempre 0. Respuesta de `approve` (evento con `lot_id` del lote creado, R-153, `review/service.py:511-515`) **descartada**: no se ofrece el enlace al lote creado. |
| CorrectionForm | `POST /corrections`, `GET /masters/correction-types` | `CorrectionCreate` (`corrections/schemas.py:8-23`); lista blanca `campos_corregibles()` (`corrections/service.py:171-181`) | `any` | selector de campo (solo `observations`, `CorrectionForm.tsx:72-74`) | La UI solo corrige `observations` aunque el backend admite todos los campos de `OperationalEventUpdate` menos tres. |
| AuditPage | `GET /audit?limit=50&action&module&date_from&date_to` | `AuditLog` ORM (proyección `AuditLogRead`, `audit/schemas.py:8-30`): `user_id`, `previous_values`, `new_values`, `previous_state`, `new_state`, `change_reason`, `comments` | `any` | acción, usuario, entidad, detalle | Usa `log.user_name`, `log.old_value`, `log.new_value` (`AuditPage.tsx:90,94-95`) — **no existen** → usuario = id numérico; la pestaña «Correcciones» nunca muestra el diff; `previous_state→new_state`, `new_values`, `comments` se ignoran. `limit=50` fijo sin paginar mientras el contador muestra `total` (`:61,109`). `/audit/timeline` no se consume. |
| DashboardPage | `GET /dashboard/admin|mobile`, `PATCH /operations/alerts/{id}/resolve` | `dict` (`dashboard/service.py:75-85,142-151`); `lots_by_type = {str(row.bird_type): cnt}` (`:171`) con `bird_type` mapeado a `Enum(BirdTypeEnum)` (`masters/models.py:295`) → `str()` de un `(str, Enum)` produce **`'BirdTypeEnum.BROILER'`** (verificado con Python) | `any` (`dashboard.service.ts:11-20` tipa `top_event_types` como array; el backend devuelve dict) | contadores, `lots_by_type`, `mortality_trend`, `by_status`, `top_event_types`, `active_alerts` | Las tarjetas «Lotes activos por etapa» buscan `lotsByType['broiler'|...]` (`DashboardPage.tsx:255,513`) → **0 en las cuatro** mientras el subtítulo (suma de valores, `:148`) es correcto. Los tests (`backend/tests/test_kpi_scope.py:286,342`) comparan por substring en minúsculas y no lo detectan. `Badge` de `by_status` con `sap_confirmed/sap_error/reversed` cae a «neutral». |
| ReportsPage | `GET /reports/kpis?lot_id=2`, `GET /operations?lot_id=2&limit=100` | `get_all_kpis` dict (`reports/service.py:415-437`) | `any` | KPIs, gráficos diarios | `lotId` **hardcodeado a 2** (`ReportsPage.tsx:13`) y enlace `/reports/lot/2` (`:193-197`). Gráficos de mortalidad/alimento/peso (`:25-31`) leen sublistas que la lista no devuelve → series planas (solo `water_liters`, que es columna, funciona). Sin selector de lote real (input numérico). |
| LotReportPage | `GET /reports/lot/{id}` + 6 KPIs | dict (`reports/service.py:346-377`) | `any` | lote, saldo, resumen, KPIs | Paridad correcta con las claves devueltas (`nacimiento_pct`, `insufficient_data`, `viabilidad_pct`, `cv_pct`…). `bird_type` en cabeceras de export no viene en `report.lot`. |
| SapComparisonPage | `GET /reports/sap-comparison` | dict (`reports/service.py:394-407`) | `any` | totales, eventos | Paridad correcta; `event_type`/`status` crudos. |
| SapManagerPage | `GET /sap/references|sync/jobs|payloads|connection-check`, `POST /sap/consolidate|export` | `PayloadStatus = prepared|sending|confirmed|failed|retrying`, `SyncStatus = pending|in_progress|completed|failed` (`sap/models.py:40-52`); `connection-check` devuelve `connected, adapter, delivers_to_sap, mode` (`sap/router.py:179-184`) | `any` | KPIs, pestañas | Filtra por `'pending'|'draft'|'sent'|'error'` (`SapManagerPage.tsx:70-72,204,228,252`) → «Pendientes» y «Errores» **siempre 0**, los `failed` nunca se ven; jobs `failed` reciben variante `pending` (`:191,291`). `delivers_to_sap`/`mode` ignorados: un adaptador simulado se muestra como conectado (`:90-96`). `SapSyncJob`/`SapPayload` TS (`sap.service.ts:14-31`) sin `error_message`, `sap_document_id`. |
| NotificationBell | `GET /notifications`, `/unread-count`, `PATCH .../read` | `NotificationRead` (`notifications/schemas.py:10-22`) | `Notification` (correcto) | tipo, detalle, hora, leído | Paridad correcta. Solo compone texto para `record_rejected` y `sap_send_failed` (`NotificationBell.tsx:108-113`); `mortality_over_threshold`, `weight_out_of_standard`, `review_pending_24h`, `lot_near_close` muestran solo el título (payload ignorado). `related_entity_type='lot'` (`notifications/sla.py:194`) no navega (`notifications.ts:56-61`). |
| UnitAccessPage / UserBusinessUnitsButton | `/business-units*`, `/users/{id}/business-units*` | `HabilitacionRead`, `ConcesionRead`, `CandidatoRead` (`business_units/schemas.py`) | tipos exactos (`businessUnits.service.ts:10-30`) | todo | Paridad correcta (contrato GA-FE-02). |
| MasterListPage | `GET/POST/PUT/DELETE /masters/{entity}` | `list[read_schema]` + cabecera `X-Total-Count` (`masters/router.py:30-49`) | `any` | columnas configuradas | Correcto (usa `x-total-count`). El formulario solo edita las columnas visibles: para `houses` no captura `farm_id` obligatorio (`HouseCreate`, `masters/schemas.py:95-103`) → 422 garantizado en alta (ver Audit 2). |
| WeightCurvesPage | `/masters/genetic-lines/{id}/weight-curves`, `POST /masters/weight-curves`, `PUT .../activate` | `WeightCurveRead` | tipos exactos (`weightCurves.ts`) | todo | Paridad correcta; maneja el informe de filas rechazadas. |
| UsersPage / RolesPage / ProfilePage | `/users`, `/roles`, `/roles/permissions-catalog`, `POST /users/{id}/password` | `UserRead`, `RoleRead` (con `permissions`), `PermissionCatalog` | `any` | usuario, rol (`roles.find`), área, empresa | Paridad correcta. `company_id: null` de rol de sistema (`RoleRead.company_id`, `auth/schemas.py:192`) no se distingue en RolesPage («visible ≠ asignable» no se muestra). |
| TraceabilityTree | `GET /lots/{id}/traceability`, `POST /lots/egg-batches|chick-batches` | `TraceabilityNode`, `EggBatchCreate` (exige `hatchery_lot_id: int`, `lots/schemas.py:218-224`) | tipos locales (correctos) | vínculos | Envía `hatchery_lot_id: Number(...) || null` (`TraceabilityTree.tsx:88,107`) → 422 (lista) si vacío → ver Audit 2. Fechas con `new Date(...).toLocaleDateString()` (`:180,214,233,267`) → desfase −1 día en UTC−4. |
| Header / company.store | `GET /masters/companies?limit=100`, `POST /switch-company` | `CompanyCatalogRead`, `TokenResponse` | `CompanyOption` | nombre empresa | Correcto (`data.items ?? data` defensivo, `company.store.ts:40`). |
| hooks/* (useLots, useMasters, useOperations, useReview, useApprovals, useSap, useReports) | — | — | — | **Ningún page los importa** (grep vacío). `useLotDetail` asigna `kpis` con la **lista de lotes** (`useLots.ts:50,55`); `useOperationDetail` busca el evento recorriendo `/operations?limit=100` (`useOperations.ts:55-59`); `useLots/useMasters/useOperations` leen `e.response.data.error.message`, forma que FastAPI nunca produce (`useLots.ts:23`, `useMasters.ts:16`, `useOperations.ts:31,61,91`). `api.types.ts` (`PaginatedResponse {data,meta}`, `ApiError {error}`) no coincide con ningún endpoint. Deuda muerta pero engañosa. |

### 1.3 Enumerados, nulos, fechas y paginación (transversal)

| Tema | Backend | Frontend | Hallazgo |
|---|---|---|---|
| `EventStatus` | 14 valores incl. `reversed` (`operations/models.py:58-72`) | i18n `status.*` completo (14) ✔; `statusColors.STATUS_ALIASES` (`statusColors.ts:122-138`) **sin** `sap_confirmed`, `sap_error`, `reversed` (y con alias fantasma `sent/confirmed/error`); `domain.types.EventStatus` (`domain.types.ts:20-33`) sin `reversed`; mapas locales incompletos (`OperationListPage.tsx:10-14`, `OperationDetailPage.tsx:11-18`, `ReviewCenter.tsx:20-30`, `ReviewDetail.tsx:11-16`, `ApprovalPanel.tsx:13-16`) | Badges grises/neutros para el ciclo SAP y `reversed`. Baja. |
| `LotStatus`, `BirdType`, `Sex` | `active|closed|cancelled`, 4 tipos, 3 sexos | i18n `lotStatus.*`, `birdTypes.*` completos ✔ | OK (salvo filtro sin `hatchery` en LotListPage). |
| Alertas `alert_type` | `high_mortality`, `temperature_out_of_range`, `humidity_out_of_range`, `weight_deviation` (`operations/validators.py:572-729`) | i18n `alerts.type` sin `weight_deviation` | Se muestra «weight deviation» sin traducir. Baja. |
| Estados SAP | ver SapManagerPage | vocabulario distinto | Media (arriba). |
| `lot.house_id` / `farm_id` nulos (lotes autocreados) | nulables (`masters/models.py:285`) | `'—'` ✔ pero ids sin nombre | Baja. |
| `start_date` nulo / no nulo | `datetime|null` | nulo → `'—'` ✔; no nulo → ISO completo | Media (render). |
| `event.lot_id` nulo | permitido (`OperationalEventBase.lot_id`) | «Lote #» en 5 listados; solo OperationDetailPage explica «Se creará al aprobar» (`OperationDetailPage.tsx:201-207`) | Baja. |
| Fechas `YYYY-MM-DD` | `date` | `new Date(str).toLocaleDateString()` en MyPendingPage y TraceabilityTree | −1 día en zonas negativas (R-75/R-80). Media. |
| Paginación | listas planas (`/lots`, `/operations`, `/masters/*` con `X-Total-Count`), `{events,total}`, `{logs,total}`, `{batches,total}`, `{references|jobs|payloads,total}` | Todos los `limit` respetan `le=100/200` (GA-REM-011 corregido) ✔ | Sin 422 por límite. Pero `/lots?limit=100` y `/operations?limit=100` se usan como «todo» y se filtra en cliente (LotListPage, OperationListPage, OperationFormPage). Media. |
| Array vs objeto | mixto | consumidores correctos (`data.events`, `data.logs`, `data.batches`, `data.references`) ✔; `MyPendingPage`/`ReportsPage`/`useMyPending` toleran `items ?? data` | OK. |
| `rule` en 400 de negocio | `{detail, rule}` (`main.py:89-98`) | `getErrorMessage` solo lee `detail` (`Toast.tsx:134-140`) | El identificador de regla nunca llega al usuario. Baja. |

---

## AUDIT 2 — MANEJO DE ERRORES

### 2.1 Infraestructura

| Pieza | Comportamiento | Evidencia | Valoración |
|---|---|---|---|
| Interceptor 401 | Un intento de refresh por petición (`_retry`), `axios.post('/api/v1/refresh')` fuera de la instancia, cola compartida (`refreshPromise`); si falla → `forceLogout()` (`auth.store.logout`) → `ProtectedRoute` navega a `/login` | `api.ts:26-80`, `auth.store.ts:133-139`, `App.tsx:45-51` | Refresh implementado y usado ✔. El cierre de sesión es **silencioso**: sin toast ni «sesión expirada»; el formulario abierto (p. ej. OperationFormPage a los 30 min, `config.py:48`) se pierde. Cualquier 401 no relacionado con el token dispara refresh+logout (hoy el backend no emite 401 en negocio: `change_password` usa 400/403/404, `auth/service.py:446-463`). |
| 403 / 404 / 409 / 422 / 5xx / red | Sin manejo central; cada pantalla decide | `api.ts` | Ver 2.2. |
| Error boundary | **No existe** (`grep ErrorBoundary|componentDidCatch` vacío; `main.tsx`, `App.tsx`) | — | Cualquier excepción de render (React #31) = pantalla en blanco sin recuperación. |
| `getErrorMessage` | Normaliza `detail` string/lista/objeto a texto (R-189 F-01) | `Toast.tsx:95-140` | Correcto donde se usa. Fallback: `err.message` de axios («Request failed with status code 403», inglés técnico) cuando no hay `detail`. |
| Traducción | `detail` del backend en español fijo; UI EN muestra mensajes ES | — | Aceptable para el alcance; anotado. |

### 2.2 Por pantalla

| Pantalla | Carga (catch) | Mutaciones (catch) | `detail` crudo en JSX/toast | Estado de formulario tras 4xx | Nativos / recarga | Hallazgo |
|---|---|---|---|---|---|---|
| LoginPage | — | `getErrorMessage` + banner + toast (`LoginPage.tsx:40-44`) | no | preservado ✔ | no | Toast «Bienvenido» aunque `/me` falle (`fetchMe` traga, `auth.store.ts:160-163`). |
| DashboardPage | banner de error + reintento (`:100-105,131-144`) | resolver alerta: toast ✔ | no | — | `window.location.reload()` (`:136`) | Reintento por recarga completa. |
| LotListPage | `console.error` (`:32-33`) | — | — | — | no | 403/500 ≡ «No hay lotes» (patrón R-120/R-150 no aplicado). |
| LotDetailPage | `console.error` (`:66-67`) → «Lote no encontrado» para 403/404/500; sub-cargas `allSettled` mudas | cerrar lote (`:114-116`) y transición (`:133-135`): **solo `console.error`**; resolver alerta: toast ✔ | no | modal ya cerrado antes del POST (`:109,123`) → no hay retorno visible | no | Cierre de lote fallido sin ningún feedback; transición de fase falla **siempre** (422) y nadie lo ve. |
| LotFormPage | `allSettled` mudo (selects vacíos) | `toast.error(err?.response?.data?.detail ?? ...)` (`:137`) | **sí** → si `detail` es lista (422) el `ToastProvider` renderiza objetos → React #31 → app en blanco | preservado (react-hook-form) ✔ | no | Riesgo real de crash global desde el toast. |
| OperationFormPage | lotes: aviso (`:2088`) ✔; catálogos `allSettled` mudos | `getErrorMessage` + banner + toast (`:456-461`) ✔ | no | preservado ✔ | no | Correcto (R-189). Éxito: navega a `/operations` en 1,5 s sin enlace al registro. |
| OperationListPage | `catch { setEvents([]) }` (`:42`) | — | — | — | no | 403 ≡ «Sin resultados». |
| MyPendingPage | mensaje genérico traducido (`:37-38`) | — | no | — | no | No distingue denegado/error. |
| OperationDetailPage | `getErrorMessage` + estado `error` + toast (`:67-71`) ✔ | subir/borrar evidencia, enviar: toast ✔; envío recarga tras éxito y fallo (`:139-143`) ✔ | no | — | no | Correcto. |
| ReviewCenter | toast (`:99-101`), lista vacía → «Sin resultados» | toast `getErrorMessage` ✔ | no | — | `prompt()` (`:123,144`) | Observación de devolución vía `prompt`; backend exige ≥10 caracteres (`review/schemas.py:98-101`) → 422 lista (manejado). |
| ReviewDetail | `console.error` (`:55-56`) → «Evento no encontrado» | toast ✔; navega a `/review` (`:77`) | no | — | `alert()` (`:69,74`) | — |
| CorrectionForm | `console.error` (`:38-39`) | toast ✔ | no | preservado ✔ | `alert()` (`:49`) | — |
| ApprovalPanel | toast (`:43-44`) | toast ✔ | no | — | no | Sin guarda de doble clic en aprobar (`:52-61,358`). |
| AuditPage | `.catch(() => {})` (`:74`) | — | — | — | no | 403 ≡ «Sin registros de auditoría». |
| ReportsPage | `.catch(() => {})` (`:17,33`); export `console.error` (`:61`) | — | — | — | no | Lote 2 inexistente/ajeno → página vacía sin mensaje. |
| LotReportPage / SapComparisonPage | toast de error pero la vista queda en «Cargando…» para siempre (`LotReportPage.tsx:64`, `SapComparisonPage.tsx:17`) | — | no | — | no | Sin estado de error ni reintento. |
| SapManagerPage | cada `catch` devuelve vacío (`:39-42`) | toast ✔ | no | — | no | 403/feature-flag off ≡ «Sin actividad». |
| MasterListPage | `console.error` (`:59-61`) | guardar: `setFormError(detail)` (`:99`) **crudo**; borrar: `console.error` (`:114`), modal queda abierto sin mensaje | **sí** (422 lista → React #31) | modal preservado ✔ | no | Repro directo: Maestros ▸ Galpones ▸ Nuevo ▸ solo nombre ▸ Guardar → `HouseCreate.farm_id` obligatorio → 422 lista → pantalla en blanco. |
| WeightCurvesPage | banner (`:69-70`) | filas rechazadas + mensaje seguro (`:24-34,119-122`) ✔ | no (`mensajeGeneral` comprueba tipo) | preservado ✔ | no | Referencia correcta. |
| UsersPage | estados `prohibido/error/ok` + reintento (`:37-63,99-101`) ✔ (R-120/R-150 de referencia) | `alert(detail || ...)` (`:85`), `alert(t(...))` (`:88,91`) | `alert` coacciona → «[object Object]» si lista (sin crash) | modal preservado ✔ | `alert/confirm` (`:71,82,85,88,91`) | Carga ejemplar; mutaciones con diálogos nativos y detalle perdido. |
| RolesPage | `console.error` (`:42-43`) → «Sin roles» | `alert(t('common.error'))` (`:85,97`) | no | modal preservado ✔ | `alert/confirm` (`:85,92,97`) | Detalle del backend (p. ej. rol de sistema no editable) perdido. |
| ProfilePage | — | `setMessage(detail || ...)` (`:38`) | **sí** (422 lista → React #31; p. ej. `new_password` < 8 sin validar antes… hoy se valida en cliente `:23`, pero `extra=forbid` u otras 422 siguen posibles) | preservado ✔ | no | — |
| UnitAccessPage / UserBusinessUnitsButton | banner + reintento (`UnitAccessPage.tsx:57-60,198-205`) ✔ | toast genérico; `detail` descartado (`:124,138,153`; `UserBusinessUnitsButton.tsx:85,99`) | no | — | no | Motivos 403/409 del backend (segregación, unidad apagada) invisibles. |
| NotificationBell | error explícito ≠ bandeja vacía (`:78-82,151-153`) ✔ | marcar leída: error (`:93-96`) ✔ | no | — | no | Correcto. |
| TraceabilityTree | error + reintento (`:151-157`) ✔ | `setLinkError(detail ?? ...)` (`:95,114`) **crudo** | **sí** (422 lista si `hatchery_lot_id`/`quantity` vacíos → React #31) | modal preservado ✔ | no | Repro: «Vincular huevos» ▸ Guardar sin destino. |
| Header / company.store | `fetchCompanies` traga (`company.store.ts:42-44`) → desplegable en «Cargando…» eterno (`Header.tsx:106-107`) | `handleSwitchCompany` no captura y `switchCompany` relanza (`Header.tsx:37-40`, `company.store.ts:57-60`) → **unhandled rejection**, sin feedback | no | — | no | — |
| auth.store.fetchMe | traga (`:160-163`) → sesión degradada (sin `permissions`) → todas las `CapabilityRoute` muestran «No tiene permiso» | — | — | — | — | El usuario no sabe que `/me` falló. |

Inventario de nativos: `alert()` ×9 (UsersPage 71,82,85,88,91; RolesPage 85,97; ReviewDetail 69,74; CorrectionForm 49), `confirm()` ×2 (UsersPage 88; RolesPage 92), `prompt()` ×2 (ReviewCenter 123,144), `window.location.reload()` ×1 (DashboardPage 136). `JSON.stringify` mostrado al usuario: ninguno.

---

## AUDIT 3 — ESTADO TRAS ÉXITO / FALLO

| Mutación | Pantalla · evidencia | ¿Relee estado persistido? | ¿Aparece el ítem / cambia estado / enlace / siguiente acción? | Tras fallo: ¿pantalla estable y reintentable? | ¿Toast de éxito prematuro? | ¿F5 / relogin coherente? |
|---|---|---|---|---|---|---|
| Crear operación | `OperationFormPage.tsx:452-455` | No; navega a `/operations` (lista se recarga al montar) | El registro aparece en la lista, pero **sin** enlace directo ni CTA «enviar a revisión»; queda `registered` | Sí (banner + toast, formulario intacto) | No | Sí |
| Enviar / reenviar a revisión | `OperationDetailPage.tsx:128-144` | Sí (`loadEvent()` en `finally`) | Badge pasa a `pending_review`; botón desaparece | Sí | No | Sí |
| Editar / cancelar operación | — | — | **No existe UI** (`PUT /operations/{id}` y `POST .../cancel` existen, `operations/router.py:295-327`; `EDITABLES` incluye `returned/rejected`, `operations/service.py:73-76`). Un evento **devuelto** solo puede reenviarse **sin corregir** | — | — | — |
| Subir / borrar evidencia | `OperationDetailPage.tsx:76-114` | No (append/filter local) | Aparece al instante… | Sí | No | **No**: al refrescar la lista vuelve vacía (router descarta `evidences`, ver 1.2) |
| Iniciar / devolver / completar revisión | `ReviewCenter.tsx:123-135`; `ReviewDetail.tsx:64-81` | Sí (`fetchEvents()` / navegación) | El evento **desaparece** de todas las pestañas (in_review no se lista) → no hay siguiente acción visible; `Completar/Devolver` inalcanzables desde la lista | Sí | No | Sí (pero el evento sigue «perdido») |
| Aprobar / rechazar (individual y lote) | `ApprovalPanel.tsx:52-127` | Sí (`fetchEvents()`) | Sale de la lista ✔; **no** se muestra el lote creado (R-153) ni observaciones; sin guarda de doble clic | Sí | No | Sí |
| Corregir campo | `CorrectionForm.tsx:47-67` | Sí (vuelve a ReviewDetail, que recarga) | Corrección listada; estado `corrected`; aparecen approve/reject ✔ | Sí | No | Sí |
| Cerrar lote | `LotDetailPage.tsx:108-119` | Parcial: `closeResult` de la respuesta + `status` optimista; eventos/KPIs no se releen | Resumen de cierre ✔; tiles «Registrar operación» siguen visibles (gate solo por permiso, `:229-243`) | **No hay feedback** (console) aunque el botón sigue disponible | No | Sí |
| Transición Cría→Producción | `LotDetailPage.tsx:121-138` | Relee `/phases` solo si el POST no falla | **Falla siempre** (422: `phase_code` vs `phase_id`+`lot_id`) | Sin feedback | No | — |
| Crear lote | `LotFormPage.tsx:133-135` | Sí (navega al detalle, que hace GET) | Aparece ✔ (sin `sap_reference`) | Sí | No | Sí |
| Activación manual (saldo de apertura) | — | — | **Sin pantalla** (`lots.service.activateManual` sin uso; `POST /lots/activate-manual`, `lots/router.py:99-106`) | — | — | — |
| Vincular huevos / pollitos | `TraceabilityTree.tsx:82-118` | Sí (`load()`) | Vínculo aparece ✔ | Sí (salvo crash por `detail` lista) | No | Sí |
| Maestros crear/editar/borrar | `MasterListPage.tsx:88-118` | Sí (`fetchItems()`) | ✔ | Guardar: sí; borrar: modal abierto sin mensaje | No | Sí |
| Curvas de peso subir/activar | `WeightCurvesPage.tsx:101-138` | Sí (`refrescar()`) | ✔ | Sí | No | Sí |
| Usuarios crear/editar/activar/desactivar/contraseña | `UsersPage.tsx:70-91` | Sí (`fetchData()`) | ✔ (sin toast de éxito) | `alert` | No | Sí |
| Roles crear/editar/desactivar | `RolesPage.tsx:76-99` | Sí | ✔ | `alert` genérico | No | Sí |
| Cambio de contraseña propia | `ProfilePage.tsx:18-40` | n/a | mensaje ✔ | Sí | No (mensaje tras 204) | — |
| Unidad habilitar/deshabilitar; conceder/revocar | `UnitAccessPage.tsx:115-159`; `UserBusinessUnitsButton.tsx:79-105` | Sí (tras éxito **y** fallo) | ✔ | Sí | No | Sí |
| Marcar notificación leída | `NotificationBell.tsx:88-104` | Usa la respuesta del PATCH + contador local (y sondeo 60 s) | ✔ | Sí | No | Sí |
| Resolver alerta | `LotDetailPage.tsx:75-83`; `DashboardPage.tsx:108-116` | No (filtro local tras éxito) | ✔ | Sí | No | Sí |
| SAP consolidar / exportar | `SapManagerPage.tsx:51-67` | **No** | KPIs/pestañas siguen con datos previos hasta F5 | Sí | No (toast tras respuesta) | Sí |
| SAP reintentar fallidos | — | — | **Sin UI** (`sapService.retry` sin uso) y los `failed` no se listan | — | — | — |
| Cambiar empresa (super admin) | `Header.tsx:37-40`; `company.store.ts:47-61` | Sí (`fetchMe`) | ✔ | **Unhandled rejection** | No | Sí |

### 3.1 Estado persistido en cliente (F5 / relogin)

- Tokens: `sessionStorage` en web (`auth.store.ts:60-69`) → F5 conserva sesión en la misma pestaña; una pestaña nueva pide login (`localStorage` solo en Telegram). `isLoading: !!token` fuerza `fetchMe` antes de los guards ✔ (`auth.store.ts:100`, `App.tsx:177-181`).
- `company.store` **no** persiste: `activeCompanyId/Name` se derivan de `/me` en cada arranque ✔ (`auth.store.ts:146-159`).
- `ui.store`, `theme.store` (`localStorage 'theme-storage'`, `main.tsx:18-23`), `i18n` (`localStorage`), `useSidebar` (`localStorage`), `MenuHubPage` pila (`sessionStorage`, `:65-68`), `operationBackTarget` (`sessionStorage`, `ProcessStagePage.tsx:48`): solo preferencias de UI. **No hay lotes/operaciones/empresa cacheados** → paridad F5 correcta salvo los casos de arriba (evidencias, SAP sin refetch).

### 3.2 OperationDetailPage y LotDetailPage — acciones por estado

| Pantalla | Acción | Condición en cliente | Backend | Observación |
|---|---|---|---|---|
| OperationDetailPage | Enviar / Reenviar | `SUBMITTABLE_STATUSES = registered|returned|rejected` (`:26,121-123`) ∧ `operations:create` ∧ unidades | `REENVIABLES` idéntico (`operations/service.py:75`) → 400 si no | Solo comprobación cliente, pero coherente y con relectura tras 400 ✔ |
| OperationDetailPage | Cancelar | **no existe** | `NO_CANCELABLES` (`:76-78`) | Gap |
| OperationDetailPage | Editar | **no existe** | `EDITABLES = draft|registered|returned|rejected` (`:73-74`) | Gap crítico para `returned` |
| OperationDetailPage | Corregir (revisor) | **no existe** (solo desde `/review/{id}/correct`) | `corrections.service` | Sin enlace desde el detalle |
| OperationDetailPage | Subir/borrar evidencia | solo permiso (`:297,327`), **cualquier estado** (aprobado, consolidado, cancelado…) | `create_evidence` solo comprueba unidad (`operations/service.py:1369`) | Adjuntar a un evento aprobado no está bloqueado ni en UI ni (verificado parcialmente) en backend |
| LotDetailPage | Cerrar lote | `status==='active'` ∧ `lots:create` (`:186`) | `close_lot` | OK |
| LotDetailPage | Iniciar producción | `stageKey` derivado de `phase.name` inexistente (`:91-106`) | `add_phase` con `phase_id` | Siempre visible en cría y siempre 422 |
| LotDetailPage | Registrar operación (tiles) | solo `operations:create` (`:229`), **también en lotes cerrados** | BR-07 rechaza | Feedback tardío (400 al guardar) |

---

## GAP REGISTER

| # | Página | Clase | Severidad | Evidencia | Repro |
|---|---|---|---|---|---|
| 1 | OperationDetailPage | RESPONSE_CONTRACT | **Alta** | `operations/router.py:277-292` descarta `evidences`/`egg_storage_records`; `OperationDetailPage.tsx:66` | Subir un adjunto a cualquier evento → F5 → lista «Sin archivos» aunque `GET /operations/{id}/evidences` los devuelve |
| 2 | ReviewCenter | RESPONSE_CONTRACT | **Alta** | `ReviewCenter.tsx:33-40,88,96` envía `status`/`operator_id`; `review/router.py:23-33` no los declara; `review/service.py:165-174` fija `registered|pending_review` | Pulsar «Iniciar» sobre un pendiente → cambiar a pestaña «En revisión» → la misma lista de pendientes; el evento `in_review` no aparece en ninguna pestaña; «Completar/Devolver» jamás visibles |
| 3 | LotDetailPage | RESPONSE_CONTRACT | **Alta** | `LotDetailPage.tsx:126-131` envía `{phase_code,...}`; `lots/schemas.py:98-109` exige `lot_id`+`phase_id`; `lots/router.py:143`; error solo `console.error` (`:135`) | Lote reproductoras activo → «Iniciar Producción» → Confirmar → 422 silencioso; botón sigue visible |
| 4 | LotDetailPage / ReportsPage | RESPONSE_CONTRACT | **Alta** | `/operations` lista = `OperationalEventRead` sin sublistas (`operations/schemas.py:256-266`); `LotDetailPage.tsx:249-268`; `ReportsPage.tsx:25-31` | Lote con pesajes/mortalidad/alimento → «Vista semanal» nunca se muestra; gráficos de mortalidad/alimento/peso planos (solo agua) |
| 5 | OperationDetailPage | STATE_FEEDBACK | **Alta** | No hay editar/cancelar (`OperationDetailPage.tsx:121-145`); backend `EDITABLES` incluye `returned/rejected` (`operations/service.py:73-76`), `PUT /operations/{id}` (`router.py:295-303`) | Revisor devuelve un evento con observación → operador solo ve «Reenviar a revisión»: no puede corregir el dato antes de reenviar |
| 6 | LotFormPage · MasterListPage · TraceabilityTree · ProfilePage | ERROR_HANDLING | **Alta** | `LotFormPage.tsx:137`, `MasterListPage.tsx:99`, `TraceabilityTree.tsx:95,114`, `ProfilePage.tsx:38` colocan `detail` crudo; `Toast.tsx:74` renderiza `{t.message}`; sin ErrorBoundary | Maestros ▸ Galpones ▸ Nuevo ▸ solo «Nombre» ▸ Guardar → 422 (`HouseCreate.farm_id`, `masters/schemas.py:95-103`) → React #31 → pantalla en blanco |
| 7 | App (global) | ERROR_HANDLING | **Alta** | Sin `ErrorBoundary` (`main.tsx`, `App.tsx`; grep vacío) | Cualquier excepción de render (p. ej. #6) deja la app sin UI ni botón de recuperación |
| 8 | LotDetailPage | ERROR_HANDLING | **Alta** | Cierre de lote: `catch → console.error` (`LotDetailPage.tsx:114-116`); modal cerrado antes del POST (`:109`) | Cerrar lote con eventos pendientes (400 BR-05) → no pasa nada visible; sin toast, sin mensaje |
| 9 | DashboardPage | RESPONSE_CONTRACT | Media | `dashboard/service.py:171` `str(row.bird_type)` → `'BirdTypeEnum.BROILER'` (`masters/models.py:295`, verificado `str()` de `(str, Enum)`); `DashboardPage.tsx:255,513` busca `'broiler'`; tests `test_kpi_scope.py:286,342` comparan por substring | Empresa con lotes activos → subtítulo «N lotes activos» correcto, las cuatro tarjetas muestran 0 |
| 10 | SapManagerPage | RESPONSE_CONTRACT | Media | `SapManagerPage.tsx:70-72,191,204,228,252,291` usa `pending/draft/sent/error`; backend `prepared/sending/confirmed/failed/retrying` y jobs `failed` (`sap/models.py:40-52`); `delivers_to_sap` ignorado (`:90-96`) | Payload en `failed` → pestaña «Errores» dice «Sin errores SAP»; adaptador simulado aparece «conectado» |
| 11 | AuditPage | RESPONSE_CONTRACT | Media | `AuditPage.tsx:90,94-95` usa `user_name/old_value/new_value`; `audit/schemas.py:8-30` expone `user_id/previous_values/new_values/previous_state/new_state` | Pestaña «Correcciones» → nunca muestra el cambio; usuario aparece como número |
| 12 | LotFormPage | RESPONSE_CONTRACT | Media | `LotFormPage.tsx:131` envía `sap_reference`; `LotCreate`/`LotRead` (`lots/schemas.py:12-86`) no lo declaran | Crear lote con «Referencia SAP» → 201 → el detalle no la tiene; no existe en BD |
| 13 | ApprovalPanel / ReviewDetail | STATE_FEEDBACK | Media | Respuesta de `approve` descartada (`ApprovalPanel.tsx:54-58`, `ReviewDetail.tsx:72,77`); lote creado en `review/service.py:511-515` (R-153) | Aprobar `grandparent_import` sin lote → toast «Evento aprobado» sin enlace al lote `L-GP-…`; hay que buscar el evento en Operaciones |
| 14 | OperationFormPage | STATE_FEEDBACK | Media | `OperationFormPage.tsx:452-455` descarta la respuesta y navega a la lista | Guardar operación → 1,5 s → lista; sin id, sin enlace, sin «enviar a revisión» |
| 15 | ReviewDetail | RESPONSE_CONTRACT | Media | `ReviewDetail.tsx:46-53` deriva historial de `/review/batches`; acciones individuales tienen `batch_id=null`; sin endpoint por evento | Evento rechazado individualmente → «Historial de acciones» vacío |
| 16 | LotDetailPage | RESPONSE_CONTRACT | Media | Ignora `opening_balance` de `LotDetailRead` (`lots/schemas.py:89-91`); no hay población; `phase` anidado ausente (`:91`); granja/galpón por id (`:346-347`) | Tras recepción de aves el detalle del lote no refleja población alguna |
| 17 | MyPendingPage · TraceabilityTree · LotListPage · LotDetailPage | RESPONSE_CONTRACT | Media | `new Date('YYYY-MM-DD').toLocaleDateString()` (`MyPendingPage.tsx:102`, `TraceabilityTree.tsx:180,214,233,267`); `start_date` ISO crudo (`LotListPage.tsx:88,117`, `LotDetailPage.tsx:342`) | Navegador en UTC−4: evento del 2026-09-13 se lista como 12/9/2026; lote muestra «2026-09-01T00:00:00+00:00» |
| 18 | ReportsPage | RESPONSE_CONTRACT | Media | `ReportsPage.tsx:13` `useState(2)`; `:193-197` enlace `/reports/lot/2`; errores `.catch(() => {})` (`:17,33`) | Empresa sin lote 2 → tarjetas vacías sin mensaje |
| 19 | LotListPage · OperationListPage · AuditPage · ReportsPage · SapManagerPage · RolesPage · ReviewDetail · CorrectionForm · MasterListPage | ERROR_HANDLING | Media | `LotListPage.tsx:32-33`, `OperationListPage.tsx:42`, `AuditPage.tsx:74`, `ReportsPage.tsx:17,33`, `SapManagerPage.tsx:39-42`, `RolesPage.tsx:42-43`, `ReviewDetail.tsx:55-56`, `CorrectionForm.tsx:38-39`, `MasterListPage.tsx:59-61` | Usuario sin `lots:read` (guard cliente omitido/desactualizado) o backend 500 → «No hay lotes» / «Sin resultados» / «Evento no encontrado» en vez de «denegado/error» (contraste: `UsersPage.tsx:37-63` sí distingue) |
| 20 | LotReportPage · SapComparisonPage · Header | ERROR_HANDLING | Media | `LotReportPage.tsx:64`, `SapComparisonPage.tsx:17` («Cargando…» eterno); `Header.tsx:106-107` + `company.store.ts:42-44`; `Header.tsx:37-40` + `company.store.ts:57-60` (rechazo no capturado) | 403 en `/reports/lot/{id}` → toast y spinner infinito; fallo en `/switch-company` → nada visible, error en consola |
| 21 | api.ts / auth.store | ERROR_HANDLING | Media | `api.ts:66-78` → `forceLogout` silencioso; `auth.store.ts:160-163` traga fallo de `/me` | Refresh vencido (7 d) o `/refresh` 401 a mitad de formulario → salto a `/login` sin aviso y formulario perdido; `/me` 500 → «No tiene permiso» en todas las secciones |
| 22 | SapManagerPage | STATE_REFRESH | Media | `SapManagerPage.tsx:51-67` sin refetch tras consolidar/exportar; sin UI para `/sap/retry` | Consolidar → toast «N consolidados» → KPIs y pestañas sin cambio hasta F5 |
| 23 | LotDetailPage | STATE_FEEDBACK | Media | Tiles «Registrar operación» gated solo por permiso (`LotDetailPage.tsx:229-243`) | Lote cerrado → tile → formulario → 400 BR-07 al guardar |
| 24 | Lotes (activación manual) · SAP retry · Auditoría timeline · Correcciones | STATE_FEEDBACK | Media | `lots.service.activateManual`, `sapService.retry`, `auditService.getTimeline` sin consumidores (grep); `CorrectionForm.tsx:72-74` solo `observations` frente a `campos_corregibles()` (`corrections/service.py:171-181`) | Capacidades de backend sin superficie: P-11 saldo de apertura, reintento SAP, línea de tiempo por entidad, corrección de campos numéricos |
| 25 | ApprovalPanel | RESPONSE_CONTRACT | Baja | `ApprovalPanel.tsx:146,168` cuentan `approved/rejected` en una lista solo `corrected` | KPIs «Aprobados/Rechazados» siempre 0 |
| 26 | Listados de eventos | RESPONSE_CONTRACT | Baja | «Lote #» con `lot_id=null` (`OperationListPage.tsx:87`, `ReviewCenter.tsx:313,405`, `ApprovalPanel.tsx:234,301`, `ReviewDetail.tsx:109`); `MyPendingPage.tsx:101` `op.lot?.name` inexistente | Importación de abuelas sin lote → «Lote #» |
| 27 | statusColors / mapas locales / tipos | RESPONSE_CONTRACT | Baja | `statusColors.ts:122-138` sin `sap_confirmed/sap_error/reversed`; `domain.types.ts:20-33` sin `reversed`; `OperationListPage.tsx:10-14`, `OperationDetailPage.tsx:11-18`, `ReviewCenter.tsx:20-30`; `alerts.type` sin `weight_deviation` (`validators.py:729`); estados/tipos crudos (`OperationListPage.tsx:85`, `LotDetailPage.tsx:319-327`, `ReviewDetail.tsx:95`, `SapComparisonPage.tsx:43`) | Evento `sap_error` → badge gris «neutral»; alerta de peso → «weight deviation» |
| 28 | WeightEvaluation · Toast | RESPONSE_CONTRACT | Baja | `reason` no renderizado (`WeightEvaluation.tsx:84-91` vs `operations/schemas.py:367`); `rule` de 400 no leído (`main.py:95-98`, `Toast.tsx:134-140`) | Pesaje sin curva → «Sin referencia» sin motivo; BR-08 → texto sin id de regla |
| 29 | OperationDetailPage | RESPONSE_CONTRACT | Baja | `:196-258` no muestra `water_liters`, cuadre de recepción, `chicks_*`, vacuna/medicamento/proveedor, `hatchery_params`, `inspection_details`, `egg_storage_records` | Consumo de agua → detalle sin litros |
| 30 | UnitAccessPage · UserBusinessUnitsButton · RolesPage · UsersPage · MyPendingPage | ERROR_HANDLING | Baja | `detail` descartado por mensaje genérico (`UnitAccessPage.tsx:124,138,153`; `UserBusinessUnitsButton.tsx:85,99`; `RolesPage.tsx:85,97`; `UsersPage.tsx:88,91`; `MyPendingPage.tsx:38`) | Conceder unidad a uno mismo (403 segregación) → «Error al guardar» sin motivo |
| 31 | UsersPage · RolesPage · ReviewDetail · CorrectionForm · ReviewCenter · DashboardPage | ERROR_HANDLING | Baja | `alert/confirm/prompt` (ver inventario 2.2); `window.location.reload()` (`DashboardPage.tsx:136`) | — |
| 32 | NotificationBell | RESPONSE_CONTRACT | Baja | `NotificationBell.tsx:108-113` solo compone `record_rejected`/`sap_send_failed`; `related_entity_type='lot'` sin destino (`notifications.ts:56-61`) | Aviso «Mortalidad sobre umbral» sin lote ni valor; «Lote próximo a cierre» no navega |
| 33 | LotListPage · OperationFormPage · OperationListPage · AuditPage | RESPONSE_CONTRACT | Baja | `/lots?limit=100` filtrado en cliente (`LotListPage.tsx:29-30`, `OperationFormPage.tsx:343`); `/operations?limit=100` (`OperationListPage.tsx:26`); `AuditPage.tsx:61` `limit=50` sin paginar | >100 lotes → los últimos no aparecen en selector ni listado |
| 34 | hooks/* · api.types.ts · services | RESPONSE_CONTRACT | Baja | `useLots.ts:23,50,55`, `useMasters.ts:16`, `useOperations.ts:31,55-61,91`, `api.types.ts:6-28`, `review.service.ts:35`, `dashboard.service.ts:17`, `auth.service.ts:14-26`, `lots.service.ts:16`, `sap.service.ts:14-31` | Deuda: contratos TS que no coinciden con el backend y hooks sin uso |
| 35 | ApprovalPanel | STATE_FEEDBACK | Baja | Sin guarda de doble clic (`ApprovalPanel.tsx:52-61,355-364`) | Doble clic en «Aprobar» → segundo POST 400 «no está en estado aprobable» |
| 36 | OperationDetailPage | STATE_FEEDBACK | Baja | Evidencias en cualquier estado (`:297,327`); backend solo verifica unidad (`operations/service.py:1369`) | Adjuntar archivo a evento aprobado/consolidado |
| 37 | LoginPage | STATE_FEEDBACK | Baja | `LoginPage.tsx:37-38` toast tras `login()` aunque `fetchMe` haya fallado | Con `/me` caído: «Bienvenido» y luego «No tiene permiso» en todo |

### Conteo

| Clase | Alta | Media | Baja | Total |
|---|---|---|---|---|
| RESPONSE_CONTRACT | 4 (#1,#2,#3,#4) | 8 (#9,#10,#11,#12,#15,#16,#17,#18) | 8 (#25,#26,#27,#28,#29,#32,#33,#34) | 20 |
| ERROR_HANDLING | 3 (#6,#7,#8) | 3 (#19,#20,#21) | 2 (#30,#31) | 8 |
| STATE_FEEDBACK | 1 (#5) | 4 (#13,#14,#23,#24) | 3 (#35,#36,#37) | 8 |
| STATE_REFRESH | 0 | 1 (#22) | 0 | 1 |
| **Total** | **8** | **16** | **13** | **37** |

Rutas consumidas sin `response_model`: 22 (ver 1.1). Páginas con `catch` que confunde denegación con vacío: 9 (#19) + 2 con carga eterna (#20). Puntos con `detail` crudo en JSX/toast (riesgo React #31): 4 (#6) + 1 `alert` (`UsersPage.tsx:85`). Diálogos nativos: 13 llamadas. Mutaciones sin relectura tras éxito: evidencias (#1), SAP (#22), resolver alerta (local, aceptable), cierre de lote (optimista + respuesta).
