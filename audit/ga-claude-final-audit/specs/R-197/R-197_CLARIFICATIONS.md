# R-197 · CLARIFICACIONES

Fecha: 2026-09-13 · Resueltas con supuesto por defecto antes de C1; las marcadas `OWNER_DECISION_REQUIRED` no bloquean la RED pero sí la implementación del punto afectado.

| # | Pregunta | Resolución / supuesto por defecto | Fuente |
|---|---|---|---|
| C-01 | ¿Dónde vive el filtro por estado: backend o sólo frontend? | **Backend**: `GET /review/pending` acepta `status` (lista) y filtra en la consulta antes de paginar (patrón GA-REM-032 AC10 «el filtrado ocurre en el servidor»). El frontend deja de inventar parámetros | `review/router.py:22-33`; GA-REM-032 AC09-AC11 |
| C-02 | ¿Se renombra `/review/pending`? | No. Se conserva la ruta y su semántica por defecto (compatibilidad); el nombre se documenta como «bandeja de revisión» | §25 |
| C-03 | Conjunto admitido en `status` | `registered, pending_review, in_review, returned, corrected, approved, rejected, consolidated, reversed`; otro valor ⇒ 422. `draft`, `sent_to_sap`, `sap_confirmed`, `sap_error`, `cancelled` no son estados de bandeja (operador / SAP / terminal) | `operations/models.py EventStatus`; `docs/12 §4` |
| C-04 | ¿«Pendientes» incluye `registered` (registrado, aún no enviado)? | **Se conserva** el comportamiento actual (`registered,pending_review`): `create_review_batch` admite ambos (`review/service.py:211`) y el supervisor «toma» registros al crear el lote de revisión. Cambiarlo altera el proceso (docs/12 §4 fila 2/3) | `OWNER_DECISION_REQUIRED` (baja): confirmar que el supervisor debe ver lo `registered` no enviado |
| C-05 | Filtro «Operador» sin `users:read` | Se **oculta** el selector y no se pide `/users`. Con `users:read` se pobla y viaja `registered_by_id`. No se añade `users:read` a ningún rol | `runtime-gp-e2e.json httpErrores`; R-212 |
| C-06 | Nombre del parámetro de operador | `registered_by_id` (columna real). `operator_id` deja de enviarse; si llega, se ignora (documentado) | `OperationalEvent.registered_by_id` |
| C-07 | ¿`GET /approvals/pending` incluye `in_review` (aprobación directa con un nivel, `docs/12 §4` fila 4; `_get_event_for_approval` lo acepta, `review/service.py:629`)? | Por defecto **sin cambio** (sólo `corrected`): con las pestañas corregidas, el aprobador con `review:read` llega a `in_review` por la bandeja y aprueba desde el detalle. Incluirlo cambia quién ve qué en el panel | `OWNER_DECISION_REQUIRED` |
| C-08 | Historial: ¿ruta nueva o incluir `actions` en el detalle del evento? | **Ruta nueva** `GET /review/events/{event_id}/actions` (`review:read`): el detalle de operaciones (`operations:read`) no debe cargar acciones de revisión a quien no puede leer revisión; guardián de rutas 211 → 212 exacto | `ApprovalActionRead` existente |
| C-09 | ¿La lectura de acciones exige habilitación de unidad (`exigir_unidad_operativa`) y bloqueo? | No: es lectura de control; usa empresa + `_ambito_de_unidad` (404 fuera de alcance) y **sin** `FOR UPDATE` (R-166 sólo serializa decisiones) | `review/service.py:387-402`, `:89-108` |
| C-10 | Enlace al lote tras aprobar: ¿cómo se sabe que se creó? | Comparar `lot_id` antes (nulo en la fila listada / detalle) y después (respuesta del POST). Si cambió ⇒ toast persistente con `Link` a `/lots/{lot_id}`; opcionalmente se lee `lot_code` con `GET /lots/{id}` para el texto | `review/service.py:509-515`; `OperationDetailPage.tsx:201-207` |
| C-11 | `prompt()`/`alert()` | Se sustituyen **en las pantallas de revisión** (modal `ConfirmDialog`/`Modal` existentes en `components/ui`); el resto del inventario nativo queda en R-220 C-31 | `C_response_error_state.md §2.2` |
| C-12 | Rol de semillas `TEST Aprobador` sin `review:read` (`test_seeds.py:136-141`) | El E2E local usa los roles canónicos sembrados (`Supervisor Avícola`, `Aprobador`) creando usuarios de prueba para ellos; **no** se cambia la semilla en este paquete (GA-REM-025 AC03 cuenta asociaciones) | `test_seeds.py:145-155` |
| C-13 | KPIs del panel «Aprobados/Rechazados» | Se retiran (no calculables desde la lista `corrected`); queda «Pendientes = total» y «Seleccionados». Un histórico real vendría de `/audit` (fuera de alcance) | `ApprovalPanel.tsx:144-170` |
| C-14 | Doble clic en Aprobar (C-35) | Guarda en cliente (`ref` en vuelo, botón `disabled`) como `OperationDetailPage.tsx:127-144`; el servidor ya responde 400 a la segunda (R-166) | `ApprovalPanel.tsx:52-62` |
| C-15 | `response_model` en listas: ¿rompe algo? | No: consumidores leen `id,status,event_type,lot_id,event_date` (+ `registered_by_id`); `OperationalEventRead` los incluye. `ApprovalPanel`/`ReviewCenter` no leen sublistas | `operations/schemas.py:256-266` |
| C-16 | Contrapartidas de reverso en «Pendientes» | Se listan como cualquier `pending_review` (ya ocurre); el marcador «Contrapartida de reverso» y su bandeja son de **R-207** | R-207 |
| C-17 | Permisos de la barra de lote en `ApprovalPanel` (`review:review`) | **R-208** los corrige; aquí no se toca esa barra salvo la guarda de doble clic | R-208 |

Decisiones abiertas para el propietario: **C-04** (visibilidad de `registered` en «Pendientes», baja) · **C-07** (`in_review` en el panel de aprobación). Ninguna bloquea C1 (RED) ni la mayor parte de C2.
