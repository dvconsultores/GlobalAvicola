# R-199 · CLARIFICACIONES

Fecha: 2026-09-13 · Resueltas por defecto salvo las marcadas `OWNER_DECISION_REQUIRED`; el defecto se aplica si no hay respuesta antes de C2.

| # | Pregunta | Resolución / valor por defecto | Fuente | Estado |
|---|---|---|---|---|
| C-01 | ¿Qué tupla exacta constituye «autoridad global»? | `module == "*"` **y** `scope_type == "all"`, con cualquier `action`. Es la definición literal de `docs/02 §3.1.4` y la que usan `get_current_user` (`security.py:119`), `_es_super_admin` (`service.py:233-235`) y `_es_autoridad_global` (`:168-170`). No se amplía ni se reduce. | `OD-13.c` | RESUELTA |
| C-02 | ¿Se rechaza el par global por **actor** (actor de empresa) o por **rol** (rol de inquilino)? | Por **rol**: un rol con `company_id ≠ NULL` nunca porta el par, lo escriba quien lo escriba (también la autoridad global situada en esa empresa). La autoridad global sigue creando plantillas (`company_id NULL`) con el par. Es una invariante del modelo (`OD-13.b`) y evita depender de quién llama. | `OD-13.b/e` | RESUELTA |
| C-03 | ¿Sigue admitido el comodín de módulo `("*", accion)` con `scope_type ≠ "all"` en un rol de inquilino? | Sí. `tiene_permiso` lo documenta como atajo de catálogo (`security.py:202-204`) y no confiere `is_super_admin`. Se conserva y se cubre con un control (AC11). Observación: `scope_type` `company`/`farm` no filtra datos hoy (sólo distingue el comodín); reinterpretarlo queda fuera de alcance. | `security.py:199-209` | RESUELTA |
| C-04 | ¿Dónde vive la validación de catálogo: esquema o servicio? | `action` y `scope_type` en `PermissionCreate` (pydantic, `422` estándar); `module` en el servicio contra `AuthService.MODULOS ∪ {"*"}` para no importar el servicio desde el esquema. Alternativa aceptable: mover `MODULOS` a `auth/catalog.py` y validar todo en el esquema. Lo decide el implementador; el contrato (`422`) es el mismo. | `GAP-16` | RESUELTA |
| C-05 | ¿Se audita el intento denegado de fabricar autoridad global? | **Por defecto: sí.** Asiento `PERMISSION_CHANGE`, módulo `USERS`, `comments="rechazado: autoridad global desde una superficie de empresa"`, `company_id` de la empresa efectiva del actor, con `commit` propio en la rama de rechazo (patrón `LOGIN_FAILED`, `auth/service.py:187-199`; excepción documentada de `GA-REM-026`). Sin nombrar otra empresa. Alternativa: sólo `logger.warning` (sin asiento en `P-09`), coherente con «una denegación no deja asiento de éxito» pero deja sin rastro un intento de escalada. | `GA-REM-032 AC01/AC03` · `R-83` | **`OWNER_DECISION_REQUIRED`** (defecto: sí) |
| C-06 | Si el inventario (§12) encuentra roles de inquilino ya envenenados, ¿qué se hace con ellos? | **Por defecto: nada automático.** Con la corrección de sesión dejan de conferir autoridad global; se listan en la certificación con `id`, `company_id`, `name`. Opciones para el propietario: (a) borrar los permisos `("*", all)` de esos roles (script de una vez, auditado como `PERMISSION_CHANGE` por el super admin situado); (b) desactivar los roles. `ENV-01`: no hay producción real, así que el coste de (a) es nulo. | `ENV-01` · `OD-13.b` | **`OWNER_DECISION_REQUIRED`** sólo si el inventario `> 0` (esperado `0`) |
| C-07 | ¿Se retira `get_company_filter` (`security.py:170-178`)? | Sí: sin consumidores (grep en `backend/app`), semántica pre-`OD-14` («Super Admin → sin filtro»). Se elimina de `security.py` y de la re-exportación en `dependencies.py:2`. Si un grep en C2 hallara un consumidor, se documenta y se conserva. | `GAP-16` · `OD-14.a` | RESUELTA |
| C-08 | ¿Código HTTP de la denegación del par global: `403` o `422`? | `403`. Es una decisión de autorización («no puedes conceder esto»), no un cuerpo mal formado; alinea con `test_r06` (`403`) y con `create_user`/`update_user` («Ese rol no es asignable…», `403`). `422` queda para el catálogo. | `test_role_tenancy.py:244-250` | RESUELTA |
| C-09 | ¿Hay que tocar `_es_autoridad_global`? | No: contesta «¿ese rol porta el par?» y sirve tal cual. Lo que cambia es que `_rol_asignable` la consulta también para roles de inquilino, y que la **sesión** exige además `Role.company_id IS NULL`. | `service.py:159-171` | RESUELTA |
| C-10 | ¿La corrección de `get_current_user` puede dejar sin autoridad al Super Admin sembrado? | No: la semilla crea el rol «Super Administrador» con `company_id NULL` (`OD-13.b`: «los seis roles sembrados son roles de sistema»); `test_rbac.py::test_ac09_el_super_admin_pasa_en_todos_los_modulos` lo vigila. | semilla `l2m3n4o5p6q7` | RESUELTA |
| C-11 | ¿Se necesita UAT del propietario? | No: backend-only, sin superficie nueva; el uso legítimo no cambia (AC10). Se informa en el ledger. | §46 (UAT «si visible/material») | RESUELTA |
