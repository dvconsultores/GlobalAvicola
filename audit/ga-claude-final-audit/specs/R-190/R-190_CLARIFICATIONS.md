# R-190 · CLARIFICACIONES

Fecha: 2026-09-13 · HEAD `c0b4afc` · Resolver antes de C2 (implementación). Las marcadas `OWNER_DECISION_REQUIRED` exigen respuesta explícita del propietario; las demás tienen supuesto por defecto verificado en código.

| # | Pregunta | Supuesto por defecto | Fuente | Decisión |
|---|---|---|---|---|
| C-01 | ¿Qué tipos de evento abarca R-190? | Los 10 de `location_events` (`validators.py:828-832`) **menos** los de etapa incubadora (`egg_reception_hatchery`, `chick_dispatch` → R-194) y **menos** `bird_reception` (ya F-01e; se conserva sin cambio). Es decir: `bird_distribution`, `bird_transfer`, `bird_exit`, `farm_inspection`, `transport_inspection`, `egg_collection`, `egg_dispatch` | `validators.py:822-839`; `GA_F01E_SUBSANACION_ANNEX.md §3` | técnica (sin decisión) |
| C-02 | Cuando el lote declara galpón y el operador elige otro en la fila, ¿cuál es el `house_id` del evento? | **El del lote** (regla F-01e, C29): el galpón del evento identifica «dónde está el lote»; el detalle por galpón viaja en las filas (`target_house_id`) y lo evalúa R-211 | `OperationFormPage.tsx:392-393`; `R189_CLARIFICATIONS.md C29` | técnica |
| C-03 | ¿Debe persistirse `house_id` en el lote al aprobar la primera recepción de un lote sin galpón (alternativa B), para que los eventos posteriores lo hereden sin selector? | **No por defecto** (alternativa A: derivación en el asistente, sin tocar el lote ni el backend). La alternativa B cambia el modelo de datos del lote en la aprobación (`review/service.py:349-351` / `lots/service.py:128-178`) y afecta a OD-25 (B) | `OD-25 (B)`; `GA_OD_25_GRANDPARENT_LOT_ON_APPROVAL_DECISION.md` | **`OWNER_DECISION_REQUIRED`** — A (por defecto) o B; B no bloquea A y se especificaría como anexo |
| C-04 | `bird_transfer`: ¿el `house_id` del evento es el galpón origen o el destino? | **Origen** (`source_house_id` de la fila 0), y en su defecto destino. El traslado es intra-lote y neutro en saldo (RR-02); el evento «ocurre» donde están las aves | `OperationFormPage.tsx:844-862`; `GA-REM-005 RR-02` | técnica; ratificar con dominio en C1 |
| C-05 | `bird_exit`: el formulario captura granja/planta **destino** pero no galpón origen. ¿Selector nuevo o derivación desde el lote? | Selector «Galpón del evento» (obligatorio si el lote no tiene galpón; preseleccionado y bloqueado si lo tiene) | `OperationFormPage.tsx:869-989` | técnica |
| C-06 | `egg_collection`/`egg_dispatch`: ¿el galpón es obligatorio también cuando la recolección abarca varios galpones? | Sí: el evento lleva un galpón (BR-08); la recolección multi-galpón se registra como eventos separados (sin cambio de dominio). Se anota como residual de UX si el propietario quiere filas por galpón | BR-08 | técnica |
| C-07 | `farm_inspection`: ¿se exige granja en cliente aunque el lote sea opcional? | Sí: `farm_id` = selector de granja ?? granja del primer galpón inspeccionado; sin ninguno ⇒ bloqueo con `operations.farmRequired` | `OperationFormPage.tsx:385-386,438,2060-2071` | técnica |
| C-08 | ¿Se filtra el catálogo del selector por la granja del lote? | Sí (`farmHouses`, `:291-298`); si el lote no tiene granja, catálogo completo de la empresa y `farm_id` se deriva del galpón elegido | código | técnica |
| C-09 | ¿Cambia BR-08 o su mensaje en el backend? | No. La regla y sus mensajes quedan intactos; sólo se añade una prueba de control | `validators.py:822-839` | técnica |
| C-10 | ¿Se reabre la edición (`PUT`) de ubicación por UI? | No: no existe pantalla de edición de ubicación; residual R-220. La guarda de edición (`service.py:1197-1201`) sigue exigiendo BR-08 | `service.py:1197-1201` | técnica |
| C-11 | ¿Qué ocurre con el recorrido GA-UAT-09 (R-189 C4) pendiente del propietario? | Se recertifica sobre la generación de R-190/R-205 (retry 7/7 + los nuevos pasos); no se bloquea | `audit/ga-f01/R189_PLAN_CHECKLIST_TASKS.md P12` | técnica |
| C-12 | Orden de ejecución con R-205 | Misma tranche FE; RED de ambos antes de cualquier producto; implementación en un commit por paquete o commits consecutivos; certificación runtime conjunta | registro §3 | técnica |

Decisiones abiertas que bloquean **sólo** la parte opcional: **C-03** (`OWNER_DECISION_REQUIRED`). La implementación por defecto (alternativa A) está habilitada al cerrar C-04 con dominio.
