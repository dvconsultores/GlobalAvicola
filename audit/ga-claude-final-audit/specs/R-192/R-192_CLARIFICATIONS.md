# R-192 · CLARIFICACIONES

Fecha: 2026-09-13 · Se resuelven antes de C2 (implementación). Cada fila lleva **supuesto por defecto**; las marcadas `OWNER_DECISION_REQUIRED` necesitan decisión del propietario (si no llega, se implementa el defecto y se registra como decisión pendiente en el backlog).

| # | Pregunta | Supuesto por defecto | Fuente / justificación | Estado |
|---|---|---|---|---|
| C-01 | **¿`REVERSED` cuenta como «cerrado/decidido» a efectos de R7 (docs/12 §6 «registros sin aprobar»)?** | **Sí**: el par original+contrapartida en `REVERSED` es un registro **decidido** por el flujo de aprobación (`OD-19 §6`: la contrapartida se aprueba por el motor existente). No es «sin aprobar»; es terminal y no admite acción alguna (`operations/service.py:76-78`). Bloquearlo deja el lote atascado sin salida. | `OD-19 §1, §3, §6`; `GA-REM-041 §3.3`; `validators.py:409-455` | **`OWNER_DECISION_REQUIRED`** (es la precisión de una regla de negocio documentada, R7; el propietario decidió `OD-19`) |
| C-02 | ¿Una contrapartida **pendiente** (`PENDING_REVIEW`/`IN_REVIEW`/`CORRECTED`/`RETURNED`/`REJECTED`) bloquea el cierre? | **Sí** (sin cambio): es un registro vivo sin decidir; R7 aplica literalmente. El cierre se desbloquea aprobándola (⇒ ambos `REVERSED`) o rechazándola/cancelándola. | R7; `OD-19 §2`; `test_lot_close_approval.py::test_t_076_02` | resuelta |
| C-03 | ¿Los sumatorios del resumen excluyen `REVERSED` (ambos miembros) o aplican `_suma_neta` (original suma, contrapartida resta)? | **Excluir ambos** (`status ∉ {CANCELLED, REVERSED}`): resultado idéntico al neto (los dos miembros del par comparten estado y cantidades, `reversals/service.py:140-145,215-216`) con una consulta más simple y sin unir `reversals`. | `validators.py:21-46`; `reversals/service.py` | resuelta |
| C-04 | ¿Cambian `total_events`/`approved_events`? | **No** (compatibilidad): `total_events` sigue contando todos; `approved_events` sigue con `{APPROVED, CONSOLIDATED, SENT_TO_SAP, SAP_CONFIRMED}`. Campos informativos (`cancelled_events`, `reversed_events`) quedan para `R-144`/AOD-08 si el propietario los quiere. | `lots/service.py:501-517`; `GA-REM-029 AC02` | resuelta (revisable en R-144) |
| C-05 | ¿BR-05 debe ignorar pesaje/alimento **revertidos**? | **Sí**: un pesaje cuyo par está `REVERSED` no existe a efectos de FCR/peso final (`OD-19 §3.3`); BR-05 exige «al menos uno vigente». Coste: un lote cuyo único pesaje fue revertido necesita otro pesaje para cerrar (legítimo). | `validators.py:362-390` | resuelta (defecto sí; el propietario puede vetarlo en UAT) |
| C-06 | ¿Toast de éxito al cerrar? | **Opcional, no exigido**: la tarjeta de resumen ya es el feedback de éxito (`LotDetailPage.tsx:198-209`). Si se añade, clave existente `lots.closedSummary`. | UI vigente | resuelta |
| C-07 | ¿El mensaje R7 distingue «reverso pendiente de decisión» cuando los registros vivos son contrapartidas? | **Sí, si el coste es bajo** (una subconsulta a `reversals.reversal_event_id`): «…(1 reverso pendiente de decisión)». Si complica R7, se mantiene el mensaje actual (el estado ya se nombra). Texto en ES (patrón vigente). | `validators.py:445-455` | resuelta (AC17 condicional) |
| C-08 | ¿Se auditaría el cierre en este paquete? | **No**: E-10 se resuelve en `P1-12-REOPEN` (un único productor de auditoría). Evitar duplicar. | `E_domain_ledger.md §3.2` | resuelta |
| C-09 | ¿La UI debe bloquear el botón «Cerrar Lote» si hay registros sin aprobar (pre-validación en cliente)? | **No**: la verdad es del backend (`R-169` patrón); la UI muestra el `detail`. Una pre-comprobación en cliente exigiría listar estados y duplicar R7. | `R-189 F-01 §3` | resuelta |
| C-10 | ¿Cómo se crea el par `REVERSED` en las pruebas backend? | Por la ruta real: `POST /reversals` (actor con `reversals:create`) + `review/start` + `review/complete` (un nivel) con el aprobador; helper reutilizado de `tests/test_internal_reversal.py:226-253` (`_solicitar`, `_aprobar_reverso`, `_reverso_efectivo`). `_fijar_estado` sólo como control adicional (sin fila `reversals`) para documentar que R7 mira el estado, no la tabla. | `test_internal_reversal.py` | resuelta |
| C-11 | ¿Se recertifica `GA-REM-041` (reverso) tras el cambio? | Sí, por regresión: `test_internal_reversal.py` completo (R-130 21/21 incluido) en C2; sin cambio en su código. | `GA-REM-041 §8` | resuelta |
| C-12 | ¿Alcance de la UI: sólo `handleCloseLot` o también `handleTransitionPhase` (`:120-137`, también `console.error`)? | Sólo cierre. La transición de fase es `R-191` (contrato `phase_id`/`lot_id` + feedback) y no debe tocarse aquí para no solapar. | `R-191` | resuelta |

Clarificación crítica abierta: **C-01** (`OWNER_DECISION_REQUIRED`). Con el defecto («sí») la implementación está habilitada; si el propietario decide «no», la única alternativa coherente es una **acción explícita** que descargue el par revertido del lote (fuera de `OD-19` vigente) y este paquete se reescribe.
