# GA-CLAUDE · REGISTRO DE BRECHAS DESCUBIERTAS Y PAQUETES SPEC (§67)

Auditoría independiente Claude · fecha 2026-09-13 · repo `dvconsultores/GlobalAvicola` · rama `main` · HEAD `c0b4afc` (== `origin/main`) · runtime `https://avicola.globaldv.net` (bundle `index-DDCcWL76.js` == build local de HEAD).

Modo: **AUDITORÍA + RECONCILIACIÓN + GENERACIÓN DE SPECS**. Producto intocado (diff 0). Cada brecha material tiene un paquete Spec Development en `audit/ga-claude-final-audit/specs/<ID>/` (6 ficheros: `FINDING`, `SPEC`, `CLARIFICATIONS`, `PLAN_CHECKLIST_TASKS`, `AC_MATRIX`, `RED_E2E_UAT_DESIGN`); las brechas no bloqueantes llevan paquete **compacto** (2 ficheros: `FINDING_SPEC` con todas las secciones §47 y `AC_RED_E2E_UAT`).

## 0 · Asignación de identificadores (dedup §48)

Registro inspeccionado antes de asignar: `audit/remediation/REMEDIATION_BACKLOG.md` (R-001…R-189; P1-01…P1-16; GA-REM-001…042; OD-01…OD-25; AOD-01…AOD-25; OBS-UAT-01…06; GA-UAT-01…09; GA-FE-01…08; GA-GOV-01…02), `specs/remediation/*`, `audit/ga-*/`, `audit/final-frontend-audit/*`. Máximos vigentes: **R-189 · OD-25 · AOD-25 · GA-REM-042 · GA-GOV-02 · GA-TD-060**. Identificadores nuevos asignados aquí: **R-190 … R-221** y **GA-GOV-03**. No se asignan GA-REM nuevos (se asignarán al autorizar la ejecución; siguiente libre: GA-REM-043). `R-189` no figura en el backlog oficial (deriva documental; se registra en `GA_CLAUDE_OPEN_FINDING_RECONCILIATION.md`).

Fuentes de evidencia (todas en `audit/ga-claude-final-audit/evidence/`): `runtime-gp-e2e.json` (+PNG `R0x-*.png`, runtime real, actores UAT-09, empresa 1); `ui-e2e-local-pass1.json` y `ui-e2e-local-pass2.json` (+PNG, pila local aislada con semillas `seeds.test_seeds`, aprobación de un nivel); `backend_full_suite.log`, `backend_targeted_failing.log`, `me500_probe.log`; `frontend_checks.log` (vitest/tsc/build); `playwright_e2e.log` (suites `e2e/` del repositorio); informes de inventario `A_fe_be_trace.md`, `B_form_contracts.md`, `C_response_error_state.md`, `D_security_tx.md`, `E_domain_ledger.md`, `F_i18n_nav_responsive.md`.

Clases (§9): `MISSING_UI` · `DEAD_ACTION` · `BROKEN_FLOW` · `REQUEST_CONTRACT` · `RESPONSE_CONTRACT` · `STATE_REFRESH` · `ERROR_HANDLING` · `SECURITY` · `DATA_INTEGRITY` · `GOVERNANCE` · `UX`.

## 1 · Tabla maestra

| Gap | Hallazgo canónico | Existente/nuevo | Prio | Proceso | FE | BE | Integr. | UI/UX | Seg. | Datos | Bloquea SAP | Paquete (carpeta) | UAT |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| G-01 | **R-190** Eventos de ubicación por UI sobre lotes sin galpón (BR-08) | NUEVO (familia R-189/F-01e; B-04) | **P1** | P-01, P-02, P-03, P-04 | sí | no | sí | sí | no | no | **SÍ** | `specs/R-190/` | sí |
| G-02 | **R-191** Transición de fase por UI → 422 silencioso | NUEVO (B-07, C-3, B-41) | **P1** | P-02, P-04, P-06 | sí | no | sí | sí | no | no | **SÍ** | `specs/R-191/` | sí |
| G-03 | **R-192** Cierre de lote imposible tras reverso efectivo; resumen de cierre con anulados | NUEVO (E-02, E-03; vecino R-144) | **P1** | P-06 cierre, OD-19 | no | sí | no | sí | no | sí | **SÍ** | `specs/R-192/` | sí |
| G-04 | **R-193** BR-18 cuenta original y contrapartida tras reverso | NUEVO (E-01; vecino OD-19 §5, R-176) | P2 | P-01, P-03, P-06 recepción | no | sí | no | no | no | sí | **SÍ** (integridad) | `specs/R-193/` | no |
| G-05 | **R-194** Cadena Incubadora por UI bloqueada (BR-08 granja forzada a `undefined`; recepción sin `egg_movements` fértil ⇒ BR-03; `arrival_date`; nacimiento bloqueado en silencio; `hatchery_id` nunca viaja) | NUEVO (B-01/B-02/B-03/B-05/B-22/B-33; F-01b sólo documentaba `arrival_date`) | **P1** | P-05, P-10 | sí | parcial | sí | sí | no | sí | **SÍ** | `specs/R-194/` | sí |
| G-06 | **R-195** Edición de usuarios imposible (`PUT /users` con `username`/`company_id` ⇒ 422 `extra=forbid`; `alert('[object Object]')`) | NUEVO (B-06; vecino R-117/R-118 cerrados) | **P1** | P-13 | sí | no | sí | sí | no | no | **SÍ** | `specs/R-195/` | sí |
| G-07 | **R-196** Maestros estructurales no creables/editables por UI (`{}` ⇒ 422; `capacity:''`; sin reactivación) | NUEVO (B-08/B-14/B-36) | **P1** | P-12 | sí | no | sí | sí | no | no | **SÍ** | `specs/R-196/` | sí |
| G-08 | **R-197** Centro de revisión: pestañas/filtros muertos, `in_review` huérfano, historial vacío, respuesta de aprobación descartada | NUEVO (B-13, C-2, C-13, C-15, A-P5) | P2 | P-07 | sí | parcial | sí | sí | no | no | **SÍ** (flujo P-07 operativo) | `specs/R-197/` | sí |
| G-09 | **R-198** Evidencias: el detalle las descarta al recargar; adjuntar en cualquier estado; sin auditoría de subida/borrado | NUEVO (C-1, C-36, GAP-12/E-15) | P2 | P-09 | sí | sí | sí | sí | no | sí | **SÍ** (trazabilidad) | `specs/R-198/` | sí |
| G-10 | **R-199** Fabricación de autoridad global desde un rol de inquilino (`("*", all)` en rol de empresa ⇒ `is_super_admin` ⇒ `switch-company`) | NUEVO (GAP-01; R-117 sólo cubrió la plantilla global) | **P1** | P-13 | no | sí | no | no | sí | no | **SÍ** | `specs/R-199/` | no |
| G-11 | **R-200** Refresh token (7 d) aceptado como access token | NUEVO (GAP-03; vecino GA-REM-003 abierto) | P2 | transversal | parcial | sí | no | no | sí | no | **SÍ** (sesión) | `specs/R-200/` | no |
| G-12 | **R-201** Autoridad global sin contexto lee/reintenta SAP de todas las empresas (fail-open) | NUEVO (GAP-02; vecino R-112/OD-12) | P2 | P-08 | no | sí | no | no | sí | no | **SÍ** (fase SAP) | `specs/R-201/` | no |
| G-13 | **R-202** Restablecimiento de contraseña sólo `is_super_admin` y sin contexto de empresa | NUEVO (GAP-04) | P2 | P-13 | no | sí | no | no | sí | no | NO (superficie de control acotada; corregir antes de delegar `users:update`) | `specs/R-202/` (compacto) | no |
| G-14 | **R-203** Lote con `house_id`/`genetic_line_id`/`weight_curve_id` sin verificar pertenencia | NUEVO (GAP-06; clase R-42/R-59/R-180) | P2 | P-11, lotes | no | sí | no | no | sí | sí | **SÍ** (cruce de inquilino) | `specs/R-203/` | no |
| G-15 | **R-204** Lecturas agregadas sin predicado de unidad (`/reports/kpis/hatchery`, `/reports/kpis`, `dashboard.active_alerts`) | NUEVO (GAP-07/GAP-08/E-22/E-23) | P2 | P-15, panel | no | sí | no | no | sí | no | **SÍ** (fuga BU) | `specs/R-204/` | no |
| G-16 | **R-205** Recepción de reproductoras: bloque de cuadre BR-20 sólo con `stage` del asistente (paso 1), inalcanzable por la navegación del producto (`?type=`) ⇒ 400 BR-20 | NUEVO (GA-REM-021-B B01 certificada sólo por API) | **P1** | P-03 | sí | no | sí | sí | no | no | **SÍ** | `specs/R-205/` | sí |
| G-17 | **R-206** Vacíos del asistente: fecha opcional `''` ⇒ 400; numéricos de `extra_data` como cadenas; `''` persistido | NUEVO (B-11/B-26/B-37; familia R-189) | P2 | P-01 (y demás formularios) | sí | no | sí | sí | no | no | NO (existe rodeo: rellenar el campo) | `specs/R-206/` (compacto) | sí |
| G-18 | **P1-12 (REAPERTURA)** Auditoría duplicada (listener `after_flush` + helper ⇒ 2 filas por acción en runtime) e incompleta (cierre/activación/fases de lote, usuarios, evidencias, exportaciones) | EXISTENTE (P1-12 → GA-REM-003 AC06 / GA-REM-019 / GA-REM-032) | P2 | P-09 | no | sí | no | no | no | sí | **SÍ** (integridad de la traza) | `specs/P1-12-REOPEN/` | no |
| G-19 | **R-207** Reverso interno (OD-19) sin superficie de usuario (flujo sólo backend) | NUEVO (GA-REM-041 «sin frontend»; §10 del encargo) | P2 | P-07, OD-19 | sí | no | sí | sí | no | no | **SÍ** (§10: ningún flujo requerido sólo backend) | `specs/R-207/` | sí |
| G-20 | **R-208** `batch-approve`/`batch-reject` exigen `review:review` (no `approvals:*`) | NUEVO (E-09) | P2 | P-07 | sí | sí | sí | no | sí | no | **SÍ** (control interno) | `specs/R-208/` | no |
| G-21 | **R-209** OC SAP enviada por `id` interno en salida de aves y alimento (`sap_document_ref`/`sap_order_id`) | NUEVO (B-09/B-10; familia R-189 F-01 §2) | P2 | P-06, P-08 | sí | no | sí | no | no | sí | **SÍ** (dato fuente SAP) | `specs/R-209/` | no |
| G-22 | **R-210** Peso capturado como kg con curva y KPI en gramos | NUEVO (B-12) | P2 | P-03, P-06, P-15 | sí | no | sí | sí | no | sí | **SÍ** (integridad de pesos/curva) | `specs/R-210/` | sí |
| G-23 | **R-211** BR-17 valida Σ filas contra un solo galpón mientras la UI distribuye por galpón | NUEVO (B-16/E-04; vecino R-176) | P2 | P-03, P-06 recepción | sí | sí | sí | no | no | sí | **SÍ** (recepción multi-galpón) | `specs/R-211/` | sí |
| G-24 | **R-212** UI no consciente del permiso: home sin `dashboard:read`, KPI 403 en detalle de lote, `/users` 403 en revisión, denegación mostrada como «vacío/no encontrado» | NUEVO (N-1/N-3 del backlog; familia R-119/R-120; C-19/C-20) | P3 | transversal | sí | no | sí | sí | no | no | NO | `specs/R-212/` (compacto) | no |
| G-25 | **R-213** `/me` y `/users` responden 500 si el correo persistido no supera `EmailStr` | NUEVO (sonda `me500_probe.log`) | P3 | P-13 | no | sí | no | no | no | no | NO | `specs/R-213/` (compacto) | no |
| G-26 | **R-214** KPI: doble conteo de huevos clasificados y agregados sin filtro de estado | EXISTENTE (familia R-131/R-134/R-141, Wave C pausada; E-05/E-24) | P2 | P-15 | no | sí | no | no | no | sí | NO (Wave C; no es dato fuente SAP) | registro en backlog (sin paquete nuevo; actualiza R-141) | no |
| G-27 | **R-215** Errores estructurados (422) renderizados como hijos de React fuera del asistente + sin `ErrorBoundary` | NUEVO (B-15, C-6, C-7; R-189 sólo cubrió `OperationFormPage`) | P2 | P-12, P-13, lotes, trazabilidad, perfil | sí | no | no | sí | no | no | **SÍ** (pantalla en blanco) | `specs/R-215/` | sí |
| G-28 | **R-216** Panel: `lots_by_type` con claves `BirdTypeEnum.*` ⇒ tarjetas en 0 | NUEVO (C-9) | P2 | panel | no | sí | sí | sí | no | no | NO | `specs/R-216/` (compacto) | no |
| G-29 | **R-217** `SapManagerPage` con estados inexistentes, sin refetch ni reintento | NUEVO (C-10/C-22/C-24) | P2 | P-08 | sí | no | sí | sí | no | no | NO ahora · SÍ para la fase SAP | `specs/R-217/` (compacto) | no |
| G-30 | **R-218** Vista semanal y gráficos planos (lista sin sublistas) | NUEVO (C-4) | P2 | P-15 | sí | sí | sí | sí | no | no | NO | `specs/R-218/` (compacto) | no |
| G-31 | **R-219** `AuditPage` con campos inexistentes (`user_name/old_value/new_value`) | NUEVO (C-11) | P2 | P-09 | sí | no | sí | sí | no | no | NO | `specs/R-219/` (compacto) | no |
| G-32 | **R-220** Residuales de contrato/UX P3 (fechas sin zona, lote codificado en reportes, `Lote #` sin lote, badges sin `reversed`, `reason` no mostrado, doble clic, `alert/confirm`, límite 100 sin paginar, etc.) | NUEVO (C-17/18/25–37, B-17…B-41, F-*) | P3 | transversal | sí | no | parcial | sí | no | no | NO | `specs/R-220/` (compacto, itemizado) | no |
| G-33 | **GA-GOV-03** Gobernanza de pruebas y certificaciones: 25 fallos backend en HEAD, 12 fallos Playwright, CI sólo en PR, certificaciones anteriores a 32 commits de producto | NUEVO (vecinos GA-REM-013/014; P1-7) | **P1** (gobernanza) | transversal | — | — | — | — | — | — | **SÍ** (sin suite verde no hay certificación válida) | `specs/GA-GOV-03/` | no |
| G-34 | **R-221** Eventos sin lote (`farm_inspection`, `hatchery_inspection`) no derivan unidad del tipo ⇒ la guarda OD-16/concesión sólo exige «alguna unidad»: inspección de incubadora registrada con la unidad `hatchery` **apagada** (autoridad global) o sin concesión `hatchery` (actor de empresa) | NUEVO (vecino R-153 C2b, que sólo derivó `grandparent`; OD-16.e) | P2 | P-05, P-01/P-03/P-06 inspecciones | no | sí | no | no | sí | sí | **SÍ** (escritura productiva fuera de la unidad habilitada/concedida) | `specs/R-221/` | no |

Totales: **34 brechas** · P1 **9** (R-190, R-191, R-192, R-194, R-195, R-196, R-199, R-205, GA-GOV-03) · P2 **22** (incluye P1-12 reabierta y R-214) · P3 **3** (R-212, R-213, R-220) · bloqueantes **24** (9 P1 + 15 P2; +1 condicional de fase SAP: R-217) · con paquete completo **24** · con paquete compacto **9** · sólo backlog **1** (R-214).

> **Corrección documental D-02 (aplicada):** recuento anterior («P2 21 · bloqueantes 25 · completo 25 · compacto 8») corregido por enumeración directa de la tabla: P2 = 22 (G-04, 08, 09, 11–15, 17–23, 26–31, 34) · bloqueantes = 24 SÍ + R-217 condicional · paquetes completos = 24 · compactos = 9. Todos los paquetes referenciados existen ya en `specs/` (incluidos R-194…R-221, P1-12-REOPEN y los compactos).

Confirmaciones de la pasa 2 local (`evidence/ui-e2e-local-pass2.json`, 2026-09-13): **R-192** `H8b`: lote gemelo de control cierra 200; lote con reverso efectivo ⇒ `close` 400 «2 registro(s) sin aprobar (2 en «reversed»)» y `cancel` 400 ⇒ nunca cierra. **R-205** `BR2`: por el hub `received_total` ausente (0 campos) ⇒ 400 BR-20; por URL directa `/operations/new` (paso 1 → paso 2) el campo existe (1) ⇒ 201; enlaces del producto a `/operations/new` sin `?type=`: 0. **R-194** `HAT2`: recepción de huevos por UI ⇒ 422 `arrival_date`; sin almacenamiento ⇒ 400 BR-08 «requiere una granja asignada»; sonda API con granja/galpón y sin almacenamiento ⇒ 201 pero carga posterior ⇒ 400 BR-03 «0 disponibles» (sin `egg_movements` fértiles); `incubation_load` y `birth_registration` por UI ⇒ sin petición (bloqueo silencioso, con y sin dosis); sonda API del nacimiento ⇒ 201; `chick_dispatch` por UI ⇒ 400 BR-08 granja. **R-221** `OD16b-global-actor-bu-off-api`: `POST /operations {event_type:'hatchery_inspection'}` sin lote ⇒ **201** con `hatchery` apagada (disable por UI 200 inmediatamente antes; enable 200 después). **OD-23 por UI** PASS (apagar marca `revoked_at`; reencender no revive; concesión nueva restaura). **P1-12** `H8b-timeline-*`: 13 filas para 6 acciones en el original (`created ×2`, `review_started ×3`, `approved ×2`, más una fila `corrected` espuria escrita por `complete_review` al aprobar — `review/service.py:363` usa `ActionType.CORRECTED` en ambas ramas). **R-206** `BR2-egg_dispatch-payload`: `extra_data.transport_*` viajan como `''`.

## 2 · Fichas de brecha (insumo de los paquetes)

### R-190 · Eventos de ubicación por UI sobre lotes sin galpón (P1 · BLOQUEA)
- **Clase**: REQUEST_CONTRACT / BROKEN_FLOW. **Familia** R-189 (F-01e resolvió sólo `bird_reception`).
- **Evidencia runtime** (2026-09-13, lote autocreado `L-GP-2026-12` id 66, `house_id=null`, `farm_id=1`): `bird_distribution` → 400 «requiere un galpón asignado» (payload con `farm_id:1`, sin `house_id`); `bird_exit` → 400 ídem; `egg_collection` → 400 ídem; `farm_inspection` → 400 «requiere una granja asignada» (payload con `house_id:1` sin `farm_id`; el selector de granja quedó vacío y la UI no lo exige antes de enviar). Fuente: `evidence/runtime-gp-e2e.json` (`R-08-*`, `R-10-recoleccion`). Local pasa 2: `GP2-14/15/17` (misma clase).
- **Causa raíz**: `OperationFormPage.tsx:387-394` deriva `house_id` sólo para `bird_reception` (`derivedHouseId`); para el resto usa `lot.house_id`, nulo en lotes OD-25 (`crear_lote_de_importacion_si_procede` no fija galpón) y en lotes creados sin galpón (`LotFormPage` galpón opcional). El backend (`validators.py:validate_farm_house`, `location_events` = 10 tipos) exige `farm_id`+`house_id` del cuerpo. Sin selector de galpón del evento en `bird_distribution`/`bird_exit`/`egg_collection`/`egg_dispatch`/`bird_transfer`/`transport_inspection` (B-04).
- **Comportamiento esperado**: para todo evento de `location_events` el asistente resuelve `house_id`/`farm_id` = galpón del lote si existe; si no, exige un selector «Galpón del evento» (o toma la primera fila de distribución, patrón F-01e), y `farm_id` = granja del galpón/lote; el envío se bloquea con mensaje si faltan (sin depender del 400). Backend sin cambio de regla (BR-08 intacta). Alternativa B (decisión): persistir `house_id` en el lote al aprobar la primera recepción (F-01e) — requiere decisión del propietario (clarificación).
- **AC semilla**: AC01 lote sin galpón + `bird_distribution` por UI ⇒ 201 con `house_id` del galpón elegido; AC02 `bird_exit` ⇒ 201; AC03 `egg_collection` ⇒ 201; AC04 `farm_inspection` sin granja ⇒ bloqueo en cliente con mensaje (no 400); AC05 lote con galpón ⇒ sin cambio (regresión F-01e 4/4, R-189 29/29); AC06 BR-08 sigue devolviendo 400 por API; AC07 móvil/EN; AC08 sin migración ni permiso nuevo.
- **Estado (2026-09-14)**: **CERRADA — `CLOSED_TECHNICALLY`** · C1 `a79fe0c` (RED 26F/2P FE + control BE BR-08 4/4) · C2 `d1c16a7` (helper `resolverUbicacionDelEvento` + selector «Galpón del evento» + guardas + i18n; GREEN 32/32; tsc 0; FE 360/360; build OK) · C2s `801d18c` (S1 2F/S2 9F/S3 2F/S4 2F) · C3 runtime (E2E-R190-01…08) planificada con la ventana de deploy/credenciales (misma clase G-06) · certificación `GA_CLAUDE_R190_RUNTIME_CERTIFICATION.md`.

### R-191 · Transición de fase por UI (P1 · BLOQUEA)
- **Evidencia runtime**: `POST /lots/66/phases` con `{phase_code:'production', start_date, start_population_male:0, start_population_female:0}` ⇒ 422 `lot_id`/`phase_id` Field required; sin toast (`toasts: []`); botón sigue visible. Fuente `runtime-gp-e2e.json R-09-transicion-ui`; local `GP2-16`.
- **Causa raíz**: `LotDetailPage.tsx:125-134` envía `phase_code` y omite `lot_id`; `lots/schemas.py:98-109` (`LotPhaseCreate`) exige `lot_id` + `phase_id` (id de `productive_phases`); el `catch` sólo hace `console.error` (`:135`, B-41); no se cargan las fases productivas (`GET /masters/productive-phases`) para resolver el id.
- **Esperado**: el detalle carga `productive-phases`, resuelve `phase_id` por código, envía `{lot_id, phase_id, start_date, start_population_*}` con poblaciones reales (saldo por sexo) o sin ellas si el backend las deriva; muestra éxito/error con toast; refresca `phases` y estado; en móvil también. Contrato de respuesta: `LotPhaseRead`.
- **AC semilla**: AC01 lote cría→producción por UI ⇒ 201 y fase activa visible; AC02 error 400/422 ⇒ toast con `detail` legible; AC03 sin `lot_id` en cuerpo ⇒ imposible por construcción; AC04 regresión `tests/test_lots_bu_enforcement.py::test_l08_phases_*`; AC05 API sin cambio de contrato.

### R-192 · Cierre de lote tras reverso efectivo (P1 · BLOQUEA)
- **Evidencia código**: `validators.py:400-406,436-438` (`validate_lot_records_approved`: cuenta como «sin aprobar» todo estado ∉ `ESTADOS_APROBADOS ∪ {CANCELLED}`; `REVERSED` ∉ ambos) · `operations/service.py:76-78` (`NO_CANCELABLES` incluye `REVERSED`) ⇒ un lote con un reverso efectivo (original + contrapartida en `REVERSED`) nunca cierra. `lots/service.py:469-517` (`close_lot`) suma mortalidad/alimento/huevos sin filtro de estado (incluye `CANCELLED`) y `approved_events` excluye `SAP_ERROR`/`REVERSED` (E-03). Evidencia runtime local: `ui-e2e-local-pass1.json H8-*` (reverso efectuado por API: contrapartida `pending_review` → aprobada → ambos `reversed`; `cancel` ⇒ 400) y `ui-e2e-local-pass2.json H8b-*` (lotes gemelos: control cierra / con reverso …).
- **Esperado**: R7 trata `REVERSED` como estado terminal aceptable (par original+contrapartida), el resumen de cierre excluye `CANCELLED`/`REVERSED` netos, y la UI de cierre muestra el motivo del rechazo (hoy `console.error`, C-8). Tests R7 con `REVERSED`.
- **AC semilla**: AC01 lote con reverso efectivo y demás registros aprobados ⇒ `close` 200; AC02 resumen excluye anulados; AC03 lote con contrapartida pendiente ⇒ 400 con motivo; AC04 UI muestra el motivo del 400; AC05 regresión `test_lot_close_approval.py`.

### R-193 · BR-18 tras reverso (P2 · BLOQUEA integridad)
- `validators.py:769-783` (`validate_oc_limit`) suma cantidades de eventos con la misma `sap_document_ref` sin excluir `REVERSED` ni contrapartidas ⇒ acumulado 2n; entregas parciales posteriores rechazadas. Esperado: aplicar `_suma_neta` (`validators.py:21-46`) o excluir `REVERSED` + contrapartidas. AC: reverso de recepción de n ⇒ nueva recepción de n contra la misma OC ⇒ 201.

### R-194 · Cadena Incubadora por UI (P1 · BLOQUEA)
- **Evidencia**: `B_form_contracts.md` B-01/B-02/B-03/B-05/B-22/B-33 con `OFP:274-278,386,438` (`farm_id` forzado a `undefined` en etapa incubadora ⇒ BR-08 `egg_reception_hatchery`/`chick_dispatch`), `OFP:170-177,1218-1236` (`egg_storage_records` sin `arrival_date` ⇒ 422; cantidad recibida en `egg_storage_records` mientras BR-03 lee `egg_movements[fertile]` ⇒ carga 400 BR-03), `OFP:1651` (`dosage_per_bird` sin `valueAsNumber` ⇒ `''` ⇒ zod falla sin render ⇒ submit silencioso en `birth_registration`), `OFP:139-149,2044-2056` (`hatchery_id` nunca en filas). Local pasa 2 `HAT2-*` (resultado en `ui-e2e-local-pass2.json`). Suite `e2e/proceso-p05-incubacion.spec.ts` (API) falla en HEAD por BR-21 (`chicks_healthy/weak` obligatorios desde `c653ff8`) y BR-04 en cascada ⇒ certificación P-05 obsoleta.
- **Esperado**: el asistente en etapa incubadora envía `farm_id`/`house_id` de la planta o del lote (o BR-08 exime a la etapa incubadora por decisión); la recepción de huevos escribe `egg_movements[{egg_type:'fertile', quantity}]` además del almacenamiento; `arrival_date` capturada o derivada de `event_date`; `dosage_per_bird` con `valueAsNumber` y error visible; `hatchery_id` en las filas; nacimiento con sanos/débiles (ya en UI). Cadena P-05 completa por UI: inspección → recepción → clasificación → carga → ovoscopía → transferencia → nacimiento → despacho.

### R-195 · Edición de usuarios (P1 · BLOQUEA)
- `UsersPage.tsx:76-79,85` envía `username`, `company_id` en `PUT /users/{id}`; `auth/schemas.py` `UserUpdate` `extra="forbid"` ⇒ 422; UI `alert('[object Object]')`. Local pasa 1 `H3-users-edit-boton: no visible` (control de edición no localizado por el arnés en `/users`). Esperado: `PUT` con el subconjunto permitido (`first_name`, `last_name`, `email`, `phone`, `role_id`, `is_active`, `view_type`, `area_id`), errores legibles (`getErrorMessage`), sin `alert`. AC: editar rol y nombre ⇒ 200 y lista refrescada; 422 ⇒ mensaje humano.

### R-196 · Maestros estructurales por UI (P1 · BLOQUEA)
- Local: `MAS-hatchery-create-ui` ⇒ `POST /masters/hatcheries {}` ⇒ 422 (`company_id`, `name`) + `pageerror` React #31 (`Objects are not valid as a React child (found: object with keys {type, loc, msg, input})`); `H4-masters-house-create-ui` ⇒ `POST /masters/houses {}` ⇒ 422 (`farm_id`, `name`). Código: `MasterListPage.tsx:80,92-94,99,193-203` (formulario genérico sin campos de padre, `capacity:''`, `detail` crudo, sin `is_active`); `masters/schemas.py` (`Create` exige `company_id`/`farm_id`/`hatchery_id`). Esperado: formularios por entidad con selector de padre (granja→galpón, planta→incubadora/nacedora), `company_id` resuelto en servidor (véase R-50/GAP-05), numéricos opcionales como `null`, reactivación, errores legibles. 20/21 maestros sólo por URL directa (F-*: navegación) se documentan en `GA_CLAUDE_NAVIGATION_ACTION_AUDIT.md` y entran en el alcance de este paquete.

### R-197 · Centro de revisión (P2 · BLOQUEA P-07 operativo)
- Runtime: evento en `in_review` no aparece en pestañas «Pendientes» ni «En revisión» (`R-12-review-tabs-in_review {pending_review:false,in_review:false}`); `GET /review/pending?status=in_review` devuelve sólo `registered|pending_review` (`review/service.py:165-174`); `ReviewCenter.tsx:33-40,88,96` envía `status`/`operator_id` que `review/router.py:23-33` no declara; «Completar/Devolver» nunca visibles en el centro (`:128-129`); historial derivado de `/review/batches` ⇒ vacío para acciones individuales (C-15); respuesta de `approve` descartada ⇒ sin enlace al lote creado (C-13). Local `H2-*` (mismo resultado). Nota de robustez: el rol canónico «TEST Aprobador» de semillas carece de `review:read`; `ReviewDetail` carga el evento por `/operations/{id}` (OK) pero `ReviewCenter`/`ApprovalPanel` responden 403 sin mensaje (véase R-212).
- Esperado: backend acepta `status`/`operator_id` (o el frontend deja de enviarlos y consulta por estado real); `in_review` visible y con acciones; historial por evento (`GET /review/actions?event_id`) o inclusión en el detalle; enlace al lote tras aprobar `grandparent_import`.

### R-198 · Evidencias (P2 · BLOQUEA P-09)
- `operations/router.py:277-292` descarta `evidences`/`egg_storage_records` del detalle tipado; `OperationDetailPage.tsx:66,76-114` lista local ⇒ tras F5 «Sin archivos» aunque `GET /operations/{id}/evidences` las devuelve; adjuntar permitido en cualquier estado (`:297,327`; `service.py:1369`); subida/borrado sin auditoría (`service.py:1363-1414`); borrado físico antes del commit. Esperado: detalle devuelve/relee evidencias; adjuntar sólo en estados editables o con auditoría `UPDATED`; auditoría de subida/borrado; borrado físico tras commit.

### R-199 · Autoridad global fabricada desde rol de inquilino (P1 · BLOQUEA)
- `auth/service.py:585-619,621-658` (`create_role`/`update_role` no validan `permissions`), `:116-142` (`_rol_asignable` rama `:142`), `security.py:119-120` (`is_super_admin` por permiso sin mirar `Role.company_id`), `:507-540` (`switch-company`). Contradice `OD-13.c` y `AC-R06`. Sin test del camino. Esperado: rechazar `("*", scope all)` y módulos fuera de catálogo en roles de inquilino (`PermissionCreate` validado contra catálogo, GAP-16), `_es_autoridad_global` aplicado también a roles de inquilino, `is_super_admin` sólo con `Role.company_id IS NULL`; auditoría del intento. AC: actor con `users:create/update` de empresa A crea rol con comodín ⇒ 422/403; asigna plantilla global ⇒ 403 (ya R-117); ninguno obtiene `switch-company`.
- **Estado (2026-09-13)**: `CLOSED_TECHNICALLY` · **C3 runtime pendiente de G-06** (credenciales privilegiadas de runtime) — C1 `32c9906` · C2 `62cd0e1` · C2s `22c843e` (sensibilidad **6/6**, con refuerzo RED-05 anti-enmascaramiento); suite `1238/0/49`; certificación `GA_CLAUDE_R199_RUNTIME_CERTIFICATION.md`.

### R-200 · Refresh token como access (P2 · BLOQUEA)
- `security.py:59-65,87-93` no comprueban `type == "access"`; `create_refresh_token` `:48-56` (7 d, `localStorage`). Esperado: `get_current_user` rechaza `type != access` (401); `refresh` sólo con `type == refresh` (ya); test de cruce; documentar interacción con GA-REM-003 (logout/rotación, sigue abierta).
- **Estado (2026-09-13)**: **CERRADA — `CLOSED_FUNCTIONALLY_CERTIFIED`** — C1 `9406573` · C2 `bf1746c`; suite `1247/0/49`; sensibilidad M1; runtime **pre/post observado** (`runtime-prefix.json` → 200; `runtime-c3.json` → 401 «no es un token de acceso»); certificación `GA_CLAUDE_R200_RUNTIME_CERTIFICATION.md`. **No cierra `GA-REM-003 AC04`** (logout/rotación — rider siguiente de T2).
- **Actualización (2026-09-13, noche-8)**: `GA-REM-003 AC04` **cerrado** (logout con denylist `jti`; runtime 204 → 401 «Token revocado») — `GA_CLAUDE_GA_REM_003_AC04_CERTIFICATION.md`; la nota anterior queda levantada.

### R-201 · SAP sin contexto fail-open (P2 · BLOQUEA fase SAP)
- `sap/service.py:76-81` `_company_filter` ⇒ `true()` si `company_id is None`; afecta `list_references :128-135`, `consolidate_approved :158-161`, `export_to_sap :239-242`, `retry_failed :404-408` (escritura), `list_sync_jobs/consolidated/errors/payloads`. Esperado: `false()` sin contexto (patrón `_acotar_a_empresa`), `retry_failed` con `_require_company_id`; tests de autoridad global sin contexto.
- **Estado (2026-09-13)**: `CLOSED_TECHNICALLY` · **C3 runtime pendiente de G-06** — C1 `8a442ec` (RED 5F/18P) · C2 `4050eef` (GREEN 32/32; suite conjunta `1278/0/49`; sensibilidad S1 3F/S2 1F); certificación `GA_CLAUDE_R201_RUNTIME_CERTIFICATION.md`.

### R-202 · Restablecimiento de contraseña (P2 · no bloquea) — `auth/service.py:434-455`: objetivo buscado sin empresa; sólo `is_super_admin`; esperado `_usuario_alcanzable(user_id, actor)` + `tiene_permiso(users:update)`; auditoría con empresa efectiva.
- **Estado (2026-09-13)**: `CLOSED_TECHNICALLY` · **C3 runtime pendiente de G-06** — C1 `58b374a` (RED 3F/2P) · C2 `fafd262` (dirigidas 37/37; suite `1252/0/49`; sensibilidad M1/M2); certificación `GA_CLAUDE_R202_RUNTIME_CERTIFICATION.md`.

### R-203 · Referencias del lote sin verificar (P2 · BLOQUEA cruce) — `lots/service.py:362-392,275-307,411-436`; `masters/service.py:187` (`_PADRES_TENANT` sin `house_id`); `lots/schemas.py:14-20,65-67`. Esperado: `verificar_pertenencia(House vía Farm)`, `verificar_catalogo_de_empresa(GeneticLine)`, curva colgando de línea de la empresa; `weight-evaluation` no expone curvas ajenas.
- **Estado (2026-09-13)**: `CLOSED_TECHNICALLY` · **C3 runtime pendiente de G-06** — C1 `fc8f193` (RED 5F/7P; clarificación C-07: contrato 400/`BR-07`) · C2 (implementación de las tres puertas; suite conjunta `1278/0/49`; sensibilidad S1 2F/S2 2F; inventario §12: 0/0/0); certificación `GA_CLAUDE_R203_RUNTIME_CERTIFICATION.md`.

### R-204 · Agregados sin unidad (P2 · BLOQUEA fuga BU) — `reports/service.py:191-207,262-308,415-420` (`_filtro_de_lotes` `:64-70` sin uso); `dashboard/service.py:206-242` (`_get_active_alerts` calcula `_ambito` y no lo aplica). Esperado: `lot_id.in_(lotes_alcanzables)` en los sumatorios y en las alertas; `route_scope` `UNIDAD_UNICA` aplicado o retirado (GAP-17).
- **Estado (2026-09-13)**: **CERRADA — `CLOSED_TECHNICALLY`** · C1 `ca1fa39` (RED 4F/2P) · C2 `c1a21bd` (GREEN 53/53; suite completa **1284/0/49** en 1137.13s `full_suite_c2.log`; sensibilidad S1 3F/S2 1F) · C3 runtime encolada en **G-06** (misma clase R-199/201/202/203) · certificación `GA_CLAUDE_R204_RUNTIME_CERTIFICATION.md`.

### R-205 · Cuadre BR-20 inalcanzable por navegación (P1 · BLOQUEA)
- Código: `OperationFormPage.tsx:316` (`stage` estado local), `:319` (`goToStep2` único setter, paso 1 del asistente), `:1901-1915` (con `prefillType` el asistente arranca en paso 3 con `stage=null`), bloque de cuadre `stage === 'breeder_rearing'` (`:667-680`, campos `received_total`/`dead_on_arrival`/`rejected_on_arrival`); `validators.py:536-565` (BR-20: para `breeder` los tres campos son obligatorios ⇒ 400 «falta: received_total, dead_on_arrival, rejected_on_arrival»). Ninguna ruta/enlace del producto lleva a `/operations/new` sin `?type=` (grep: sólo `App.tsx:260`); el hub (`GA-FE-08`), el detalle de lote y los deep links usan `?type=`. Evidencia local: pasa 1 `BR-EXCEPCION` (campo `received_total` inexistente vía hub); pasa 2 `BR2-hub-campo-cuadre-visible`/`BR2-01-hub-resultado` (400 BR-20) vs `BR2-asistente-*` (URL directa: campos presentes). Suites `e2e/proceso-p03-reproductoras-cria.spec.ts`, `p04`, `p11` (API) fallan en HEAD por BR-20 ⇒ certificaciones P-03/P-04/P-11 obsoletas.
- Esperado: `stage` derivado del contexto (`?stage=` del tile, `lot.bird_type`+fase, o tipo de evento) y del lote seleccionado (`bird_type=breeder` ⇒ cuadre visible y obligatorio en cliente); mismo formulario por cualquier ruta; tests de paridad hub/asistente/deep link.
- **Estado (2026-09-14)**: **CERRADA — `CLOSED_TECHNICALLY`** · C1 `fe3bd3d` (RED 12F/2P — cuadre oculto en deep link; control BR-20 10/10) · C2 `72aa7f4` (helper `resolverStageDelAsistente` + visibilidad por lote + guarda cliente `cuadreRequired`; GREEN 29/29; tsc 0; FE 360/360; build OK) · C2s `dbc782d` (S1 4F/S2 3F/S3 1F) · C3 runtime (E2E-R205-01…06 + recertificación R-189) planificada · certificación `GA_CLAUDE_R205_RUNTIME_CERTIFICATION.md`.

### R-206 · Vacíos del asistente (P2 · no bloquea) — `OFP:1782` (`quarantine_end_date` `type=date` sin transformación ⇒ `''`); `PlanDeImportacion` rechaza `''` (400 BR-22); local `GP2-01a-contrato-fecha-vacia` (`quarantine_end_date_enviado: ""`). Extensión a B-26 (numéricos de `extra_data` como cadenas) y B-37 (`''` en `sap_document_ref`/`vaccination_route`). Esperado: normalizador `''`→`undefined` en el serializador (ampliar `limpiarNumerosNoFinitos`), fechas opcionales omitidas, backend tolerante (`''`→`None` en `PlanDeImportacion` para campos opcionales).

### P1-12 (REAPERTURA) · Auditoría duplicada e incompleta (P2 · BLOQUEA integridad de la traza)
- Local (uvicorn con `lifespan`, listener registrado): `GET /audit/timeline/operational_event/1` ⇒ `created:registered ×2`; evento 15: `created ×2`, `review_started ×2`, `approved ×2` (`ui-e2e-local-pass1.json H6-audit-timeline`, `H8-audit-timeline-original`). Código: `audit/listeners.py:71-113` + `audit/helpers.py` invocados ambos (`operations/service.py:308`, `review/service.py`); tests con `ASGITransport` sin `lifespan` no registran el listener ⇒ afirman `== 1` y no detectan el duplicado. Faltantes: `close_lot`/`activate_manual`/`add_phase` (`lots/service.py:438-549,555-659,685-697`), usuarios (`auth/service.py:337-421,491-506`), evidencias, curvas, exportaciones (E-10/E-13/E-12/E-15/E-16). Esperado: un único productor (retirar listener o helpers), tests bajo `lifespan`, cobertura de los productores faltantes; GA-REM-032 AC01/AC04 reconciliados.

### R-207 · Reverso interno sin UI (P2 · BLOQUEA §10) — `reversals` sin consumidor en `frontend/src` (grep 0); local `H8-*` sólo por API. Esperado: acción «Solicitar reverso» en el detalle de evento aprobado (permiso `reversals:create`), bandeja de contrapartidas en revisión, estado `reversed` en badges (`statusColors.ts`, C-27), enlace original↔contrapartida, motivo obligatorio (≥5).

### R-208 · Batch approve con `review:review` (P2 · BLOQUEA control) — `review/router.py:123-160` vs unitarias `approvals:approve/reject`; `ApprovalPanel.tsx:100,119,173`. Esperado: permisos `approvals:*` en rutas por lote y segregación BR-14 aplicada por evento.
- **Estado (2026-09-13)**: **CERRADA — `CLOSED_FUNCTIONALLY_CERTIFIED`** — C1 `11b8f89` (RED 2F/2P) · C2 `331ad83` (GREEN BE 17/17; FE 5/5 + suite FE 316/316; suite BE `1256/0/49`; sensibilidad S1); runtime no destructivo (`runtime-c3.json`: operador 403 `approvals:*`; aprobador 404 tras la puerta; deploy Docker #121); certificación `GA_CLAUDE_R208_RUNTIME_CERTIFICATION.md`.

### R-209 · OC por id (P2 · BLOQUEA dato fuente SAP) — `OFP:917-927` (`sap_document_ref = String(id)` en bloque de salida) vs `OFP:2008-2035` (bloque compartido, código canónico); `OFP:1033-1040` (`feed_movements.sap_order_id ← String(id)`); `V:471-474,758`. Esperado: siempre el código canónico (`doc_number||ref_id||sap_code`), como R-189 §2.

### R-210 · Unidad de peso (P2 · BLOQUEA integridad) — `OFP:470` y afines: etiqueta «(kg)» step 0.001 con curva/KPI en gramos (`masters/curves.py`, `reports/service.py`). Esperado: unidad única (g) en captura, etiqueta e i18n; conversión explícita si se captura kg; tests de evaluación de curva.

### R-211 · BR-17 vs distribución por galpón (P2 · BLOQUEA) — `OFP:725-772,392-393` (filas por galpón, `house_id` único) vs `validators.py:712-725` (capacidad por evento contra un solo galpón, no acumulada, E-04). Esperado: validar capacidad por galpón de cada fila (y acumulada por saldo del galpón si se decide, RR-02); decisión del propietario sobre capacidad acumulada.

### R-212 · UI y permisos (P3) — runtime `httpErrores`: operador `GET /reports/kpis`, `/reports/kpi/ipe/66`, `/reports/kpi/weight-uniformity/66` 403 ×12 c/u (detalle de lote), `/dashboard/admin` 403 (home), aprobador `/users` 403 ×7 (centro de revisión); `C-19` (denegación mostrada como vacío) y `C-20` (carga eterna). Esperado: no pedir lo que el rol no puede leer; mostrar «sin permiso» distinto de «vacío»; home útil sin `dashboard:read` (N-1).

### R-213 · `/me` 500 por `EmailStr` (P3) — `auth/service.py:329` `UserRead.model_validate(user)` lanza `ValidationError` (500) para correos con TLD reservado (`.test`) u otros que pydantic rechaza (`me500_probe.log`). Esperado: lectura tolerante (`email: str` en lectura o validación relajada) y validación estricta sólo en alta/edición.

### R-215 · Errores estructurados fuera del asistente + ErrorBoundary (P2 · BLOQUEA) — `MasterListPage.tsx:99,203`, `LotFormPage.tsx:137`, `TraceabilityTree.tsx:95,114,347,389`, `ProfilePage.tsx:38,70`, `UsersPage.tsx:85` renderizan `detail` crudo; sin `ErrorBoundary` (`main.tsx`, `App.tsx`). Local: React #31 ×2. Esperado: `getErrorMessage` en todos los consumidores, `ErrorBoundary` global con recuperación, test de render de 422.

### R-216 · `lots_by_type` con claves de enum (P2) — `dashboard/service.py:171` `str(row.bird_type)` ⇒ `'BirdTypeEnum.BROILER'`; local `H5-dashboard-admin` (`BirdTypeEnum.BREEDER`…); `DashboardPage.tsx:255,513` busca `'broiler'`. Esperado: `.value`; test de contrato.
- **Estado (2026-09-14)**: **CERRADA — `CLOSED_TECHNICALLY`** · C1 `074ee0a` (RED 1F/1P — contrato `test_r216_lots_by_type_contract.py`) · C2 `1ba1b11` (`.value`; GREEN dirigido 49/49; sensibilidad S1 2F/S2 2F `.name`; `test_kpi_scope` endurecido a claves exactas) · C3 runtime piggyback de **G-06** (verificación visual de tarjetas) · certificación `GA_CLAUDE_R216_RUNTIME_CERTIFICATION.md`.

### R-217 · `SapManagerPage` (P2, fase SAP) — `SapManagerPage.tsx:70-72,191-291` estados `pending/draft/sent/error` vs backend `prepared/sending/confirmed/failed/retrying`; sin refetch tras consolidar/exportar; sin UI de `/sap/retry`. Esperado: alinear estados, refetch, reintento con permiso `sap:send_sap`.

### R-218 · Vista semanal/gráficos (P2) — `/operations` lista sin sublistas (`operations/schemas.py:256-266`); `LotDetailPage.tsx:249-268`, `ReportsPage.tsx:25-31`. Esperado: endpoint agregado semanal o detalle con sublistas.

### R-219 · `AuditPage` (P2) — `AuditPage.tsx:90,94-95` vs `audit/schemas.py:8-30`. Esperado: mapear `user_id`→nombre, `previous_values/new_values`, `previous_state/new_state`.

### R-221 · Inspecciones sin lote sin unidad derivada (P2 · BLOQUEA)
- **Código**: `operations/service.py:61-68` (`LOT_OPTIONAL_EVENTS` = `FARM_INSPECTION`, `HATCHERY_INSPECTION`, `GRANDPARENT_IMPORT`), `:242-246` (`unidad_directa` sólo para `grandparent_import`), `:185-197` (sin lote ni `business_unit_id` ⇒ `unidad=None`), `business_units/service.py:281-290` (`unidad=None` ⇒ global: basta «alguna habilitada»; empresa: basta «alguna efectiva»). El evento nace con `business_unit_id=null` (pendiente de clasificar) aunque `hatchery_inspection` es de cadena inequívoca (`classification.py` «tipo inequívoco» sólo para lectura).
- **Evidencia**: local pasa 2 `OD16b-global-actor-bu-off-api` ⇒ 201 con `hatchery` apagada; por construcción, un actor de empresa con concesión sólo `broiler` registra `hatchery_inspection` (`efectivas` no vacío). Contradice OD-16.e («apagado = inaccesible también para la autoridad global») y OD-09 (concesión por unidad).
- **Esperado**: derivar la unidad por tipo cuando es inequívoca (`hatchery_inspection` ⇒ `hatchery`, patrón R-153 C2b), y para `farm_inspection` exigir clasificación previa o derivarla de la granja/galpón declarados (decisión del propietario: ¿qué unidad tiene una inspección de granja sin lote?); tests W-xx con unidad apagada/no concedida para eventos sin lote.
- **AC semilla**: AC01 global situada con `hatchery` OFF ⇒ `hatchery_inspection` 403; AC02 operador sólo `broiler` ⇒ 400 BR-07/403; AC03 operador con `hatchery` ⇒ 201 y `business_unit_id` fijado; AC04 `farm_inspection` sin lote ⇒ regla decidida (clasificación o derivación); AC05 regresión R-153 (import sin lote intacto).
- **Estado (2026-09-14)**: **PARCIAL (T3)** · paquete `specs/R-221/` completo · C1 `8126f97` RED 3F/2P (`test_r221_unit_derivation_no_lot.py`: AC-01/02/03 rojos — 201 con `hatchery` OFF, 201 sin concesión, `business_unit_id` NULL; AC-04 statu quo fijado; AC-05 control con lote) · C2 `b056ef1` (lista `UNIDAD_POR_TIPO_INEQUIVOCO`; guarda estricta `OD-16.e`/`OD-09`; atribución al nacer `business_unit_id` para `hatchery_inspection` sin lote; GREEN 134/134 con regresión BU/clasificación/R-153; S1 1F/S2 2F) · **AC-04 espera AOD-13** (C-02, opciones A/B/C en cola; T-05 sin ejecutar).

### R-220 · Residuales P3 (itemizado en el paquete compacto): C-17 fechas sin zona; C-18 lote 2 codificado; C-25/26/27/28/29/32/33/34/35/36/37; B-17…B-41 no cubiertos arriba; F-* (móvil sin logout/perfil; evidencias ocultas en táctil; usuario web <1024 px sin navegación; enumerados crudos EN); `/my-pending` huérfana; `/poultry` huérfana; `/reports/lot/:id` casi huérfana.

### GA-GOV-03 · Gobernanza de pruebas y certificaciones (P1 gobernanza · BLOQUEA)
- **Backend** (`backend/scripts/run_tests.sh`, PG aislado): 1201 passed · 25 failed · 49 skipped (1210 s); reproducido en aislamiento 189/25 (`backend_targeted_failing.log`). Clasificación: 17 × TEST_DEFECT (obsoletos vs OD-16 `9ffc5ec`: esperan 403/visibilidad de la autoridad global sobre unidad apagada; hoy 404 fail-closed): `test_lots_bu_enforcement` (l08 ×4, l11, e06 = 6), `test_operations_bu_enforcement` (w13 ×4, a08, a13 = 6), `test_review_bu_enforcement::test_165_01`, `test_state_continuity::test_s07`, `test_internal_reversal::test_s03_s04`, `test_od14_productive_surfaces::test_s02`, `test_review_decision_concurrency::test_r166_12` (= 17); 5 × TEST_DEFECT (`test_r188_bu_lifecycle`: fixture con correo `@e.test` ⇒ `/me` 500 por `EmailStr`; la suite R-188 **nunca ha pasado** en ningún entorno: local «10 skipped», CI sólo en PR); 3 × TEST_DEFECT (guardas obsoletas: cabeza Alembic `x4y5z6a7b8c9`→`y5z6a7b8c9d0` en `test_company_catalog::test_t10`, `test_population_invariant::test_ac14`; fechas literales en comentarios `test_time_determinism::test_t028_04`). 0 × APP_DEFECT directo (R-213 es hallazgo colateral). Último registro documentado de suite completa: «1139 passed · 49 skipped · 0 failed» (2026-09-10, tranche 14); desde entonces 32 commits de producto con «suite PG declarada a CI».
- **CI**: `.github/workflows/backend-ci.yml` sólo `on: pull_request` (comentario explícito «NUNCA en push directo a main»); toda la historia son pushes directos a `main` ⇒ ninguna suite «declarada a CI» se ha ejecutado. Repositorio privado (API de Actions 404 anónima): no verificable, y por construcción no ejecutable.
- **Playwright** (`bash scripts_e2e.sh`, 129 pruebas): 117 passed · 12 failed. Clasificación: 11 × TEST_DEFECT obsoletas frente a reglas posteriores (BR-20 `p03`/`p04`/`p11`; BR-21 `p05`/`p10` ×5 + BR-04 en cascada; BR-03 fixture `p15`; R-118 fixture `p11` aislamiento) y 1 × TEST_DEFECT (locator `getByText(/135/)` ambiguo con «Evento #135», `p03-curvas-ui`) ⇒ **las certificaciones P-03, P-04, P-05, P-10, P-11, P-15 no son reproducibles en HEAD**.
- **Frontend**: Vitest 314/314 · tsc · build OK. **Runtime**: paridad bundle/backend PASS.
- Esperado (paquete): tranche de higiene de pruebas (actualizar los 25+12 casos a las reglas vigentes o documentar su invalidez), CI en `push` a `main` con PG, regla «no GREEN por declaración», recertificación de procesos con artefactos de ejecución.

## 3 · Orden de ejecución recomendado (cola de remediación)

1. **GA-GOV-03** (suite verde reproducible + CI en push) — prerrequisito de toda certificación.
2. **R-199** (P1 seguridad) · **R-200** · **R-203** · **R-204** · **R-221** · **R-208** · **R-201** (bloque de seguridad; sin UI).
3. **R-205** (P-03 recepción) · **R-190** (ubicación en lotes sin galpón) · **R-191** (transición de fase) · **R-206** (vacíos) · **R-209** · **R-210** · **R-211** — familia del asistente/contratos de formulario (una tranche FE con RED unit + runtime).
4. **R-194** (Incubadora por UI, P-05) — depende de 3.
5. **R-192** · **R-193** · **P1-12-REOPEN** · **R-198** — integridad/ledger/auditoría (backend + detalle).
6. **R-196** · **R-195** · **R-215** — maestros/usuarios/errores.
7. **R-197** · **R-207** — revisión y reverso con superficie.
8. Compactos no bloqueantes: **R-202**, **R-212**, **R-213**, **R-216**, **R-218**, **R-219**, **R-220**; **R-217** en la fase SAP; **R-214** en Wave C.
9. Recertificación E2E de P-01…P-15 (UI + API) con artefactos de ejecución y UAT del propietario (GA-UAT-09 retry, R-153 7 casos, GA-F01) — sólo entonces se reevalúa el veredicto GO/NO-GO.
